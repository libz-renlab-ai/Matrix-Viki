{
  "name": "@viki/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "viki": "./src/bin.ts"
  },
  "scripts": {
    "prebuild": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "tsup src/bin.ts --format esm && pnpm build:hook",
    "build:hook": "tsup --config tsup.hook.config.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.0",
    "@viki/adapters": "workspace:*",
    "@viki/core": "workspace:*",
    "@viki/ports": "workspace:*",
    "@viki/types": "workspace:*",
    "cli-table3": "^0.6.5",
    "js-tiktoken": "^1.0.12",
    "picocolors": "^1.1.1",
    "sqlite-vec": "0.1.9",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
