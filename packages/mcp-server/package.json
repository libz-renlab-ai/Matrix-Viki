{
  "name": "@teamagent/mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/server.ts",
  "exports": {
    ".": "./src/server.ts"
  },
  "bin": {
    "teamagent-mcp-server": "./src/server.ts"
  },
  "scripts": {
    "build": "tsup src/server.ts --format esm --dts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@teamagent/core": "workspace:*",
    "@teamagent/types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
