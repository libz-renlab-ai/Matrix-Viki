# M2.5-half：全自动错误识别与规则归类系统 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 TeamAgent 自动从 A/B/C/D/G/H 六类错误信号中提炼规则候选，暂存候选队列，经 `review-candidates` 交互式逐条确认后写入知识库，形成自动化飞轮。

**Architecture:** ErrorSignalCollector（聚合六路信号） → SignalFilter（efficient mode 粗筛）→ ErrorBatchBuilder（按 C/E/S/K 分组） → llmBasedKnowledgeExtractor（换 prompt） → CandidateQueue（SQLite 暂存） → review-candidates（交互审核 → 写库 → 校准 → 重编）。全部核心逻辑为纯函数，IO 在 Adapter/CLI 层。

**Tech Stack:** TypeScript, Node 22 `node:sqlite`（已有），vitest，现有 `@teamagent/adapters`/`ports`/`core`/`types`

---

## 文件清单

### 新增
| 文件 | 职责 |
|------|------|
| `packages/types/src/persisted-event.ts` | 追加 3 个 kind（修改现有文件）|
| `packages/ports/src/error-signal-collector.ts` | Port 接口 |
| `packages/ports/src/candidate-queue.ts` | Port 接口 |
| `packages/ports/src/__tests__/error-signal-collector-contract.ts` | 契约测试套件 |
| `packages/ports/src/__tests__/candidate-queue-contract.ts` | 契约测试套件 |
| `packages/ports/src/index.ts` | 追加 export（修改现有）|
| `packages/core/src/error-collector/cross-session-cluster.ts` | H 信号聚类（纯函数）|
| `packages/core/src/error-collector/cross-session-cluster.test.ts` | 单元测试 |
| `packages/core/src/error-collector/signal-filter.ts` | 粗筛（纯函数）|
| `packages/core/src/error-collector/signal-filter.test.ts` | 单元测试 |
| `packages/core/src/error-collector/error-batch-builder.ts` | prompt 合并（纯函数）|
| `packages/core/src/error-collector/error-batch-builder.test.ts` | 单元测试 |
| `packages/core/src/error-collector/error-extraction-prompt.ts` | 批量错误信号 prompt 工厂 |
| `packages/adapters/src/storage/sqlite/sqlite-candidate-queue.ts` | CandidateQueue SQLite 实现 |
| `packages/adapters/src/storage/sqlite/__tests__/sqlite-candidate-queue.test.ts` | 集成测试 |
| `packages/adapters/src/error-collector/composite-error-signal-collector.ts` | 六路信号聚合 Shell |
| `packages/adapters/src/error-collector/__tests__/composite-error-signal-collector.test.ts` | 单元测试 |
| `packages/cli/src/commands/scan-errors.ts` | scan-errors 命令 |
| `packages/cli/src/__tests__/scan-errors.test.ts` | CLI 单元测试 |
| `packages/cli/src/commands/review-candidates.ts` | review-candidates 命令 |
| `packages/cli/src/__tests__/review-candidates.test.ts` | CLI 单元测试 |

### 修改
| 文件 | 修改内容 |
|------|---------|
| `packages/types/src/persisted-event.ts` | kind union 追加 3 个 |
| `packages/ports/src/index.ts` | 导出 2 个新 Port |
| `packages/adapters/src/storage/sqlite/schema.ts` | 追加 `rule_candidates` DDL |
| `packages/cli/src/bin.ts` | 追加 `scan-errors` 和 `review-candidates` case |

---

## Task 1：追加 PersistedEvent kind

**Files:**
- Modify: `packages/types/src/persisted-event.ts`

- [ ] **Step 1: 在 `kind` union 末尾追加 3 个新事件类型**

在 `packages/types/src/persisted-event.ts` 的 `kind` 字段 union 里，在 `| "scenario.run"` 后面加：

```typescript
    | "error.candidate.added"    // CandidateQueue 入队（payload: { count: number }）
    | "error.candidate.approved" // 用户批准（payload: { knowledge_id: string }）
    | "error.candidate.rejected" // 用户拒绝（payload: { candidate_id: string }）
```

- [ ] **Step 2: typecheck 通过**

```bash
pnpm typecheck
```

Expected: 0 errors

- [ ] **Step 3: commit**

```bash
git add packages/types/src/persisted-event.ts
git commit -m "feat(m2.5-half): add error.candidate.* PersistedEvent kinds"
```

---

## Task 2：Port — ErrorSignalCollector 接口 + 契约测试

**Files:**
- Create: `packages/ports/src/error-signal-collector.ts`
- Create: `packages/ports/src/__tests__/error-signal-collector-contract.ts`
- Modify: `packages/ports/src/index.ts`

- [ ] **Step 1: 创建 Port 接口文件**

创建 `packages/ports/src/error-signal-collector.ts`：

```typescript
/**
 * 错误信号采集 Port。
 *
 * 信号类型：
 *   A - 用户纠正 AI（correction_moment）
 *   B - build/test 失败（hook-post.result, succeeded=false）
 *   C - AI override 被人类强行覆盖（ai.override.ignored）
 *   D - 同一任务多次连续失败（multi_failure）
 *   G - hook-pre.blocked 后用户继续（规则被绕过）
 *   H - 同一 tag/pattern 跨 session 重复出现（聚类）
 */
export interface RawErrorSignal {
  /** 信号唯一 ID */
  id: string;
  /** 信号类型 */
  signalType: "A" | "B" | "C" | "D" | "G" | "H";
  /** 信号权重 0-1，用于 efficient mode 阈值过滤 */
  weight: number;
  /** 来源 session ID 列表（H 信号可能来自多个 session） */
  sessionIds: string[];
  /** 供 LLM 提取的原文上下文（纠正对话片段 / 错误日志 / 聚类摘要等）*/
  context: string;
  /** LLM 提取时推荐使用的 category（仅提示，可被 LLM 覆盖）*/
  suggestedCategory?: "C" | "E" | "S" | "K";
  /** ISO 8601 */
  timestamp: string;
}

/** 采集 since 时间点之后的所有错误信号。 */
export interface ErrorSignalCollector {
  collect(since: Date): Promise<RawErrorSignal[]>;
}
```

- [ ] **Step 2: 创建契约测试套件**

创建 `packages/ports/src/__tests__/error-signal-collector-contract.ts`：

```typescript
import { describe, it, expect } from "vitest";
import type { ErrorSignalCollector, RawErrorSignal } from "../error-signal-collector.js";

function makeSignal(overrides: Partial<RawErrorSignal> = {}): RawErrorSignal {
  return {
    id: "sig-001",
    signalType: "B",
    weight: 0.7,
    sessionIds: ["sess-abc"],
    context: "build failed: tsc exited with code 1",
    timestamp: "2026-04-16T10:00:00Z",
    ...overrides,
  };
}

/**
 * 契约测试套件——任何 ErrorSignalCollector 实现都应通过。
 *
 * 使用方式：
 *   describe("MyCollector", () => {
 *     runErrorSignalCollectorContract(() => new MyCollector(...));
 *   });
 */
export function runErrorSignalCollectorContract(
  factory: (signals: RawErrorSignal[]) => ErrorSignalCollector,
): void {
  describe("ErrorSignalCollector contract", () => {
    it("returns empty array when no signals exist", async () => {
      const collector = factory([]);
      const result = await collector.collect(new Date("2026-01-01T00:00:00Z"));
      expect(result).toEqual([]);
    });

    it("returns signals after since date", async () => {
      const old = makeSignal({ id: "old", timestamp: "2026-04-15T00:00:00Z" });
      const fresh = makeSignal({ id: "fresh", timestamp: "2026-04-16T10:00:00Z" });
      const collector = factory([old, fresh]);
      const result = await collector.collect(new Date("2026-04-16T00:00:00Z"));
      expect(result.map((s) => s.id)).toContain("fresh");
      expect(result.map((s) => s.id)).not.toContain("old");
    });

    it("returned signals have required fields", async () => {
      const sig = makeSignal({ id: "check" });
      const collector = factory([sig]);
      const result = await collector.collect(new Date("2026-01-01T00:00:00Z"));
      expect(result.length).toBeGreaterThan(0);
      const s = result[0]!;
      expect(typeof s.id).toBe("string");
      expect(["A","B","C","D","G","H"]).toContain(s.signalType);
      expect(typeof s.weight).toBe("number");
      expect(Array.isArray(s.sessionIds)).toBe(true);
      expect(typeof s.context).toBe("string");
      expect(typeof s.timestamp).toBe("string");
    });

    it("weight is between 0 and 1", async () => {
      const sig = makeSignal({ weight: 0.5 });
      const collector = factory([sig]);
      const result = await collector.collect(new Date("2026-01-01T00:00:00Z"));
      for (const s of result) {
        expect(s.weight).toBeGreaterThanOrEqual(0);
        expect(s.weight).toBeLessThanOrEqual(1);
      }
    });
  });
}
```

- [ ] **Step 3: 追加 Port export**

在 `packages/ports/src/index.ts` 末尾添加：

```typescript
export type {
  ErrorSignalCollector,
  RawErrorSignal,
} from "./error-signal-collector.js";
```

- [ ] **Step 4: typecheck**

```bash
pnpm typecheck
```

Expected: 0 errors

- [ ] **Step 5: commit**

```bash
git add packages/ports/src/error-signal-collector.ts \
        packages/ports/src/__tests__/error-signal-collector-contract.ts \
        packages/ports/src/index.ts
git commit -m "feat(m2.5-half): ErrorSignalCollector port + contract test"
```

---

## Task 3：Port — CandidateQueue 接口 + 契约测试

**Files:**
- Create: `packages/ports/src/candidate-queue.ts`
- Create: `packages/ports/src/__tests__/candidate-queue-contract.ts`
- Modify: `packages/ports/src/index.ts`

- [ ] **Step 1: 创建 Port 接口**

创建 `packages/ports/src/candidate-queue.ts`：

```typescript
import type { KnowledgeEntry } from "@teamagent/types";

/** 候选规则条目（待人工审核）。 */
export interface RuleCandidate {
  id: string;
  /** 提取出的知识条目（尚未写入 KnowledgeStore） */
  entry: KnowledgeEntry;
  /** 来源信号摘要，展示给用户看 */
  sourceSignals: string;
  status: "pending" | "approved" | "rejected" | "skipped";
  created_at: string;
  reviewed_at?: string;
}

/** 待审核规则候选队列。 */
export interface CandidateQueue {
  /** 批量入队（id 重复则忽略） */
  enqueue(candidates: Omit<RuleCandidate, "status" | "created_at">[]): void;
  /** 返回所有 status=pending 的候选，按 created_at ASC */
  listPending(): RuleCandidate[];
  /** 更新状态。id 不存在时静默忽略。 */
  updateStatus(id: string, status: RuleCandidate["status"]): void;
  /** 总条目数（含所有状态） */
  count(): number;
}
```

- [ ] **Step 2: 创建契约测试套件**

创建 `packages/ports/src/__tests__/candidate-queue-contract.ts`：

```typescript
import { describe, it, expect, beforeEach } from "vitest";
import type { CandidateQueue, RuleCandidate } from "../candidate-queue.js";
import type { KnowledgeEntry } from "@teamagent/types";

function makeEntry(id = "e-001"): KnowledgeEntry {
  return {
    id,
    scope: { level: "personal" },
    category: "E",
    tags: ["build", "test"],
    type: "avoidance",
    nature: "objective",
    trigger: "build fails on Windows",
    wrong_pattern: "fileParallelism: true",
    correct_pattern: "fileParallelism: false",
    reasoning: "OOM on Windows vitest concurrent mode",
    confidence: 0.6,
    enforcement: "suggest",
    status: "active",
    hit_count: 0,
    success_count: 0,
    override_count: 0,
    evidence: { success_sessions: 0, success_users: 0, correction_sessions: 0 },
    created_at: "2026-04-16T10:00:00Z",
    last_hit_at: "",
    last_validated_at: "",
    source: "accumulated",
    conflict_with: [],
    current_tier: "experimental",
    max_tier_ever: "experimental",
    tier_entered_at: "",
    demerit: 0,
    demerit_last_updated: "",
    resurrect_count: 0,
  };
}

function makeCandidate(
  id = "cand-001",
  entryId = "e-001",
): Omit<RuleCandidate, "status" | "created_at"> {
  return { id, entry: makeEntry(entryId), sourceSignals: "B×2 跨 2 session" };
}

/**
 * 契约测试套件——任何 CandidateQueue 实现都应通过。
 *
 * 使用方式：
 *   describe("SqliteCandidateQueue", () => {
 *     runCandidateQueueContract(() => new SqliteCandidateQueue(openDb(":memory:")));
 *   });
 */
export function runCandidateQueueContract(factory: () => CandidateQueue): void {
  describe("CandidateQueue contract", () => {
    let queue: CandidateQueue;

    beforeEach(() => {
      queue = factory();
    });

    it("starts empty", () => {
      expect(queue.listPending()).toEqual([]);
      expect(queue.count()).toBe(0);
    });

    it("enqueue + listPending returns pending items", () => {
      queue.enqueue([makeCandidate("c1", "e1")]);
      const pending = queue.listPending();
      expect(pending).toHaveLength(1);
      expect(pending[0]!.id).toBe("c1");
      expect(pending[0]!.status).toBe("pending");
    });

    it("duplicate id is ignored on second enqueue", () => {
      queue.enqueue([makeCandidate("dup", "e1")]);
      queue.enqueue([makeCandidate("dup", "e1")]);
      expect(queue.count()).toBe(1);
    });

    it("updateStatus removes from pending", () => {
      queue.enqueue([makeCandidate("c1", "e1")]);
      queue.updateStatus("c1", "approved");
      expect(queue.listPending()).toHaveLength(0);
      expect(queue.count()).toBe(1); // still in table
    });

    it("updateStatus on non-existent id does not throw", () => {
      expect(() => queue.updateStatus("ghost", "rejected")).not.toThrow();
    });

    it("count includes non-pending items", () => {
      queue.enqueue([makeCandidate("c1", "e1"), makeCandidate("c2", "e2")]);
      queue.updateStatus("c1", "approved");
      expect(queue.count()).toBe(2);
    });

    it("listPending returns items sorted by created_at ASC", () => {
      queue.enqueue([makeCandidate("c1", "e1"), makeCandidate("c2", "e2")]);
      const ids = queue.listPending().map((c) => c.id);
      expect(ids[0]).toBe("c1");
    });
  });
}
```

- [ ] **Step 3: 追加 Port export**

在 `packages/ports/src/index.ts` 末尾追加：

```typescript
export type { CandidateQueue, RuleCandidate } from "./candidate-queue.js";
```

- [ ] **Step 4: typecheck**

```bash
pnpm typecheck
```

Expected: 0 errors

- [ ] **Step 5: commit**

```bash
git add packages/ports/src/candidate-queue.ts \
        packages/ports/src/__tests__/candidate-queue-contract.ts \
        packages/ports/src/index.ts
git commit -m "feat(m2.5-half): CandidateQueue port + contract test"
```

---

## Task 4：Core — cross-session-cluster（H 信号聚类）

**Files:**
- Create: `packages/core/src/error-collector/cross-session-cluster.ts`
- Create: `packages/core/src/error-collector/__tests__/cross-session-cluster.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `packages/core/src/error-collector/__tests__/cross-session-cluster.test.ts`：

```typescript
import { describe, it, expect } from "vitest";
import { clusterByTag } from "../cross-session-cluster.js";
import type { RawErrorSignal } from "@teamagent/ports";

function sig(
  id: string,
  sessionId: string,
  context: string,
  weight = 0.6,
): RawErrorSignal {
  return {
    id,
    signalType: "B",
    weight,
    sessionIds: [sessionId],
    context,
    timestamp: "2026-04-16T10:00:00Z",
  };
}

describe("clusterByTag", () => {
  it("returns empty array for empty input", () => {
    expect(clusterByTag([], 2)).toEqual([]);
  });

  it("clusters signals from different sessions sharing the same context keyword", () => {
    const signals = [
      sig("s1", "sess-1", "vitest fileParallelism OOM error"),
      sig("s2", "sess-2", "vitest fileParallelism memory crash"),
      sig("s3", "sess-3", "unrelated babel config issue"),
    ];
    const clusters = clusterByTag(signals, 2);
    // vitest 跨两个不同 session 出现 → 应生成 H 信号
    expect(clusters.length).toBeGreaterThanOrEqual(1);
    const vitestCluster = clusters.find((c) =>
      c.context.includes("vitest"),
    );
    expect(vitestCluster).toBeDefined();
    expect(vitestCluster!.signalType).toBe("H");
    expect(vitestCluster!.sessionIds.length).toBeGreaterThanOrEqual(2);
  });

  it("does NOT cluster if same keyword appears in only 1 session", () => {
    const signals = [
      sig("s1", "sess-1", "vitest fileParallelism error"),
      sig("s2", "sess-1", "vitest again same session"), // 同一 session
    ];
    const clusters = clusterByTag(signals, 2);
    expect(clusters.length).toBe(0);
  });

  it("H signal weight is proportional to session count", () => {
    const signals = [
      sig("s1", "sess-1", "tsc type error unknown property"),
      sig("s2", "sess-2", "tsc type error missing field"),
      sig("s3", "sess-3", "tsc type error incompatible types"),
    ];
    const clusters = clusterByTag(signals, 2);
    const tscCluster = clusters.find((c) => c.context.includes("tsc"));
    if (tscCluster) {
      expect(tscCluster.weight).toBeGreaterThan(0.5);
    }
  });
});
```

- [ ] **Step 2: 验证测试失败**

```bash
pnpm test packages/core/src/error-collector/__tests__/cross-session-cluster.test.ts
```

Expected: FAIL — "clusterByTag is not a function" or module not found

- [ ] **Step 3: 实现 cross-session-cluster**

创建 `packages/core/src/error-collector/cross-session-cluster.ts`：

```typescript
import type { RawErrorSignal } from "@teamagent/ports";

/**
 * H 信号聚类：从一批原始信号中找出在 ≥minSessions 个不同 session 里出现的关键词，
 * 聚合成 H 类型信号返回。
 *
 * 纯函数——不依赖任何 IO。
 *
 * 算法：
 * 1. 对每条信号的 context 提取"关键词"（长度≥4 的小写 token，去除停用词）
 * 2. 对关键词建立 Map<keyword, Set<sessionId>>
 * 3. 找出跨 session 数 ≥ minSessions 的关键词，每个关键词生成一条 H 信号
 */
export function clusterByTag(
  signals: RawErrorSignal[],
  minSessions: number,
): RawErrorSignal[] {
  if (signals.length === 0) return [];

  // keyword → sessionId set
  const keywordSessions = new Map<string, Set<string>>();
  // keyword → 原始信号 id 集合（用于 H 信号 id 生成）
  const keywordSignalIds = new Map<string, string[]>();

  for (const sig of signals) {
    const tokens = tokenize(sig.context);
    const sessionId = sig.sessionIds[0] ?? "unknown";
    for (const token of tokens) {
      if (!keywordSessions.has(token)) {
        keywordSessions.set(token, new Set());
        keywordSignalIds.set(token, []);
      }
      keywordSessions.get(token)!.add(sessionId);
      keywordSignalIds.get(token)!.push(sig.id);
    }
  }

  const result: RawErrorSignal[] = [];

  for (const [keyword, sessionSet] of keywordSessions.entries()) {
    if (sessionSet.size < minSessions) continue;

    const sessionIds = Array.from(sessionSet);
    const matchingSignals = signals.filter(
      (s) => s.sessionIds.some((id) => sessionSet.has(id)) &&
              s.context.toLowerCase().includes(keyword),
    );

    // weight = min(sessionCount / 5, 1) — 5 个 session 达到满权重
    const weight = Math.min(sessionSet.size / 5, 1);

    const contextSummary = [
      `[H 聚类] 关键词 "${keyword}" 在 ${sessionSet.size} 个不同 session 中重复出现`,
      `相关上下文片段：`,
      ...matchingSignals
        .slice(0, 3)
        .map((s) => `  - ${s.context.slice(0, 120)}`),
    ].join("\n");

    result.push({
      id: `h-${keyword}-${sessionIds.join("-").slice(0, 20)}`,
      signalType: "H",
      weight,
      sessionIds,
      context: contextSummary,
      suggestedCategory: undefined,
      timestamp: new Date().toISOString(),
    });
  }

  return result;
}

const STOP_WORDS = new Set([
  "the", "and", "for", "with", "from", "that", "this", "have",
  "error", "fail", "failed", "failure", "could", "would", "should",
]);

function tokenize(text: string): string[] {
  return text
    .toLowerCase()
    .split(/[\s\-_./\\:,;()[\]{}'"!?]+/)
    .filter((t) => t.length >= 4 && !STOP_WORDS.has(t));
}
```

- [ ] **Step 4: 测试通过**

```bash
pnpm test packages/core/src/error-collector/__tests__/cross-session-cluster.test.ts
```

Expected: PASS

- [ ] **Step 5: typecheck**

```bash
pnpm typecheck
```

Expected: 0 errors

- [ ] **Step 6: commit**

```bash
git add packages/core/src/error-collector/
git commit -m "feat(m2.5-half): cross-session-cluster — H signal tag clustering"
```

---

## Task 5：Core — signal-filter（efficient mode 粗筛）

**Files:**
- Create: `packages/core/src/error-collector/signal-filter.ts`
- Create: `packages/core/src/error-collector/__tests__/signal-filter.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `packages/core/src/error-collector/__tests__/signal-filter.test.ts`：

```typescript
import { describe, it, expect } from "vitest";
import { filterSignals } from "../signal-filter.js";
import type { RawErrorSignal } from "@teamagent/ports";

function sig(
  id: string,
  sessionId: string,
  weight: number,
  knowledgeId?: string,
): RawErrorSignal {
  return {
    id,
    signalType: "B",
    weight,
    sessionIds: [sessionId],
    context: `error context for ${id}`,
    timestamp: "2026-04-16T10:00:00Z",
    suggestedCategory: knowledgeId as any,
  };
}

describe("filterSignals", () => {
  it("returns empty array for empty input", () => {
    expect(filterSignals([], { weightThreshold: 0.3, minSessions: 2 })).toEqual([]);
  });

  it("removes signals below weight threshold", () => {
    const signals = [
      sig("low", "s1", 0.2),
      sig("ok", "s2", 0.5),
    ];
    const result = filterSignals(signals, { weightThreshold: 0.3, minSessions: 1 });
    expect(result.map((s) => s.id)).not.toContain("low");
    expect(result.map((s) => s.id)).toContain("ok");
  });

  it("removes signals appearing in fewer than minSessions unique sessions", () => {
    const signals = [
      sig("one-session-1", "s1", 0.8),
      sig("one-session-2", "s1", 0.8), // same session
      sig("two-sessions", "s1", 0.8),
    ];
    // Override: give "two-sessions" two sessions
    signals[2]!.sessionIds = ["s1", "s2"];

    const result = filterSignals(signals, { weightThreshold: 0.1, minSessions: 2 });
    expect(result.map((s) => s.id)).toContain("two-sessions");
    expect(result.map((s) => s.id)).not.toContain("one-session-1");
  });

  it("deduplicates: keeps highest-weight signal among duplicates with same id prefix", () => {
    const signals = [
      { ...sig("dup", "s1", 0.5), timestamp: "2026-04-15T00:00:00Z" },
      { ...sig("dup", "s2", 0.8), timestamp: "2026-04-16T00:00:00Z" },
    ];
    // Same id → deduplicate keeping the one with higher weight
    const result = filterSignals(signals, { weightThreshold: 0.1, minSessions: 1 });
    expect(result.filter((s) => s.id === "dup")).toHaveLength(1);
    expect(result.find((s) => s.id === "dup")!.weight).toBe(0.8);
  });

  it("H signals bypass minSessions check (already clustered)", () => {
    const hSig: RawErrorSignal = {
      id: "h-cluster",
      signalType: "H",
      weight: 0.7,
      sessionIds: ["s1", "s2"],
      context: "cluster context",
      timestamp: "2026-04-16T10:00:00Z",
    };
    const result = filterSignals([hSig], { weightThreshold: 0.3, minSessions: 5 });
    expect(result.map((s) => s.id)).toContain("h-cluster");
  });
});
```

- [ ] **Step 2: 验证测试失败**

```bash
pnpm test packages/core/src/error-collector/__tests__/signal-filter.test.ts
```

Expected: FAIL

- [ ] **Step 3: 实现 signal-filter**

创建 `packages/core/src/error-collector/signal-filter.ts`：

```typescript
import type { RawErrorSignal } from "@teamagent/ports";

export interface FilterOptions {
  /** 最低权重阈值，低于此值的信号被丢弃。默认 0.3。 */
  weightThreshold: number;
  /** 最少来源 session 数（H 信号豁免）。默认 2。 */
  minSessions: number;
}

/**
 * efficient mode 粗筛：去重 + 权重阈值 + 频率门控。
 * 纯函数，不依赖任何 IO。
 */
export function filterSignals(
  signals: RawErrorSignal[],
  opts: FilterOptions,
): RawErrorSignal[] {
  if (signals.length === 0) return [];

  // 1. 去重：相同 id 保留权重最高的
  const dedupMap = new Map<string, RawErrorSignal>();
  for (const sig of signals) {
    const existing = dedupMap.get(sig.id);
    if (!existing || sig.weight > existing.weight) {
      dedupMap.set(sig.id, sig);
    }
  }

  return Array.from(dedupMap.values()).filter((sig) => {
    // 2. 权重阈值
    if (sig.weight < opts.weightThreshold) return false;

    // 3. 频率门控（H 信号已聚类，不再检查 session 数）
    if (sig.signalType !== "H") {
      const uniqueSessions = new Set(sig.sessionIds).size;
      if (uniqueSessions < opts.minSessions) return false;
    }

    return true;
  });
}
```

- [ ] **Step 4: 测试通过**

```bash
pnpm test packages/core/src/error-collector/__tests__/signal-filter.test.ts
```

Expected: PASS

- [ ] **Step 5: commit**

```bash
git add packages/core/src/error-collector/signal-filter.ts \
        packages/core/src/error-collector/__tests__/signal-filter.test.ts
git commit -m "feat(m2.5-half): signal-filter — dedup + weight + frequency gate"
```

---

## Task 6：Core — error-batch-builder + prompt 工厂

**Files:**
- Create: `packages/core/src/error-collector/error-batch-builder.ts`
- Create: `packages/core/src/error-collector/__tests__/error-batch-builder.test.ts`
- Create: `packages/core/src/error-collector/error-extraction-prompt.ts`

- [ ] **Step 1: 写失败测试**

创建 `packages/core/src/error-collector/__tests__/error-batch-builder.test.ts`：

```typescript
import { describe, it, expect } from "vitest";
import { buildErrorBatches } from "../error-batch-builder.js";
import type { RawErrorSignal } from "@teamagent/ports";

function sig(
  id: string,
  category: "C" | "E" | "S" | "K",
  context = "some error",
): RawErrorSignal {
  return {
    id,
    signalType: "B",
    weight: 0.7,
    sessionIds: ["s1"],
    context,
    suggestedCategory: category,
    timestamp: "2026-04-16T10:00:00Z",
  };
}

describe("buildErrorBatches", () => {
  it("returns empty array for empty input", () => {
    expect(buildErrorBatches([])).toEqual([]);
  });

  it("groups signals by suggestedCategory", () => {
    const signals = [
      sig("c1", "C"),
      sig("e1", "E"),
      sig("c2", "C"),
    ];
    const batches = buildErrorBatches(signals);
    expect(batches.length).toBe(2); // C and E
    const cBatch = batches.find((b) => b.category === "C");
    expect(cBatch!.signals).toHaveLength(2);
  });

  it("assigns uncategorized signals to E (engineering) by default", () => {
    const uncategorized: RawErrorSignal = {
      id: "unc",
      signalType: "B",
      weight: 0.6,
      sessionIds: ["s1"],
      context: "some build error",
      timestamp: "2026-04-16T10:00:00Z",
    };
    const batches = buildErrorBatches([uncategorized]);
    expect(batches[0]!.category).toBe("E");
  });

  it("each batch has a non-empty prompt string", () => {
    const signals = [sig("c1", "C", "typescript error in file.ts")];
    const batches = buildErrorBatches(signals);
    expect(batches[0]!.prompt.length).toBeGreaterThan(50);
  });
});
```

- [ ] **Step 2: 验证失败**

```bash
pnpm test packages/core/src/error-collector/__tests__/error-batch-builder.test.ts
```

Expected: FAIL

- [ ] **Step 3: 实现 error-extraction-prompt**

创建 `packages/core/src/error-collector/error-extraction-prompt.ts`：

```typescript
import type { RawErrorSignal } from "@teamagent/ports";

/**
 * 为一批同类型（same category）错误信号构造 LLM 提取 prompt。
 * 纯函数。
 */
export function buildBatchErrorExtractionPrompt(
  signals: RawErrorSignal[],
  category: "C" | "E" | "S" | "K",
): string {
  const categoryDesc: Record<"C" | "E" | "S" | "K", string> = {
    C: "代码层（语法、类型、API 用法）",
    E: "工程层（架构、依赖、工具链、构建）",
    S: "策略层（任务分解、实现顺序、取舍）",
    K: "认知层（用户偏好、心智模型、协作方式）",
  };

  const signalBlock = signals
    .map(
      (s, i) =>
        `--- 信号 ${i + 1} [${s.signalType}] weight=${s.weight.toFixed(2)} sessions=${s.sessionIds.length} ---\n${s.context.trim()}`,
    )
    .join("\n\n");

  return `你是知识提取器。下面是 ${signals.length} 条来自开发过程的错误信号，类别为 ${category}（${categoryDesc[category]}）。

请分析这些信号，提炼出 1-3 条有价值的"知识条目"（如果信号太弱或内容重复，提炼更少条甚至 0 条）。

【错误信号】
${signalBlock}

【输出字段（每条知识条目）】
- category: "${category}"（固定）
- tags: string[] 自由标签，2-5 个短词
- type: "avoidance" | "practice"
- nature: "objective" | "subjective"
- trigger: string 何时生效，通用场景描述
- wrong_pattern: string 错误做法关键字（可被字符串匹配）；不适用填 ""
- correct_pattern: string 正确做法一句话
- reasoning: string 一句话解释原因

【严格要求】
1. 只输出一个 JSON 数组（在 \`\`\`json fenced block 里），数组元素是 0-3 个知识条目对象
2. 如果所有信号都太弱或太私人化，输出空数组 \`[]\`
3. 不要输出除 JSON 以外的任何文字

【示例输出】
\`\`\`json
[
  {
    "category": "E",
    "tags": ["vitest", "windows", "concurrency"],
    "type": "avoidance",
    "nature": "objective",
    "trigger": "在 Windows 环境下配置 vitest",
    "wrong_pattern": "fileParallelism: true",
    "correct_pattern": "fileParallelism: false",
    "reasoning": "Windows 下 vitest 并发模式会导致 OOM，必须顺序跑"
  }
]
\`\`\``;
}
```

- [ ] **Step 4: 实现 error-batch-builder**

创建 `packages/core/src/error-collector/error-batch-builder.ts`：

```typescript
import type { RawErrorSignal } from "@teamagent/ports";
import { buildBatchErrorExtractionPrompt } from "./error-extraction-prompt.js";

export interface ErrorBatch {
  category: "C" | "E" | "S" | "K";
  signals: RawErrorSignal[];
  prompt: string;
}

/**
 * 将信号按 suggestedCategory 分组，每组生成一个 BatchPrompt。
 * 无 suggestedCategory 的信号归入 "E"（工程层）。
 * 纯函数。
 */
export function buildErrorBatches(signals: RawErrorSignal[]): ErrorBatch[] {
  if (signals.length === 0) return [];

  const groups = new Map<"C" | "E" | "S" | "K", RawErrorSignal[]>();

  for (const sig of signals) {
    const cat = sig.suggestedCategory ?? "E";
    if (!groups.has(cat)) groups.set(cat, []);
    groups.get(cat)!.push(sig);
  }

  return Array.from(groups.entries()).map(([category, sigs]) => ({
    category,
    signals: sigs,
    prompt: buildBatchErrorExtractionPrompt(sigs, category),
  }));
}
```

- [ ] **Step 5: 测试通过**

```bash
pnpm test packages/core/src/error-collector/__tests__/error-batch-builder.test.ts
```

Expected: PASS

- [ ] **Step 6: commit**

```bash
git add packages/core/src/error-collector/error-batch-builder.ts \
        packages/core/src/error-collector/error-extraction-prompt.ts \
        packages/core/src/error-collector/__tests__/error-batch-builder.test.ts
git commit -m "feat(m2.5-half): error-batch-builder + batch extraction prompt"
```

---

## Task 7：Adapter — SqliteCandidateQueue + schema

**Files:**
- Modify: `packages/adapters/src/storage/sqlite/schema.ts`
- Create: `packages/adapters/src/storage/sqlite/sqlite-candidate-queue.ts`
- Create: `packages/adapters/src/storage/sqlite/__tests__/sqlite-candidate-queue.test.ts`

- [ ] **Step 1: 在 schema.ts 的 INIT_SQL 末尾追加 DDL**

在 `packages/adapters/src/storage/sqlite/schema.ts` 的 `INIT_SQL` 字符串内，在 `INSERT OR IGNORE INTO schema_version` 一行之前，添加：

```sql
-- 候选规则队列（M2.5-half，review-candidates 用）
CREATE TABLE IF NOT EXISTS rule_candidates (
  id          TEXT PRIMARY KEY,
  entry_json  TEXT NOT NULL,
  source_signals TEXT NOT NULL,
  status      TEXT NOT NULL DEFAULT 'pending'
    CHECK(status IN ('pending','approved','rejected','skipped')),
  created_at  TEXT NOT NULL,
  reviewed_at TEXT
);

CREATE INDEX IF NOT EXISTS idx_candidates_status ON rule_candidates(status, created_at ASC);
```

- [ ] **Step 2: 写失败测试（契约测试复用）**

创建 `packages/adapters/src/storage/sqlite/__tests__/sqlite-candidate-queue.test.ts`：

```typescript
import { describe, beforeEach, afterEach } from "vitest";
import { runCandidateQueueContract } from "@teamagent/ports/contracts";
import { SqliteCandidateQueue } from "../sqlite-candidate-queue.js";
import { openDb, closeDb } from "../schema.js";
import type { DatabaseSync } from "node:sqlite";

// node:sqlite is experimental; load via createRequire to avoid vite static-import issues
import { createRequire } from "node:module";
const require = createRequire(import.meta.url);
const { DatabaseSync } = require("node:sqlite") as typeof import("node:sqlite");

describe("SqliteCandidateQueue", () => {
  let db: ReturnType<typeof openDb>;

  beforeEach(() => {
    db = openDb(":memory:");
  });

  afterEach(() => {
    closeDb(db);
  });

  runCandidateQueueContract(() => new SqliteCandidateQueue(db));
});
```

注意：`@teamagent/ports/contracts` subpath 暴露契约套件（现有 `knowledge-store-contract.ts` 已采用此模式）。检查 `packages/ports/package.json` 的 `exports` 字段，如果还没有 `"./contracts"` subpath，需要添加。

- [ ] **Step 3: 检查并按需添加 ports/contracts subpath export**

读取 `packages/ports/package.json`，找到 `exports` 字段。如果没有 `"./contracts"` 条目，添加：

```json
"./contracts": {
  "import": "./src/__tests__/index.ts"
}
```

再创建 `packages/ports/src/__tests__/index.ts`（如果不存在）：

```typescript
export { runKnowledgeStoreContract } from "./knowledge-store-contract.js";
export { runLLMClientContract } from "./llm-client-contract.js";
export { runErrorSignalCollectorContract } from "./error-signal-collector-contract.js";
export { runCandidateQueueContract } from "./candidate-queue-contract.js";
```

如果该文件已存在，只追加后两行。

- [ ] **Step 4: 验证测试失败**

```bash
pnpm test packages/adapters/src/storage/sqlite/__tests__/sqlite-candidate-queue.test.ts
```

Expected: FAIL — SqliteCandidateQueue not found

- [ ] **Step 5: 实现 SqliteCandidateQueue**

创建 `packages/adapters/src/storage/sqlite/sqlite-candidate-queue.ts`：

```typescript
import { createRequire } from "node:module";
const require = createRequire(import.meta.url);
const { DatabaseSync } = require("node:sqlite") as typeof import("node:sqlite");
import type { DatabaseSync as DatabaseSyncType } from "node:sqlite";
import type { CandidateQueue, RuleCandidate } from "@teamagent/ports";
import type { KnowledgeEntry } from "@teamagent/types";

export class SqliteCandidateQueue implements CandidateQueue {
  private readonly db: DatabaseSyncType;

  constructor(db: DatabaseSyncType) {
    this.db = db;
  }

  enqueue(candidates: Omit<RuleCandidate, "status" | "created_at">[]): void {
    const stmt = this.db.prepare(`
      INSERT OR IGNORE INTO rule_candidates
        (id, entry_json, source_signals, status, created_at)
      VALUES (?, ?, ?, 'pending', ?)
    `);
    const now = new Date().toISOString();
    for (const c of candidates) {
      stmt.run(c.id, JSON.stringify(c.entry), c.sourceSignals, now);
    }
  }

  listPending(): RuleCandidate[] {
    const rows = this.db
      .prepare(
        `SELECT * FROM rule_candidates WHERE status = 'pending' ORDER BY created_at ASC`,
      )
      .all() as any[];
    return rows.map(this.hydrate);
  }

  updateStatus(id: string, status: RuleCandidate["status"]): void {
    this.db
      .prepare(
        `UPDATE rule_candidates SET status = ?, reviewed_at = ? WHERE id = ?`,
      )
      .run(status, new Date().toISOString(), id);
  }

  count(): number {
    const row = this.db
      .prepare(`SELECT COUNT(*) as n FROM rule_candidates`)
      .get() as any;
    return row.n as number;
  }

  private hydrate = (row: any): RuleCandidate => ({
    id: row.id,
    entry: JSON.parse(row.entry_json) as KnowledgeEntry,
    sourceSignals: row.source_signals,
    status: row.status as RuleCandidate["status"],
    created_at: row.created_at,
    reviewed_at: row.reviewed_at ?? undefined,
  });
}
```

- [ ] **Step 6: 测试通过**

```bash
pnpm test packages/adapters/src/storage/sqlite/__tests__/sqlite-candidate-queue.test.ts
```

Expected: PASS（所有契约测试）

- [ ] **Step 7: typecheck**

```bash
pnpm typecheck
```

Expected: 0 errors

- [ ] **Step 8: commit**

```bash
git add packages/adapters/src/storage/sqlite/schema.ts \
        packages/adapters/src/storage/sqlite/sqlite-candidate-queue.ts \
        packages/adapters/src/storage/sqlite/__tests__/sqlite-candidate-queue.test.ts \
        packages/ports/src/__tests__/index.ts \
        packages/ports/package.json
git commit -m "feat(m2.5-half): SqliteCandidateQueue + rule_candidates schema"
```

---

## Task 8：Adapter — CompositeErrorSignalCollector（六路聚合）

**Files:**
- Create: `packages/adapters/src/error-collector/composite-error-signal-collector.ts`
- Create: `packages/adapters/src/error-collector/__tests__/composite-error-signal-collector.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `packages/adapters/src/error-collector/__tests__/composite-error-signal-collector.test.ts`：

```typescript
import { describe, it, expect } from "vitest";
import { CompositeErrorSignalCollector } from "../composite-error-signal-collector.js";
import type { PersistedEvent } from "@teamagent/types";
import type { ParsedSession } from "@teamagent/types";

function makeEvent(
  kind: PersistedEvent["kind"],
  extra: Partial<PersistedEvent> = {},
): PersistedEvent {
  return {
    id: `evt-${Math.random().toString(36).slice(2)}`,
    kind,
    timestamp: "2026-04-16T10:00:00Z",
    schema_version: 1,
    ...extra,
  };
}

function makeSession(id: string): ParsedSession {
  return {
    sessionId: id,
    turns: [],
    startedAt: "2026-04-16T09:00:00Z",
    endedAt: "2026-04-16T10:00:00Z",
  };
}

describe("CompositeErrorSignalCollector", () => {
  it("returns B signals from hook-post.result failures", async () => {
    const events: PersistedEvent[] = [
      makeEvent("hook-post.result", {
        result: { succeeded: false, exit_code: 1 },
        tool: { name: "Bash", input: { command: "pnpm test" } },
        session_id: "sess-1",
        timestamp: "2026-04-16T10:00:00Z",
      } as any),
    ];
    const collector = new CompositeErrorSignalCollector({
      events,
      sessions: [],
      since: new Date("2026-04-16T00:00:00Z"),
    });
    const signals = await collector.collect(new Date("2026-04-16T00:00:00Z"));
    const bSignals = signals.filter((s) => s.signalType === "B");
    expect(bSignals.length).toBeGreaterThanOrEqual(1);
  });

  it("returns C signals from ai.override.ignored events", async () => {
    const events: PersistedEvent[] = [
      makeEvent("ai.override.ignored", {
        session_id: "sess-2",
        timestamp: "2026-04-16T10:00:00Z",
      }),
    ];
    const collector = new CompositeErrorSignalCollector({
      events,
      sessions: [],
      since: new Date("2026-04-16T00:00:00Z"),
    });
    const signals = await collector.collect(new Date("2026-04-16T00:00:00Z"));
    expect(signals.some((s) => s.signalType === "C")).toBe(true);
  });

  it("filters out events before since date", async () => {
    const events: PersistedEvent[] = [
      makeEvent("hook-post.result", {
        result: { succeeded: false, exit_code: 1 },
        session_id: "sess-old",
        timestamp: "2026-04-14T10:00:00Z",
      } as any),
    ];
    const collector = new CompositeErrorSignalCollector({
      events,
      sessions: [],
      since: new Date("2026-04-16T00:00:00Z"),
    });
    const signals = await collector.collect(new Date("2026-04-16T00:00:00Z"));
    expect(signals.filter((s) => s.signalType === "B")).toHaveLength(0);
  });
});
```

- [ ] **Step 2: 验证失败**

```bash
pnpm test packages/adapters/src/error-collector/__tests__/composite-error-signal-collector.test.ts
```

Expected: FAIL

- [ ] **Step 3: 实现 CompositeErrorSignalCollector**

创建 `packages/adapters/src/error-collector/composite-error-signal-collector.ts`：

```typescript
import type { ErrorSignalCollector, RawErrorSignal } from "@teamagent/ports";
import type { PersistedEvent, ParsedSession } from "@teamagent/types";
import { ruleBasedCorrectionDetector } from "@teamagent/core";
import { clusterByTag } from "@teamagent/core";

export interface CompositeCollectorOptions {
  events: PersistedEvent[];
  sessions: ParsedSession[];
  since: Date;
  /** H 信号聚类的最小 session 数，默认 2 */
  minClusterSessions?: number;
}

/**
 * 聚合 A/B/C/D/G/H 六路信号的 Imperative Shell。
 * 纯数据聚合——IO（读文件、读 DB）在调用方完成并注入。
 */
export class CompositeErrorSignalCollector implements ErrorSignalCollector {
  private readonly opts: CompositeCollectorOptions;

  constructor(opts: CompositeCollectorOptions) {
    this.opts = opts;
  }

  async collect(since: Date): Promise<RawErrorSignal[]> {
    const signals: RawErrorSignal[] = [];
    const sinceIso = since.toISOString();

    // --- A: 纠正时刻 ---
    for (const session of this.opts.sessions) {
      if ((session.endedAt ?? session.startedAt) < sinceIso) continue;
      const corrections = ruleBasedCorrectionDetector.detect(session);
      for (const c of corrections) {
        signals.push({
          id: `a-${session.sessionId}-${c.turnIndex}`,
          signalType: "A",
          weight: c.weight,
          sessionIds: [session.sessionId],
          context: [
            `用户纠正：${c.correctionText}`,
            `AI 上一句：${c.previousAssistantText}`,
          ].join("\n"),
          suggestedCategory: "K",
          timestamp: c.timestamp,
        });
      }
    }

    // --- B: build/test 失败 ---
    for (const evt of this.opts.events) {
      if (evt.timestamp < sinceIso) continue;
      if (evt.kind !== "hook-post.result") continue;
      const result = (evt as any).result as { succeeded: boolean; exit_code?: number; stderr?: string } | undefined;
      if (!result || result.succeeded) continue;
      const tool = (evt as any).tool as { name: string; input: Record<string, unknown> } | undefined;
      signals.push({
        id: `b-${evt.id}`,
        signalType: "B",
        weight: 0.7,
        sessionIds: [(evt as any).session_id ?? "unknown"],
        context: [
          `工具：${tool?.name ?? "unknown"} 执行失败`,
          `exit_code: ${result.exit_code ?? "?"}`,
          result.stderr ? `stderr: ${result.stderr.slice(0, 300)}` : "",
          tool?.input ? `input: ${JSON.stringify(tool.input).slice(0, 200)}` : "",
        ]
          .filter(Boolean)
          .join("\n"),
        suggestedCategory: "E",
        timestamp: evt.timestamp,
      });
    }

    // --- C: ai.override.ignored ---
    for (const evt of this.opts.events) {
      if (evt.timestamp < sinceIso) continue;
      if (evt.kind !== "ai.override.ignored") continue;
      signals.push({
        id: `c-${evt.id}`,
        signalType: "C",
        weight: 0.8,
        sessionIds: [(evt as any).session_id ?? "unknown"],
        context: `AI 拒绝建议被人类强行覆盖（ai.override.ignored）\nevent_id: ${evt.id}`,
        suggestedCategory: "S",
        timestamp: evt.timestamp,
      });
    }

    // --- D: multi_failure（同 session 连续工具失败 ≥2） ---
    const sessionFailCounts = new Map<string, number>();
    for (const evt of this.opts.events) {
      if (evt.timestamp < sinceIso) continue;
      if (evt.kind !== "hook-post.result") continue;
      const result = (evt as any).result as { succeeded: boolean } | undefined;
      if (!result || result.succeeded) continue;
      const sid = (evt as any).session_id ?? "unknown";
      sessionFailCounts.set(sid, (sessionFailCounts.get(sid) ?? 0) + 1);
    }
    for (const [sid, count] of sessionFailCounts.entries()) {
      if (count >= 2) {
        signals.push({
          id: `d-${sid}`,
          signalType: "D",
          weight: Math.min(0.5 + count * 0.1, 0.9),
          sessionIds: [sid],
          context: `Session ${sid} 内工具连续失败 ${count} 次，可能存在系统性错误模式`,
          suggestedCategory: "E",
          timestamp: sinceIso,
        });
      }
    }

    // --- G: hook-pre.blocked 后用户继续 ---
    for (const evt of this.opts.events) {
      if (evt.timestamp < sinceIso) continue;
      if (evt.kind !== "hook-pre.blocked") continue;
      signals.push({
        id: `g-${evt.id}`,
        signalType: "G",
        weight: 0.6,
        sessionIds: [(evt as any).session_id ?? "unknown"],
        context: [
          `规则拦截被绕过（hook-pre.blocked）`,
          `knowledge_id: ${(evt as any).knowledge_id ?? "unknown"}`,
          `tool: ${JSON.stringify((evt as any).tool ?? {}).slice(0, 200)}`,
        ].join("\n"),
        suggestedCategory: "S",
        timestamp: evt.timestamp,
      });
    }

    // --- H: 跨 session 聚类 ---
    const minCluster = this.opts.minClusterSessions ?? 2;
    const hSignals = clusterByTag(signals, minCluster);
    signals.push(...hSignals);

    return signals;
  }
}
```

- [ ] **Step 4: 在 core 包中导出 clusterByTag**

读取 `packages/core/src/index.ts`（或等效导出文件），追加：

```typescript
export { clusterByTag } from "./error-collector/cross-session-cluster.js";
```

- [ ] **Step 5: 测试通过**

```bash
pnpm test packages/adapters/src/error-collector/__tests__/composite-error-signal-collector.test.ts
```

Expected: PASS

- [ ] **Step 6: 全量测试**

```bash
pnpm test
```

Expected: PASS

- [ ] **Step 7: commit**

```bash
git add packages/adapters/src/error-collector/ \
        packages/core/src/index.ts
git commit -m "feat(m2.5-half): CompositeErrorSignalCollector — A/B/C/D/G/H 六路聚合"
```

---

## Task 9：CLI — scan-errors 命令

**Files:**
- Create: `packages/cli/src/commands/scan-errors.ts`
- Create: `packages/cli/src/__tests__/scan-errors.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `packages/cli/src/__tests__/scan-errors.test.ts`：

```typescript
import { describe, it, expect } from "vitest";
import { parseScanErrorsArgs } from "../commands/scan-errors.js";

describe("parseScanErrorsArgs", () => {
  it("defaults to efficient mode", () => {
    const opts = parseScanErrorsArgs([]);
    expect(opts.mode).toBe("efficient");
  });

  it("parses --mode=full", () => {
    const opts = parseScanErrorsArgs(["--mode=full"]);
    expect(opts.mode).toBe("full");
  });

  it("parses --mode full (space)", () => {
    const opts = parseScanErrorsArgs(["--mode", "full"]);
    expect(opts.mode).toBe("full");
  });

  it("parses --min-freq=1", () => {
    const opts = parseScanErrorsArgs(["--min-freq=1"]);
    expect(opts.minFreq).toBe(1);
  });

  it("parses --dry-run flag", () => {
    const opts = parseScanErrorsArgs(["--dry-run"]);
    expect(opts.dryRun).toBe(true);
  });

  it("parses --quiet flag", () => {
    const opts = parseScanErrorsArgs(["--quiet"]);
    expect(opts.quiet).toBe(true);
  });

  it("parses --since=24h", () => {
    const opts = parseScanErrorsArgs(["--since=24h"]);
    expect(opts.sinceRaw).toBe("24h");
  });
});
```

- [ ] **Step 2: 验证失败**

```bash
pnpm test packages/cli/src/__tests__/scan-errors.test.ts
```

Expected: FAIL

- [ ] **Step 3: 实现 scan-errors.ts**

创建 `packages/cli/src/commands/scan-errors.ts`：

```typescript
import os from "node:os";
import path from "node:path";
import fs from "node:fs";
import {
  ClaudeSessionSource,
  ClaudeCodeLLMClient,
  DualLayerStore,
  SqliteEventLog,
  SqliteCandidateQueue,
  openDb,
} from "@teamagent/adapters";
import {
  filterSignals,
  buildErrorBatches,
} from "@teamagent/core";
import { CompositeErrorSignalCollector } from "@teamagent/adapters";
import type { LLMClient } from "@teamagent/ports";
import type { ParsedSession } from "@teamagent/types";

export interface ScanErrorsOptions {
  mode: "efficient" | "full";
  sinceRaw?: string;           // "24h" | ISO string | undefined
  minFreq: number;             // efficient mode 频率门控，默认 2
  dryRun: boolean;
  quiet: boolean;
  homeDir?: string;
  projectsRoot?: string;
  eventsDbPath?: string;
  candidatesDbPath?: string;
  llmClient?: LLMClient;
  now?: () => Date;
}

const SCAN_STATE_FILENAME = "scan-state.json";

function resolveSince(sinceRaw: string | undefined, homeDir: string, now: Date): Date {
  if (!sinceRaw) {
    // 读上次扫描时间
    const statePath = path.join(homeDir, ".teamagent", SCAN_STATE_FILENAME);
    try {
      const state = JSON.parse(fs.readFileSync(statePath, "utf-8"));
      if (state.lastScanAt) return new Date(state.lastScanAt);
    } catch {
      // 文件不存在，默认 24h 前
    }
    return new Date(now.getTime() - 24 * 60 * 60 * 1000);
  }

  if (/^\d+h$/.test(sinceRaw)) {
    const hours = parseInt(sinceRaw, 10);
    return new Date(now.getTime() - hours * 60 * 60 * 1000);
  }

  return new Date(sinceRaw);
}

function saveScanState(homeDir: string, now: Date, mode: string): void {
  const dir = path.join(homeDir, ".teamagent");
  fs.mkdirSync(dir, { recursive: true });
  fs.writeFileSync(
    path.join(dir, SCAN_STATE_FILENAME),
    JSON.stringify({ lastScanAt: now.toISOString(), lastScanMode: mode }, null, 2),
  );
}

export async function executeScanErrors(opts: ScanErrorsOptions = { mode: "efficient", minFreq: 2, dryRun: false, quiet: false }): Promise<string> {
  const home = opts.homeDir ?? os.homedir();
  const now = opts.now ? opts.now() : new Date();
  const since = resolveSince(opts.sinceRaw, home, now);
  const projectsRoot = opts.projectsRoot ?? path.join(home, ".claude", "projects");
  const eventsDbPath = opts.eventsDbPath ?? path.join(home, ".teamagent", "events.db");
  const candidatesDbPath = opts.candidatesDbPath ?? path.join(home, ".teamagent", "candidates.db");

  // 读 events
  let events: ReturnType<SqliteEventLog["readAll"]> = [];
  if (fs.existsSync(eventsDbPath)) {
    const eventLog = new SqliteEventLog(openDb(eventsDbPath));
    events = eventLog.readAll();
    eventLog.close();
  }

  // 读最近 sessions
  let sessions: ParsedSession[] = [];
  try {
    const src = new ClaudeSessionSource(projectsRoot);
    const recent = await src.listRecent(20);
    for (const meta of recent) {
      if (meta.startedAt < since.toISOString()) continue;
      try {
        sessions.push(await src.loadById(meta.sessionId));
      } catch {
        // 单个 session 读失败不影响整体
      }
    }
  } catch {
    // projects dir 不存在
  }

  // 采集信号
  const collector = new CompositeErrorSignalCollector({ events, sessions, since });
  let signals = await collector.collect(since);

  // efficient mode: 粗筛
  if (opts.mode === "efficient") {
    signals = filterSignals(signals, {
      weightThreshold: 0.3,
      minSessions: opts.minFreq,
    });
  }

  if (signals.length === 0) {
    if (!opts.quiet) return "📭 无新错误信号，知识库无需更新。\n";
    return "";
  }

  // 批量构建 prompts
  const batches = buildErrorBatches(signals);

  const lines: string[] = [];
  lines.push(`🔍 scan-errors [${opts.mode} mode] — since ${since.toISOString()}`);
  lines.push(`  信号数: ${signals.length}，批次数: ${batches.length}`);
  lines.push("");

  if (opts.dryRun) {
    for (const batch of batches) {
      lines.push(`  [dry-run] category=${batch.category} signals=${batch.signals.length}`);
      for (const s of batch.signals) {
        lines.push(`    - [${s.signalType}] w=${s.weight.toFixed(2)} ${s.context.slice(0, 80)}`);
      }
    }
    lines.push("");
    lines.push("  (dry-run 模式，未写入候选队列)");
    return lines.join("\n") + "\n";
  }

  // LLM 提取
  const llm = opts.llmClient ?? new ClaudeCodeLLMClient();
  fs.mkdirSync(path.dirname(candidatesDbPath), { recursive: true });
  const queue = new SqliteCandidateQueue(openDb(candidatesDbPath));

  let totalCandidates = 0;

  for (const batch of batches) {
    let rawResponse: string;
    try {
      rawResponse = await llm.complete(batch.prompt);
    } catch (e) {
      lines.push(`  ⚠ LLM 调用失败 (category=${batch.category}): ${String(e).slice(0, 100)}`);
      continue;
    }

    // 解析 JSON array
    let entries: Array<Record<string, unknown>> = [];
    try {
      const fenced = rawResponse.match(/```(?:json)?\s*\n?([\s\S]*?)\n?```/);
      const json = fenced ? fenced[1]!.trim() : rawResponse.trim();
      const parsed = JSON.parse(json);
      if (Array.isArray(parsed)) entries = parsed;
    } catch {
      lines.push(`  ⚠ LLM 响应解析失败 (category=${batch.category})`);
      continue;
    }

    for (const raw of entries) {
      const partial = validateExtractedEntry(raw);
      if (!partial) continue;

      const ts = now.toISOString().replace(/[-:T.Z]/g, "").slice(0, 14);
      const rand = Math.random().toString(36).slice(2, 8);
      const id = `cand-${ts}-${rand}`;
      const entryId = `pers-${ts}-${rand}`;

      const entry = buildFullEntry(partial, entryId, now);
      const sourceDesc = batch.signals
        .map((s) => `${s.signalType}×${s.sessionIds.length}`)
        .join(", ");

      queue.enqueue([{ id, entry, sourceSignals: sourceDesc }]);
      totalCandidates++;
    }
  }

  saveScanState(home, now, opts.mode);

  lines.push(`  ✓ 新增候选规则: ${totalCandidates} 条`);
  if (totalCandidates > 0) {
    lines.push(`  运行 teamagent review-candidates 审核`);
  }
  lines.push("");

  return lines.join("\n") + "\n";
}

function validateExtractedEntry(raw: Record<string, unknown>): Partial<import("@teamagent/types").KnowledgeEntry> | null {
  const { category, tags, type, nature, trigger, wrong_pattern, correct_pattern, reasoning } = raw;
  if (!["C","E","S","K"].includes(String(category))) return null;
  if (!["avoidance","practice"].includes(String(type))) return null;
  if (!["objective","subjective"].includes(String(nature))) return null;
  if (typeof trigger !== "string" || !trigger.trim()) return null;
  if (typeof correct_pattern !== "string" || !correct_pattern.trim()) return null;
  if (typeof reasoning !== "string" || !reasoning.trim()) return null;
  return {
    category: category as "C"|"E"|"S"|"K",
    tags: Array.isArray(tags) ? (tags as string[]).filter((t) => typeof t === "string") : [],
    type: type as "avoidance"|"practice",
    nature: nature as "objective"|"subjective",
    trigger: String(trigger).trim(),
    wrong_pattern: typeof wrong_pattern === "string" ? wrong_pattern : "",
    correct_pattern: String(correct_pattern).trim(),
    reasoning: String(reasoning).trim(),
  };
}

function buildFullEntry(
  partial: Partial<import("@teamagent/types").KnowledgeEntry>,
  id: string,
  now: Date,
): import("@teamagent/types").KnowledgeEntry {
  const ts = now.toISOString();
  return {
    id,
    scope: { level: "personal" },
    category: partial.category ?? "E",
    tags: partial.tags ?? [],
    type: partial.type ?? "avoidance",
    nature: partial.nature ?? "objective",
    trigger: partial.trigger ?? "",
    wrong_pattern: partial.wrong_pattern ?? "",
    correct_pattern: partial.correct_pattern ?? "",
    reasoning: partial.reasoning ?? "",
    confidence: 0.5,
    enforcement: "suggest",
    status: "active",
    hit_count: 0,
    success_count: 0,
    override_count: 0,
    evidence: { success_sessions: 0, success_users: 0, correction_sessions: 0 },
    created_at: ts,
    last_hit_at: "",
    last_validated_at: ts,
    source: "accumulated",
    conflict_with: [],
    current_tier: "experimental",
    max_tier_ever: "experimental",
    tier_entered_at: "",
    demerit: 0,
    demerit_last_updated: "",
    resurrect_count: 0,
  };
}

export function parseScanErrorsArgs(argv: string[]): ScanErrorsOptions {
  const opts: ScanErrorsOptions = {
    mode: "efficient",
    minFreq: 2,
    dryRun: false,
    quiet: false,
  };
  for (let i = 0; i < argv.length; i++) {
    const a = argv[i]!;
    if (a === "--mode" && argv[i + 1]) {
      const v = argv[++i]!;
      if (v === "full" || v === "efficient") opts.mode = v;
    } else if (a.startsWith("--mode=")) {
      const v = a.slice("--mode=".length);
      if (v === "full" || v === "efficient") opts.mode = v;
    } else if (a === "--min-freq" && argv[i + 1]) {
      opts.minFreq = parseInt(argv[++i]!, 10);
    } else if (a.startsWith("--min-freq=")) {
      opts.minFreq = parseInt(a.slice("--min-freq=".length), 10);
    } else if (a === "--dry-run") {
      opts.dryRun = true;
    } else if (a === "--quiet") {
      opts.quiet = true;
    } else if (a === "--since" && argv[i + 1]) {
      opts.sinceRaw = argv[++i];
    } else if (a.startsWith("--since=")) {
      opts.sinceRaw = a.slice("--since=".length);
    }
  }
  return opts;
}
```

- [ ] **Step 4: 测试通过**

```bash
pnpm test packages/cli/src/__tests__/scan-errors.test.ts
```

Expected: PASS

- [ ] **Step 5: 确保 SqliteCandidateQueue 从 @teamagent/adapters 导出**

读取 `packages/adapters/src/index.ts`，追加：

```typescript
export { SqliteCandidateQueue } from "./storage/sqlite/sqlite-candidate-queue.js";
export { CompositeErrorSignalCollector } from "./error-collector/composite-error-signal-collector.js";
```

同时确认 `filterSignals`、`buildErrorBatches` 从 `@teamagent/core` 导出。如未导出，在 `packages/core/src/index.ts` 追加对应行。

- [ ] **Step 6: typecheck**

```bash
pnpm typecheck
```

Expected: 0 errors

- [ ] **Step 7: commit**

```bash
git add packages/cli/src/commands/scan-errors.ts \
        packages/cli/src/__tests__/scan-errors.test.ts \
        packages/adapters/src/index.ts \
        packages/core/src/index.ts
git commit -m "feat(m2.5-half): scan-errors CLI command"
```

---

## Task 10：CLI — review-candidates 命令

**Files:**
- Create: `packages/cli/src/commands/review-candidates.ts`
- Create: `packages/cli/src/__tests__/review-candidates.test.ts`

- [ ] **Step 1: 写失败测试**

创建 `packages/cli/src/__tests__/review-candidates.test.ts`：

```typescript
import { describe, it, expect } from "vitest";
import { parseReviewCandidatesArgs } from "../commands/review-candidates.js";

describe("parseReviewCandidatesArgs", () => {
  it("defaults to no limit", () => {
    const opts = parseReviewCandidatesArgs([]);
    expect(opts.limit).toBeUndefined();
  });

  it("parses --limit=5", () => {
    const opts = parseReviewCandidatesArgs(["--limit=5"]);
    expect(opts.limit).toBe(5);
  });

  it("parses --limit 3 (space)", () => {
    const opts = parseReviewCandidatesArgs(["--limit", "3"]);
    expect(opts.limit).toBe(3);
  });
});
```

- [ ] **Step 2: 验证失败**

```bash
pnpm test packages/cli/src/__tests__/review-candidates.test.ts
```

Expected: FAIL

- [ ] **Step 3: 实现 review-candidates.ts**

创建 `packages/cli/src/commands/review-candidates.ts`：

```typescript
import os from "node:os";
import path from "node:path";
import fs from "node:fs";
import * as readline from "node:readline";
import {
  DualLayerStore,
  SqliteCandidateQueue,
  openDb,
  MarkdownCompiler,
  makeSkillCompiler,
} from "@teamagent/adapters";
import { runCalibrationPipeline, defaultCalibrator, runCompile } from "@teamagent/core";
import type { RuleCandidate } from "@teamagent/ports";
import type { KnowledgeEntry } from "@teamagent/types";

export interface ReviewCandidatesOptions {
  limit?: number;
  homeDir?: string;
  cwd?: string;
  candidatesDbPath?: string;
  projectDbPath?: string;
  userGlobalDbPath?: string;
  claudeMdPath?: string;
  now?: () => Date;
  /** 注入 readline（测试用）*/
  rl?: readline.Interface;
}

export async function executeReviewCandidates(
  opts: ReviewCandidatesOptions = {},
): Promise<string> {
  const home = opts.homeDir ?? os.homedir();
  const cwd = opts.cwd ?? process.cwd();
  const candidatesDbPath =
    opts.candidatesDbPath ?? path.join(home, ".teamagent", "candidates.db");
  const projectDbPath =
    opts.projectDbPath ?? path.join(cwd, ".teamagent", "knowledge.db");
  const userGlobalDbPath =
    opts.userGlobalDbPath ?? path.join(home, ".teamagent", "global.db");
  const claudeMdPath = opts.claudeMdPath ?? path.join(cwd, "CLAUDE.md");
  const now = opts.now ?? (() => new Date());

  if (!fs.existsSync(candidatesDbPath)) {
    return "📭 候选队列为空（candidates.db 不存在）。先运行 teamagent scan-errors。\n";
  }

  const queueDb = openDb(candidatesDbPath);
  const queue = new SqliteCandidateQueue(queueDb);
  let pending = queue.listPending();

  if (opts.limit !== undefined) {
    pending = pending.slice(0, opts.limit);
  }

  if (pending.length === 0) {
    queueDb.close();
    return "✅ 候选队列已清空，无待审核条目。\n";
  }

  fs.mkdirSync(path.dirname(projectDbPath), { recursive: true });
  fs.mkdirSync(path.dirname(userGlobalDbPath), { recursive: true });

  const store = new DualLayerStore({ projectDbPath, userGlobalDbPath });
  const projectStore = store.getProjectStore();

  const rl =
    opts.rl ??
    readline.createInterface({
      input: process.stdin,
      output: process.stdout,
    });

  const ask = (prompt: string): Promise<string> =>
    new Promise((resolve) => rl.question(prompt, resolve));

  const lines: string[] = [];
  lines.push(`📋 候选规则审核 — 共 ${pending.length} 条待审`);
  lines.push("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");

  let approved = 0;
  let rejected = 0;
  let skipped = 0;

  for (let i = 0; i < pending.length; i++) {
    const candidate = pending[i]!;
    const e = candidate.entry;

    process.stdout.write(`\n[${i + 1}/${pending.length}] category=${e.category}  tags=[${e.tags.join(", ")}]\n`);
    process.stdout.write(`  trigger:  ${e.trigger}\n`);
    if (e.wrong_pattern) process.stdout.write(`  wrong:    ${e.wrong_pattern}\n`);
    process.stdout.write(`  correct:  ${e.correct_pattern}\n`);
    process.stdout.write(`  reason:   ${e.reasoning}\n`);
    process.stdout.write(`  来源信号: ${candidate.sourceSignals}\n`);
    process.stdout.write(`  confidence: ${e.confidence.toFixed(2)}\n`);
    process.stdout.write("\n  [a]pprove  [r]eject  [s]kip  [q]uit\n");

    const answer = (await ask("> ")).trim().toLowerCase();

    if (answer === "q") {
      process.stdout.write("\n退出审核，剩余条目保留在队列中。\n");
      break;
    }

    if (answer === "a") {
      // 写入知识库
      const entry: KnowledgeEntry = { ...e, id: e.id };
      try {
        projectStore.add(entry);
        queue.updateStatus(candidate.id, "approved");
        approved++;
        process.stdout.write(`✓ 已写入知识库 (id: ${entry.id})\n`);
      } catch (err) {
        process.stdout.write(`⚠ 写入失败: ${String(err).slice(0, 100)}\n`);
      }
    } else if (answer === "r") {
      queue.updateStatus(candidate.id, "rejected");
      rejected++;
      process.stdout.write("✗ 已拒绝\n");
    } else {
      // s or anything else = skip
      queue.updateStatus(candidate.id, "skipped");
      skipped++;
      process.stdout.write("→ 已跳过（下次审核可见）\n");
    }
  }

  rl.close();

  // 若有批准，重新校准 + 重编 CLAUDE.md
  if (approved > 0) {
    process.stdout.write("\n重新校准 + 编译 CLAUDE.md…\n");
    try {
      await runCalibrationPipeline({
        calibrator: defaultCalibrator,
        store: projectStore as any,
        events: [],
        now,
      });
      const mdCompiler = new MarkdownCompiler(claudeMdPath, () => now().toISOString());
      await runCompile({
        store,
        markdownCompiler: mdCompiler,
        skillCompiler: makeSkillCompiler(),
      });
      process.stdout.write("✓ CLAUDE.md 已更新\n");
    } catch (err) {
      process.stdout.write(`⚠ 校准/编译失败: ${String(err).slice(0, 100)}\n`);
    }
  }

  store.close();
  queueDb.close();

  lines.push("");
  lines.push(`审核完成: ✓批准 ${approved}  ✗拒绝 ${rejected}  →跳过 ${skipped}`);
  lines.push("━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━");

  return lines.join("\n") + "\n";
}

export function parseReviewCandidatesArgs(argv: string[]): ReviewCandidatesOptions {
  const opts: ReviewCandidatesOptions = {};
  for (let i = 0; i < argv.length; i++) {
    const a = argv[i]!;
    if (a === "--limit" && argv[i + 1]) {
      opts.limit = parseInt(argv[++i]!, 10);
    } else if (a.startsWith("--limit=")) {
      opts.limit = parseInt(a.slice("--limit=".length), 10);
    }
  }
  return opts;
}
```

- [ ] **Step 4: 测试通过**

```bash
pnpm test packages/cli/src/__tests__/review-candidates.test.ts
```

Expected: PASS

- [ ] **Step 5: commit**

```bash
git add packages/cli/src/commands/review-candidates.ts \
        packages/cli/src/__tests__/review-candidates.test.ts
git commit -m "feat(m2.5-half): review-candidates interactive CLI"
```

---

## Task 11：Wire bin.ts + Walking Skeleton 验证

**Files:**
- Modify: `packages/cli/src/bin.ts`

- [ ] **Step 1: 在 bin.ts switch 中添加两个新命令**

在 `packages/cli/src/bin.ts` 顶部 import 区追加：

```typescript
import { executeScanErrors, parseScanErrorsArgs } from "./commands/scan-errors.js";
import {
  executeReviewCandidates,
  parseReviewCandidatesArgs,
} from "./commands/review-candidates.js";
```

在 `switch (command)` 中追加两个 case（放在最后的 `default` 之前）：

```typescript
    case "scan-errors": {
      const scanOpts = parseScanErrorsArgs(rest);
      const output = await executeScanErrors(scanOpts);
      if (output) process.stdout.write(output);
      return;
    }
    case "review-candidates": {
      const reviewOpts = parseReviewCandidatesArgs(rest);
      const output = await executeReviewCandidates(reviewOpts);
      if (output) process.stdout.write(output);
      return;
    }
```

- [ ] **Step 2: 全量测试通过**

```bash
pnpm test
```

Expected: 全部 PASS

- [ ] **Step 3: typecheck 通过**

```bash
pnpm typecheck
```

Expected: 0 errors

- [ ] **Step 4: Walking Skeleton — dry-run 验证**

```bash
pnpm teamagent scan-errors --dry-run --since=24h
```

Expected: 输出 "📭 无新错误信号" 或打印候选规则列表（取决于本机是否有 sessions/events），不报错，不崩溃

- [ ] **Step 5: Walking Skeleton — 完整链路**

```bash
pnpm teamagent scan-errors --since=24h
pnpm teamagent review-candidates --limit=3
pnpm teamagent stats
```

Expected:
- `scan-errors` 打印信号数和候选数（或"无新信号"），正常退出
- `review-candidates` 显示审核界面（或"队列为空"），正常退出
- `stats` 正常展示知识库状态

- [ ] **Step 6: commit**

```bash
git add packages/cli/src/bin.ts
git commit -m "feat(m2.5-half): wire scan-errors + review-candidates into bin — Walking Skeleton 贯通"
```

---

## 完成标志

以下命令全部跑通，视为 M2.5-half 完成：

```bash
pnpm test              # 全绿
pnpm typecheck         # 0 errors
pnpm teamagent scan-errors --dry-run --since=24h   # 不崩溃
pnpm teamagent scan-errors --since=24h             # 正常入队
pnpm teamagent review-candidates                   # 交互审核可用
pnpm teamagent stats                               # 知识库状态正确
```
