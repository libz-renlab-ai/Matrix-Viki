# M2.10 Close the Loop Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 接通两条断裂闭环——把 UserPromptSubmit Hook 挂上 Claude Code，并通过 Stop Hook 让知识学习自动化。

**Architecture:** Gap 1 只需修 `tsup.hook.config.ts` 命名 + 扩展 `install-hook.ts`；Gap 2 新建 `bin-stop.ts` 入口，读取 `StopHookInput.transcript_path` 后串行调用已有的 `executeAnalyze → executeCalibrate → executeCompile`；新增 `config` 命令读写 `.teamagent/config.json` 控制 sync/async 模式。

**Tech Stack:** Node.js/TypeScript, tsup (CJS bundle), vitest, existing CLI commands (executeAnalyze/Calibrate/Compile)

---

## File Map

| 操作 | 文件 | 职责 |
|------|------|------|
| Modify | `packages/cli/tsup.hook.config.ts` | 显式 entry 名，加 bin-stop 入口 |
| Modify | `packages/cli/src/commands/install-hook.ts` | 注册 UserPromptSubmit + Stop hook；uninstall 清理 |
| Create | `packages/cli/src/bin-stop.ts` | Stop hook 入口：读 stdin → 读 config → 跑流水线 |
| Create | `packages/cli/src/commands/config.ts` | `teamagent config` 命令：读写 `.teamagent/config.json` |
| Modify | `packages/cli/src/bin.ts` | 注册 `config` 命令 |
| Modify | `packages/cli/src/__tests__/install-hook.test.ts` | 补 UserPromptSubmit + Stop 注册/卸载测试 |
| Create | `packages/cli/src/__tests__/bin-stop.test.ts` | Stop 流水线单元测试 |
| Create | `packages/cli/src/__tests__/config.test.ts` | config 命令读写测试 |

---

## Task 1: Fix tsup entry names

**Files:**
- Modify: `packages/cli/tsup.hook.config.ts`

- [ ] **Step 1: 把 entry 改为对象形式，加 bin-stop**

打开 `packages/cli/tsup.hook.config.ts`，把 `entry` 数组改为对象：

```typescript
export default defineConfig({
  entry: {
    "bin-pre-tool-use":       "src/bin-pre-tool-use.ts",
    "bin-post-tool-use":      "src/bin-post-tool-use.ts",
    "bin-user-prompt-submit": "src/bin-user-prompt-submit.ts",
    "bin-stop":               "src/bin-stop.ts",
  },
  format: ["cjs"],
  platform: "node",
  target: "node20",
  outDir: "dist",
  clean: false,
  sourcemap: false,
  splitting: false,
  noExternal: [
    "@teamagent/types",
    "@teamagent/ports",
    "@teamagent/core",
    "@teamagent/adapters",
    "zod",
    "@xenova/transformers",
  ],
  external: ["sharp", "onnxruntime-node", "jsdom"],
  shims: true,
});
```

- [ ] **Step 2: 验证旧产物命名（bin-wiki-inject.cjs 是历史遗留）**

```bash
ls packages/cli/dist/bin-*.cjs
```

应看到 `bin-pre-tool-use.cjs` 和 `bin-post-tool-use.cjs`（bin-wiki-inject.cjs 是旧名，稍后 rebuild 会被替换）。

- [ ] **Step 3: Commit**

```bash
git add packages/cli/tsup.hook.config.ts
git commit -m "feat(m2.10): tsup 显式 entry 命名 + 加入 bin-stop 入口"
```

---

## Task 2: Add TeamAgentConfig + readTeamAgentConfig

**Files:**
- Create: `packages/cli/src/commands/config.ts`
- Create: `packages/cli/src/__tests__/config.test.ts`

- [ ] **Step 1: 先写测试**

新建 `packages/cli/src/__tests__/config.test.ts`：

```typescript
import { describe, it, expect, beforeEach, afterEach } from "vitest";
import fs from "node:fs";
import path from "node:path";
import os from "node:os";
import {
  readTeamAgentConfig,
  writeTeamAgentConfig,
  executeConfig,
} from "../commands/config.js";

function mkTmp() {
  const cwd = fs.mkdtempSync(path.join(os.tmpdir(), "ta-config-"));
  return { cwd, cleanup: () => fs.rmSync(cwd, { recursive: true, force: true }) };
}

describe("readTeamAgentConfig", () => {
  let tmp: ReturnType<typeof mkTmp>;
  beforeEach(() => { tmp = mkTmp(); });
  afterEach(() => { tmp.cleanup(); });

  it("returns default sync when config file missing", () => {
    const cfg = readTeamAgentConfig(tmp.cwd);
    expect(cfg.stop_mode).toBe("sync");
  });

  it("reads stop_mode from file", () => {
    const dir = path.join(tmp.cwd, ".teamagent");
    fs.mkdirSync(dir, { recursive: true });
    fs.writeFileSync(path.join(dir, "config.json"), JSON.stringify({ stop_mode: "async" }));
    const cfg = readTeamAgentConfig(tmp.cwd);
    expect(cfg.stop_mode).toBe("async");
  });
});

describe("writeTeamAgentConfig", () => {
  let tmp: ReturnType<typeof mkTmp>;
  beforeEach(() => { tmp = mkTmp(); });
  afterEach(() => { tmp.cleanup(); });

  it("creates .teamagent dir and writes config", () => {
    writeTeamAgentConfig(tmp.cwd, { stop_mode: "async" });
    const raw = fs.readFileSync(path.join(tmp.cwd, ".teamagent", "config.json"), "utf-8");
    expect(JSON.parse(raw).stop_mode).toBe("async");
  });

  it("merges into existing config", () => {
    const dir = path.join(tmp.cwd, ".teamagent");
    fs.mkdirSync(dir, { recursive: true });
    fs.writeFileSync(path.join(dir, "config.json"), JSON.stringify({ stop_mode: "async", other: "preserved" }));
    writeTeamAgentConfig(tmp.cwd, { stop_mode: "sync" });
    const raw = fs.readFileSync(path.join(dir, "config.json"), "utf-8");
    const parsed = JSON.parse(raw);
    expect(parsed.stop_mode).toBe("sync");
    expect(parsed.other).toBe("preserved");
  });
});

describe("executeConfig", () => {
  let tmp: ReturnType<typeof mkTmp>;
  beforeEach(() => { tmp = mkTmp(); });
  afterEach(() => { tmp.cleanup(); });

  it("stop-mode async writes async to config", () => {
    executeConfig({ subcommand: "stop-mode", value: "async", cwd: tmp.cwd });
    const cfg = readTeamAgentConfig(tmp.cwd);
    expect(cfg.stop_mode).toBe("async");
  });

  it("stop-mode sync writes sync to config", () => {
    executeConfig({ subcommand: "stop-mode", value: "async", cwd: tmp.cwd });
    executeConfig({ subcommand: "stop-mode", value: "sync", cwd: tmp.cwd });
    const cfg = readTeamAgentConfig(tmp.cwd);
    expect(cfg.stop_mode).toBe("sync");
  });

  it("show returns current config as JSON string", () => {
    executeConfig({ subcommand: "stop-mode", value: "async", cwd: tmp.cwd });
    const out = executeConfig({ subcommand: "show", cwd: tmp.cwd });
    expect(out).toContain("async");
  });

  it("throws on invalid stop-mode value", () => {
    expect(() =>
      executeConfig({ subcommand: "stop-mode", value: "invalid", cwd: tmp.cwd })
    ).toThrow();
  });
});
```

- [ ] **Step 2: 跑测试，确认全红**

```bash
cd packages/cli && pnpm test src/__tests__/config.test.ts 2>&1 | tail -5
```

期待：FAIL（config.js 不存在）

- [ ] **Step 3: 实现 config.ts**

新建 `packages/cli/src/commands/config.ts`：

```typescript
import fs from "node:fs";
import path from "node:path";

export interface TeamAgentConfig {
  stop_mode: "sync" | "async";
}

const DEFAULTS: TeamAgentConfig = {
  stop_mode: "sync",
};

export function readTeamAgentConfig(cwd: string): TeamAgentConfig {
  const file = path.join(cwd, ".teamagent", "config.json");
  if (!fs.existsSync(file)) return { ...DEFAULTS };
  try {
    const raw = fs.readFileSync(file, "utf-8");
    return { ...DEFAULTS, ...JSON.parse(raw) } as TeamAgentConfig;
  } catch {
    return { ...DEFAULTS };
  }
}

export function writeTeamAgentConfig(cwd: string, patch: Partial<TeamAgentConfig>): void {
  const dir = path.join(cwd, ".teamagent");
  const file = path.join(dir, "config.json");
  fs.mkdirSync(dir, { recursive: true });
  const existing = readTeamAgentConfig(cwd);
  const merged = { ...existing, ...patch };
  fs.writeFileSync(file, JSON.stringify(merged, null, 2) + "\n", "utf-8");
}

export interface ConfigOptions {
  subcommand: "stop-mode" | "show";
  value?: string;
  cwd?: string;
}

export function executeConfig(opts: ConfigOptions): string {
  const cwd = opts.cwd ?? process.cwd();

  if (opts.subcommand === "stop-mode") {
    const val = opts.value;
    if (val !== "sync" && val !== "async") {
      throw new Error(`Invalid stop-mode value: "${val}". Use "sync" or "async".`);
    }
    writeTeamAgentConfig(cwd, { stop_mode: val });
    return `stop_mode set to "${val}"`;
  }

  if (opts.subcommand === "show") {
    const cfg = readTeamAgentConfig(cwd);
    return JSON.stringify(cfg, null, 2);
  }

  throw new Error(`Unknown config subcommand: "${opts.subcommand}"`);
}
```

- [ ] **Step 4: 跑测试，确认全绿**

```bash
cd packages/cli && pnpm test src/__tests__/config.test.ts 2>&1 | tail -5
```

期待：PASS（所有测试通过）

- [ ] **Step 5: Commit**

```bash
git add packages/cli/src/commands/config.ts packages/cli/src/__tests__/config.test.ts
git commit -m "feat(m2.10): TeamAgentConfig 读写 + executeConfig 命令"
```

---

## Task 3: Implement bin-stop.ts

**Files:**
- Create: `packages/cli/src/bin-stop.ts`
- Create: `packages/cli/src/__tests__/bin-stop.test.ts`

- [ ] **Step 1: 先写测试**

新建 `packages/cli/src/__tests__/bin-stop.test.ts`：

```typescript
import { describe, it, expect, vi, beforeEach } from "vitest";
import { runStopPipeline, type StopHookInput } from "../bin-stop.js";

// mock 三个 execute 函数，避免真实 IO
vi.mock("../commands/analyze.js", () => ({
  executeAnalyze: vi.fn().mockResolvedValue("analyze done"),
}));
vi.mock("../commands/calibrate.js", () => ({
  executeCalibrate: vi.fn().mockResolvedValue({ dryRun: false }),
}));
vi.mock("../commands/compile.js", () => ({
  executeCompile: vi.fn().mockResolvedValue({ markdown: { blockLineCount: 5 }, skills: { written: [], removed: [] } }),
}));

import { executeAnalyze } from "../commands/analyze.js";
import { executeCalibrate } from "../commands/calibrate.js";
import { executeCompile } from "../commands/compile.js";

describe("runStopPipeline", () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it("calls analyze with transcript_path and commit=true", async () => {
    const input: StopHookInput = {
      session_id: "abc123",
      transcript_path: "/tmp/session.jsonl",
      cwd: process.cwd(),
      hook_event_name: "Stop",
    };
    await runStopPipeline(input);
    expect(executeAnalyze).toHaveBeenCalledWith(
      expect.objectContaining({
        session: "/tmp/session.jsonl",
        commit: true,
        cwd: process.cwd(),
      })
    );
  });

  it("calls calibrate after analyze", async () => {
    const input: StopHookInput = {
      session_id: "abc123",
      transcript_path: "/tmp/session.jsonl",
      cwd: process.cwd(),
      hook_event_name: "Stop",
    };
    await runStopPipeline(input);
    expect(executeCalibrate).toHaveBeenCalled();
    expect(executeCompile).toHaveBeenCalled();
  });

  it("continues pipeline even if analyze throws", async () => {
    vi.mocked(executeAnalyze).mockRejectedValueOnce(new Error("analyze failed"));
    const input: StopHookInput = {
      session_id: "abc123",
      transcript_path: "/tmp/session.jsonl",
      cwd: process.cwd(),
      hook_event_name: "Stop",
    };
    await expect(runStopPipeline(input)).resolves.not.toThrow();
    expect(executeCalibrate).toHaveBeenCalled();
    expect(executeCompile).toHaveBeenCalled();
  });

  it("resolves even if all steps throw", async () => {
    vi.mocked(executeAnalyze).mockRejectedValueOnce(new Error("fail"));
    vi.mocked(executeCalibrate).mockRejectedValueOnce(new Error("fail"));
    vi.mocked(executeCompile).mockRejectedValueOnce(new Error("fail"));
    const input: StopHookInput = {
      session_id: "abc123",
      transcript_path: "/tmp/session.jsonl",
      cwd: process.cwd(),
      hook_event_name: "Stop",
    };
    await expect(runStopPipeline(input)).resolves.toBeUndefined();
  });
});
```

- [ ] **Step 2: 跑测试，确认红**

```bash
cd packages/cli && pnpm test src/__tests__/bin-stop.test.ts 2>&1 | tail -5
```

期待：FAIL（bin-stop.js 不存在）

- [ ] **Step 3: 实现 bin-stop.ts**

新建 `packages/cli/src/bin-stop.ts`：

```typescript
#!/usr/bin/env node
/**
 * Stop Hook entry point (M2.10)
 *
 * stdin JSON: StopHookInput { session_id, transcript_path, cwd, hook_event_name }
 * → reads config → sync: run pipeline → async: spawn detached
 *
 * NEVER exits non-zero — must not block session close.
 */
import { spawn } from "node:child_process";
import { appendFileSync } from "node:fs";
import os from "node:os";
import path from "node:path";
import { executeAnalyze } from "./commands/analyze.js";
import { executeCalibrate } from "./commands/calibrate.js";
import { executeCompile } from "./commands/compile.js";
import { readTeamAgentConfig } from "./commands/config.js";

export interface StopHookInput {
  session_id: string;
  transcript_path: string;
  cwd: string;
  hook_event_name: string;
}

const PIPELINE_TIMEOUT_MS = 55_000;

export async function runStopPipeline(input: StopHookInput): Promise<void> {
  const cwd = input.cwd;

  // Step 1: analyze
  try {
    process.stderr.write("TeamAgent: 分析会话中...\n");
    const result = await executeAnalyze({
      session: input.transcript_path,
      commit: true,
      cwd,
    });
    process.stderr.write(`TeamAgent: ${result.split("\n")[0] ?? "分析完成"}\n`);
  } catch (e) {
    logError(cwd, "analyze", e);
  }

  // Step 2: calibrate
  try {
    process.stderr.write("TeamAgent: 校准置信度中...\n");
    await executeCalibrate({ cwd });
    process.stderr.write("TeamAgent: 校准完成\n");
  } catch (e) {
    logError(cwd, "calibrate", e);
  }

  // Step 3: compile
  try {
    process.stderr.write("TeamAgent: 编译 CLAUDE.md 中...\n");
    const r = await executeCompile({ cwd });
    process.stderr.write(
      `TeamAgent: CLAUDE.md 已更新，Skills 导出 ${r.skills.written.length} 条\n`
    );
  } catch (e) {
    logError(cwd, "compile", e);
  }
}

function logError(cwd: string, step: string, err: unknown): void {
  try {
    const logPath = path.join(os.homedir(), ".teamagent", "stop-errors.log");
    const msg = `[${new Date().toISOString()}] step=${step} cwd=${cwd} err=${String(err)}\n`;
    appendFileSync(logPath, msg, "utf-8");
  } catch {
    // 日志写入失败也不能影响退出
  }
}

async function main(): Promise<void> {
  // 检测是否作为 detached pipeline 子进程运行
  if (process.env["TEAMAGENT_STOP_PIPELINE"] === "1") {
    const input = JSON.parse(process.argv[2] ?? "{}") as StopHookInput;
    await runStopPipeline(input);
    return;
  }

  // 正常 Stop hook 入口：从 stdin 读 input
  const chunks: Buffer[] = [];
  for await (const chunk of process.stdin) chunks.push(chunk as Buffer);
  const raw = Buffer.concat(chunks).toString("utf-8").trim();
  if (!raw) return;

  const input = JSON.parse(raw) as StopHookInput;
  const cwd = input.cwd ?? process.cwd();
  const config = readTeamAgentConfig(cwd);

  if (config.stop_mode === "async") {
    // detached 模式：spawn 自身并立即返回
    const selfPath = process.argv[1]!;
    const child = spawn(process.execPath, [selfPath, JSON.stringify(input)], {
      detached: true,
      stdio: "ignore",
      cwd,
      env: { ...process.env, TEAMAGENT_STOP_PIPELINE: "1" },
    });
    child.unref();
    return;
  }

  // sync 模式（默认）：等待流水线完成，超时后强制退出
  await Promise.race([
    runStopPipeline(input),
    new Promise<void>((resolve) => setTimeout(resolve, PIPELINE_TIMEOUT_MS)),
  ]);
}

main().catch((e) => {
  try {
    const logPath = path.join(os.homedir(), ".teamagent", "stop-errors.log");
    appendFileSync(logPath, `[${new Date().toISOString()}] main-crash err=${String(e)}\n`, "utf-8");
  } catch { /* 静默 */ }
  process.exit(0); // 绝不阻断会话关闭
});
```

- [ ] **Step 4: 跑测试，确认绿**

```bash
cd packages/cli && pnpm test src/__tests__/bin-stop.test.ts 2>&1 | tail -5
```

期待：PASS

- [ ] **Step 5: Commit**

```bash
git add packages/cli/src/bin-stop.ts packages/cli/src/__tests__/bin-stop.test.ts
git commit -m "feat(m2.10): bin-stop.ts Stop hook 入口 + runStopPipeline"
```

---

## Task 4: Extend install-hook — UserPromptSubmit + Stop

**Files:**
- Modify: `packages/cli/src/commands/install-hook.ts`
- Modify: `packages/cli/src/__tests__/install-hook.test.ts`

- [ ] **Step 1: 先在测试里补新 case**

打开 `packages/cli/src/__tests__/install-hook.test.ts`，在文件末尾追加：

```typescript
describe("installHook — UserPromptSubmit + Stop", () => {
  let tmp: ReturnType<typeof mkTmp>;
  beforeEach(() => { tmp = mkTmp(); });
  afterEach(() => { tmp.cleanup(); });

  it("registers UserPromptSubmit hook when bundle provided", () => {
    installHook({
      cwd: tmp.cwd,
      hookEntry: FAKE_HOOK_ENTRY,
      userPromptEntry: FAKE_HOOK_ENTRY,
    });
    const content = JSON.parse(
      fs.readFileSync(path.join(tmp.cwd, ".claude", "settings.local.json"), "utf-8")
    );
    expect(content.hooks.UserPromptSubmit).toBeDefined();
    expect(content.hooks.UserPromptSubmit[0]._teamagentTag).toBe("teamagent-user-prompt-submit");
    expect(content.hooks.UserPromptSubmit[0].hooks[0].timeout).toBe(10);
    // UserPromptSubmit 无 matcher
    expect(content.hooks.UserPromptSubmit[0].matcher).toBeUndefined();
  });

  it("registers Stop hook when bundle provided", () => {
    installHook({
      cwd: tmp.cwd,
      hookEntry: FAKE_HOOK_ENTRY,
      stopEntry: FAKE_HOOK_ENTRY,
    });
    const content = JSON.parse(
      fs.readFileSync(path.join(tmp.cwd, ".claude", "settings.local.json"), "utf-8")
    );
    expect(content.hooks.Stop).toBeDefined();
    expect(content.hooks.Stop[0]._teamagentTag).toBe("teamagent-stop");
    expect(content.hooks.Stop[0].hooks[0].timeout).toBe(60);
    // Stop 无 matcher
    expect(content.hooks.Stop[0].matcher).toBeUndefined();
  });

  it("idempotent: second install of UserPromptSubmit not duplicated", () => {
    installHook({ cwd: tmp.cwd, hookEntry: FAKE_HOOK_ENTRY, userPromptEntry: FAKE_HOOK_ENTRY });
    installHook({ cwd: tmp.cwd, hookEntry: FAKE_HOOK_ENTRY, userPromptEntry: FAKE_HOOK_ENTRY });
    const content = JSON.parse(
      fs.readFileSync(path.join(tmp.cwd, ".claude", "settings.local.json"), "utf-8")
    );
    expect(content.hooks.UserPromptSubmit).toHaveLength(1);
  });

  it("uninstall removes UserPromptSubmit and Stop entries", () => {
    installHook({
      cwd: tmp.cwd,
      hookEntry: FAKE_HOOK_ENTRY,
      userPromptEntry: FAKE_HOOK_ENTRY,
      stopEntry: FAKE_HOOK_ENTRY,
    });
    uninstallHook({ cwd: tmp.cwd });
    const content = JSON.parse(
      fs.readFileSync(path.join(tmp.cwd, ".claude", "settings.local.json"), "utf-8")
    );
    expect(content.hooks?.UserPromptSubmit).toBeUndefined();
    expect(content.hooks?.Stop).toBeUndefined();
  });
});
```

- [ ] **Step 2: 跑测试，确认新 case 红**

```bash
cd packages/cli && pnpm test src/__tests__/install-hook.test.ts 2>&1 | tail -10
```

期待：新加的 4 个 case FAIL（InstallHookOptions 还没有 userPromptEntry/stopEntry）

- [ ] **Step 3: 更新 install-hook.ts**

打开 `packages/cli/src/commands/install-hook.ts`：

**3a. 扩展 ClaudeSettings 接口**（在现有接口里加两个字段）：

```typescript
interface ClaudeSettings {
  hooks?: {
    PreToolUse?: HookEntry[];
    PostToolUse?: HookEntry[];
    UserPromptSubmit?: HookEntry[];
    Stop?: HookEntry[];
    [k: string]: unknown;
  };
  [k: string]: unknown;
}
```

**3b. 扩展 InstallHookOptions**：

```typescript
export interface InstallHookOptions {
  cwd?: string;
  hookEntry?: string;
  postHookEntry?: string;
  userPromptEntry?: string;   // 新增
  stopEntry?: string;         // 新增
}
```

**3c. 在 `installHook()` 函数里加两个新常量和注册逻辑**（在现有 PostToolUse 注册代码后面追加）：

```typescript
const USER_PROMPT_TAG = "teamagent-user-prompt-submit";
const STOP_HOOK_TAG   = "teamagent-stop";

// UserPromptSubmit 注册
const userPromptEntry = opts.userPromptEntry
  ?? path.join(cliRoot(), "dist", "bin-user-prompt-submit.cjs");
const hasUserPromptBundle = fs.existsSync(userPromptEntry);

if (!settings.hooks.UserPromptSubmit) settings.hooks.UserPromptSubmit = [];
if (hasUserPromptBundle) {
  const upExisting = settings.hooks.UserPromptSubmit.find(
    (h) => h._teamagentTag === USER_PROMPT_TAG,
  );
  if (!upExisting) {
    const forwardPath = toForwardSlash(userPromptEntry);
    settings.hooks.UserPromptSubmit.push({
      _teamagentTag: USER_PROMPT_TAG,
      hooks: [{ type: "command", command: `node ${shellQuote(forwardPath)}`, timeout: 10 }],
    });
  }
}
if (settings.hooks.UserPromptSubmit?.length === 0) delete settings.hooks.UserPromptSubmit;

// Stop 注册
const stopEntry = opts.stopEntry
  ?? path.join(cliRoot(), "dist", "bin-stop.cjs");
const hasStopBundle = fs.existsSync(stopEntry);

if (!settings.hooks.Stop) settings.hooks.Stop = [];
if (hasStopBundle) {
  const stopExisting = settings.hooks.Stop.find(
    (h) => h._teamagentTag === STOP_HOOK_TAG,
  );
  if (!stopExisting) {
    const forwardPath = toForwardSlash(stopEntry);
    settings.hooks.Stop.push({
      _teamagentTag: STOP_HOOK_TAG,
      hooks: [{ type: "command", command: `node ${shellQuote(forwardPath)}`, timeout: 60 }],
    });
  }
}
if (settings.hooks.Stop?.length === 0) delete settings.hooks.Stop;
```

**3d. 在 `uninstallHook()` 加清理**（在现有 PostToolUse 清理后面追加）：

```typescript
if (settings.hooks.UserPromptSubmit) {
  const before = settings.hooks.UserPromptSubmit.length;
  settings.hooks.UserPromptSubmit = settings.hooks.UserPromptSubmit.filter(
    (h) => h._teamagentTag !== USER_PROMPT_TAG,
  );
  if (settings.hooks.UserPromptSubmit.length !== before) removedAny = true;
  if (settings.hooks.UserPromptSubmit.length === 0) delete settings.hooks.UserPromptSubmit;
}

if (settings.hooks.Stop) {
  const before = settings.hooks.Stop.length;
  settings.hooks.Stop = settings.hooks.Stop.filter(
    (h) => h._teamagentTag !== STOP_HOOK_TAG,
  );
  if (settings.hooks.Stop.length !== before) removedAny = true;
  if (settings.hooks.Stop.length === 0) delete settings.hooks.Stop;
}
```

**注意**：`USER_PROMPT_TAG` 和 `STOP_HOOK_TAG` 常量需要定义在 `installHook` 函数**外**（模块级），这样 `uninstallHook` 也能访问。

- [ ] **Step 4: 跑全部 install-hook 测试，确认全绿**

```bash
cd packages/cli && pnpm test src/__tests__/install-hook.test.ts 2>&1 | tail -10
```

期待：PASS（含原有 + 新增共 7 个 case）

- [ ] **Step 5: Commit**

```bash
git add packages/cli/src/commands/install-hook.ts packages/cli/src/__tests__/install-hook.test.ts
git commit -m "feat(m2.10): install-hook 注册 UserPromptSubmit + Stop；uninstall 同步清理"
```

---

## Task 5: Register config command in bin.ts

**Files:**
- Modify: `packages/cli/src/bin.ts`

- [ ] **Step 1: 在 bin.ts 顶部 import 区加 config**

在现有 import 列表里加一行（找到 `import { executeCompile` 附近）：

```typescript
import { executeConfig } from "./commands/config.js";
```

- [ ] **Step 2: 在 switch 里加 "config" case**

找到 `case "compile":` 区块后面，加：

```typescript
case "config": {
  const sub = args[1];
  const val = args[2];
  if (!sub || (sub !== "show" && sub !== "stop-mode")) {
    console.error('Usage: teamagent config stop-mode <sync|async>');
    console.error('       teamagent config show');
    process.exit(1);
  }
  try {
    const out = executeConfig({ subcommand: sub as "stop-mode" | "show", value: val });
    console.log(out);
  } catch (e) {
    console.error(String(e));
    process.exit(1);
  }
  break;
}
```

- [ ] **Step 3: 在 help 文本里加一行说明**

找到 help 文本里的 `teamagent compile` 行，在后面加：

```
"  teamagent config stop-mode <sync|async>  切换 Stop hook 运行模式（默认 sync）",
"  teamagent config show                    查看当前配置",
```

- [ ] **Step 4: 手动测试**

```bash
pnpm teamagent config show
# 期待输出: { "stop_mode": "sync" }

pnpm teamagent config stop-mode async
# 期待输出: stop_mode set to "async"

pnpm teamagent config show
# 期待输出: { "stop_mode": "async" }

pnpm teamagent config stop-mode sync
```

- [ ] **Step 5: Commit**

```bash
git add packages/cli/src/bin.ts
git commit -m "feat(m2.10): 注册 config 命令到 CLI"
```

---

## Task 6: Build + Reinstall + Smoke Test

**Files:**
- 无代码变更，验证阶段

- [ ] **Step 1: 全量 build hook bundles**

```bash
cd C:/bzli/teamagent && pnpm --filter @teamagent/cli build:hook 2>&1 | tail -10
```

期待：看到 4 个产物：
```
dist/bin-pre-tool-use.cjs
dist/bin-post-tool-use.cjs
dist/bin-user-prompt-submit.cjs
dist/bin-stop.cjs
```

- [ ] **Step 2: 确认 4 个文件都存在**

```bash
ls packages/cli/dist/bin-*.cjs
```

期待：恰好 4 个文件（旧的 `bin-wiki-inject.cjs` 已被替换）

- [ ] **Step 3: 重新安装 hooks**

```bash
pnpm teamagent install-hook 2>&1
```

期待：输出类似：
```
✓ PreToolUse hook installed
✓ PostToolUse hook installed
✓ UserPromptSubmit hook installed
✓ Stop hook installed
```

- [ ] **Step 4: 验证 settings.local.json 有全部 4 条 hook**

```bash
cat .claude/settings.local.json
```

期待 JSON 中 `hooks` 包含：
- `PreToolUse` with `_teamagentTag: "teamagent-pre-tool-use"`
- `PostToolUse` with `_teamagentTag: "teamagent-post-tool-use"`
- `UserPromptSubmit` with `_teamagentTag: "teamagent-user-prompt-submit"`
- `Stop` with `_teamagentTag: "teamagent-stop"`

- [ ] **Step 5: 跑全量测试确认无回归**

```bash
cd C:/bzli/teamagent && pnpm test 2>&1 | tail -15
```

期待：全绿，无新增失败

- [ ] **Step 6: 验证 UserPromptSubmit 注入（需要知识库有条目）**

```bash
# 查看知识库现有条目数
pnpm teamagent stats
```

如果有活跃条目，开一个新 Claude Code 会话，发送包含已知规则关键词的消息（比如 "怎么管理状态"），观察 AI 收到的 prompt 是否包含 `[TeamAgent Wiki]` 前缀。

- [ ] **Step 7: Final commit**

```bash
git add .
git commit -m "feat(m2.10): M2.10 完成 — UserPromptSubmit 挂载 + Stop Hook 自动学习"
```

---

## 退出标准（checklist）

- [ ] `dist/` 下有 4 个 `bin-*.cjs` 文件，命名与源文件一致
- [ ] `settings.local.json` 包含全部 4 条 hook
- [ ] `teamagent config stop-mode async` 一键切换，`show` 验证
- [ ] Stop hook 触发后 stderr 显示 "TeamAgent: ..." 进度，CLAUDE.md 被更新
- [ ] `pnpm test` 全绿，无回归
- [ ] 连续 5 次会话后 `~/.teamagent/stop-errors.log` 无新增错误
