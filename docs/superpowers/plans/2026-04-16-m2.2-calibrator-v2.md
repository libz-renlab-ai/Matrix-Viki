# M2.2 Calibrator v2 Implementation Plan

## Status: COMPLETED 2026-04-16

- 17 tasks merged to master
- 552 tests green (`pnpm test`)
- Typecheck clean (`pnpm typecheck`)
- Dogfood verified: `skeleton-demo` passes; `calibrate` defaults to v2 pipeline; `stats --explain` shows tier/confidence/demerit
- 3 pitfalls recorded: tierFromDemerit semantics, hysteresis NaN guard, plan spec test errors

---

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 TeamAgent Phase 2 M2.2 Calibrator v2 —— 用 Wilson Score Lower Bound + Demerit（驾照扣分制）+ 5-tier 映射 + 迟滞防抖 + 4 级死亡链取代 v1 的简单加权公式，让规则晋升/降级稳健可解释。

**Architecture:** Functional-Core / Imperative-Shell。v2 所有打分逻辑拆成纯函数模块（`wilson.ts` / `demerit.ts` / `tier.ts`），最后由 `v2.ts` 组装成 Calibrator。v1 `defaultCalibrator` 保留不动作为 baseline。新增 `CalibratorV2` port 接口并行于现有 `Calibrator`，v2 接收 `events + observations + now` 而非仅 `events`。SQLite schema 已在 M2.1 备齐新列（`demerit`/`current_tier`/`max_tier_ever`/`tier_entered_at`/`resurrect_count`/`demerit_last_updated`），本里程碑负责"写入/读出"这些字段并把 calibrator 接入 pipeline。

**Tech Stack:** TypeScript、Vitest、node:sqlite、pnpm workspace、zod（schema）。

**参考文档：**
- 设计源：`docs/superpowers/specs/2026-04-15-phase2-design-v2.md` §4（公式全集）
- 里程碑拆分：同文档 §十三 M2.2
- 项目开发约定：`CLAUDE.md`（Functional Core / Port 先写契约 / 小 commit）

**实施顺序与依赖：**
```
T1 扩 KnowledgeEntry schema ─┐
T2 扩 SQLite 读写 ───────────┤
T3 CalibratorV2 port ────────┤
T4 CalibratorV2 契约测试 ────┤
                             ↓
T5 Wilson 纯函数 ──┐
T6 Demerit 纯函数 ─┤
T7 Tier 映射 ──────┤
T8 Hysteresis ─────┤
T9 死亡链 ─────────┤
                   ↓
T10 v2 Calibrator 组装
T11 observations pipeline 胶水
T12 v2 pipeline (delta breakdown event)
T13 CLI stats --explain
T14 把 v2 接入 CLI calibrate
T15 M2.1 残留数据默认 tier 补全
T16 End-to-end dogfood 测试
T17 更新 CLAUDE.md 经验
```

---

## Task 1: 扩 `KnowledgeEntry` schema 加入 v2 字段

**目标：** 让 TS 类型能表达 tier / demerit / max_tier_ever 等 v2 字段。backward-compat：新字段全部 optional，迁移时走默认值。

**Files:**
- Modify: `packages/types/src/knowledge-entry.ts`
- Test: `packages/types/src/__tests__/knowledge-entry.test.ts`（新建）

- [ ] **Step 1: 写失败测试**

新建 `packages/types/src/__tests__/knowledge-entry.test.ts`：

```typescript
import { describe, it, expect } from "vitest";
import { KnowledgeEntrySchema } from "../knowledge-entry.js";

describe("KnowledgeEntrySchema v2 fields", () => {
  const baseValid = {
    id: "r1",
    scope: { level: "team" as const },
    category: "E" as const,
    tags: [],
    type: "avoidance" as const,
    nature: "subjective" as const,
    trigger: "t",
    correct_pattern: "c",
    reasoning: "r",
    confidence: 0.5,
    enforcement: "passive" as const,
    created_at: "2026-04-16T00:00:00Z",
    source: "accumulated" as const,
  };

  it("accepts entry with new v2 tier fields", () => {
    const parsed = KnowledgeEntrySchema.parse({
      ...baseValid,
      current_tier: "probation",
      max_tier_ever: "probation",
      tier_entered_at: "2026-04-16T00:00:00Z",
      demerit: 0,
      demerit_last_updated: "2026-04-16T00:00:00Z",
      resurrect_count: 0,
    });
    expect(parsed.current_tier).toBe("probation");
    expect(parsed.demerit).toBe(0);
  });

  it("defaults v2 fields when omitted (backward compat)", () => {
    const parsed = KnowledgeEntrySchema.parse(baseValid);
    expect(parsed.current_tier).toBe("experimental");
    expect(parsed.max_tier_ever).toBe("experimental");
    expect(parsed.demerit).toBe(0);
    expect(parsed.resurrect_count).toBe(0);
  });

  it("status accepts 'dormant'", () => {
    const parsed = KnowledgeEntrySchema.parse({ ...baseValid, status: "dormant" });
    expect(parsed.status).toBe("dormant");
  });

  it("current_tier enum rejects invalid value", () => {
    expect(() =>
      KnowledgeEntrySchema.parse({ ...baseValid, current_tier: "super-enforced" }),
    ).toThrow();
  });
});
```

- [ ] **Step 2: 跑测试确认 FAIL**

Run: `pnpm --filter @teamagent/types test -- knowledge-entry`
Expected: 失败 —— schema 没这些字段。

- [ ] **Step 3: 改 `packages/types/src/knowledge-entry.ts`**

在 `KnowledgeEntrySchema` 里加入以下字段（保持 backward compat，全部带 default）：

```typescript
/** v2 Tier 系统 —— 晋升/降级决策 */
current_tier: z
  .enum(["experimental", "probation", "stable", "canonical", "enforced", "dormant"])
  .default("experimental"),
/** 历史最高 tier（防复发半衰期选择用） */
max_tier_ever: z
  .enum(["experimental", "probation", "stable", "canonical", "enforced"])
  .default("experimental"),
/** 当前 tier 进入时间戳（hysteresis "停留时间"判定） */
tier_entered_at: z.string().default(""),
/** Demerit 累积分（驾照扣分制） */
demerit: z.number().nonnegative().default(0),
/** Demerit 上次变动时间（衰减公式用） */
demerit_last_updated: z.string().default(""),
/** 已复活次数（达到 3 次永久归档） */
resurrect_count: z.number().int().nonnegative().default(0),
```

同时把 `status` enum 扩展加 `"dormant"`：

```typescript
status: z
  .enum(["active", "conflict", "stale", "archived", "dormant"])
  .default("active"),
```

- [ ] **Step 4: 跑测试确认 PASS**

Run: `pnpm --filter @teamagent/types test -- knowledge-entry`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add packages/types/src/knowledge-entry.ts packages/types/src/__tests__/knowledge-entry.test.ts
git commit -m "feat(m2.2): Task 1 — 扩 KnowledgeEntry schema 加入 v2 tier/demerit 字段"
```

---

## Task 2: 让 `SqliteKnowledgeStore` 读写 v2 新字段

**目标：** M2.1 已加 SQLite 列但 store 没填充。让 `add()` / `update()` / `getAll()` 都保留新字段。

**Files:**
- Modify: `packages/adapters/src/storage/sqlite/sqlite-knowledge-store.ts`
- Test: `packages/adapters/src/storage/sqlite/__tests__/sqlite-knowledge-store.test.ts`（已有，追加用例）

- [ ] **Step 1: 写失败测试**

在 store 测试文件末尾追加：

```typescript
describe("v2 tier/demerit fields round-trip", () => {
  it("persists and reads back tier/demerit fields", () => {
    const store = makeFreshStore();
    const entry = {
      ...baseEntry,
      id: "r-v2",
      current_tier: "stable" as const,
      max_tier_ever: "stable" as const,
      tier_entered_at: "2026-04-16T00:00:00Z",
      demerit: 3.5,
      demerit_last_updated: "2026-04-16T00:00:00Z",
      resurrect_count: 1,
    };
    store.add(entry);
    const back = store.getById("r-v2");
    expect(back?.current_tier).toBe("stable");
    expect(back?.demerit).toBe(3.5);
    expect(back?.resurrect_count).toBe(1);
    expect(back?.max_tier_ever).toBe("stable");
  });

  it("partial update preserves v2 fields not in patch", () => {
    const store = makeFreshStore();
    store.add({
      ...baseEntry,
      id: "r-partial",
      demerit: 5,
      current_tier: "probation",
      max_tier_ever: "probation",
      tier_entered_at: "2026-04-16T00:00:00Z",
    } as any);
    store.update("r-partial", { confidence: 0.8 });
    const back = store.getById("r-partial");
    expect(back?.demerit).toBe(5);
    expect(back?.current_tier).toBe("probation");
  });
});
```

（`makeFreshStore` / `baseEntry` 沿用既有 fixture——若没有则按文件现有模式复制一份。）

- [ ] **Step 2: 跑测试确认 FAIL**

Run: `pnpm --filter @teamagent/adapters test -- sqlite-knowledge-store`
Expected: FAIL（字段读回为 undefined 或默认值）。

- [ ] **Step 3: 修改 `sqlite-knowledge-store.ts`**

在 `INSERT` 和 `SELECT` 里加入所有新字段。以 `rowToEntry()` 映射函数为例：

```typescript
function rowToEntry(row: any): KnowledgeEntry {
  return {
    // ... 原有字段 ...
    current_tier: row.current_tier ?? "experimental",
    max_tier_ever: row.max_tier_ever ?? "experimental",
    tier_entered_at: row.tier_entered_at ?? "",
    demerit: row.demerit ?? 0,
    demerit_last_updated: row.demerit_last_updated ?? "",
    resurrect_count: row.resurrect_count ?? 0,
  };
}
```

`INSERT INTO knowledge` 语句和 `update()` 的动态 SET 子句都要覆盖上述列（列名对齐 schema.ts）。

- [ ] **Step 4: 跑测试确认 PASS**

Run: `pnpm --filter @teamagent/adapters test -- sqlite-knowledge-store`
Expected: PASS。

同时跑整包 typecheck：
Run: `pnpm --filter @teamagent/adapters typecheck`
Expected: 无错。

- [ ] **Step 5: Commit**

```bash
git add packages/adapters/src/storage/sqlite/sqlite-knowledge-store.ts packages/adapters/src/storage/sqlite/__tests__/sqlite-knowledge-store.test.ts
git commit -m "feat(m2.2): Task 2 — SqliteKnowledgeStore 读写 v2 tier/demerit 字段"
```

---

## Task 3: 定义 `CalibratorV2` port 接口

**目标：** v1 接口保留不动；v2 单独新接口，接收 `events + observations + now`，返回含 `tier_before/tier_after/delta_breakdown` 的扩展结果。

**Files:**
- Create: `packages/ports/src/calibrator-v2.ts`
- Modify: `packages/ports/src/index.ts`（导出）
- Modify: `packages/ports/package.json`（若需新 subpath；此 Task 不新增 subpath）

- [ ] **Step 1: 新建 `packages/ports/src/calibrator-v2.ts`**

```typescript
import type { KnowledgeEntry, PersistedEvent } from "@teamagent/types";

/** v2 观察条目（对应 observations 表）。 */
export interface Observation {
  id: string;
  knowledge_id: string;
  timestamp: string;     // ISO8601
  outcome: "success" | "failure";
  source_event?: string;
  tool_use_id?: string;
}

/** v2 5-tier 映射。 */
export type Tier =
  | "experimental"
  | "probation"
  | "stable"
  | "canonical"
  | "enforced"
  | "dormant";

/** v2 Calibrator 输入。events 只用于抽取 demerit 触发源（如 ai.override.ignored / user_reject）。 */
export interface CalibratorV2Input {
  events: PersistedEvent[];
  observations: Observation[];
  now: Date;
}

/** v2 Tier 转变。 */
export interface TierTransition {
  from: Tier;
  to: Tier;
  reason: string;        // "hysteresis_promote" / "death_chain_soft_demote" / "manual" / ...
}

/** v2 Delta 分解 —— 每步对 confidence 或 demerit 的贡献。 */
export interface DeltaStep {
  type:
    | "obs_added"
    | "obs_decayed"
    | "demerit_added"
    | "demerit_decay"
    | "tier_transition";
  outcome?: "success" | "failure";
  weight?: number;
  conf_delta?: number;
  demerit_delta?: number;
  days_since?: number;
  note?: string;
}

/** v2 结果。 */
export interface CalibrationResultV2 {
  confidence: number;                  // Wilson LB, clamped [0,1]
  demerit: number;                     // ≥ 0
  tier_before: Tier;
  tier_after: Tier;
  status: KnowledgeEntry["status"];    // 可能由 tier_after==dormant 推导
  confidence_delta: number;
  demerit_delta: number;
  delta_breakdown: DeltaStep[];
  tier_transition: TierTransition | null;
  reason_for_no_transition?: string;   // hysteresis 未满足时解释
}

/** v2 Calibrator 接口。纯函数。 */
export interface CalibratorV2 {
  calibrate(entry: KnowledgeEntry, input: CalibratorV2Input): CalibrationResultV2;
}
```

- [ ] **Step 2: 在 `packages/ports/src/index.ts` 导出**

追加：
```typescript
export type {
  CalibratorV2,
  CalibratorV2Input,
  CalibrationResultV2,
  Observation,
  Tier,
  TierTransition,
  DeltaStep,
} from "./calibrator-v2.js";
```

- [ ] **Step 3: 确认编译通过**

Run: `pnpm --filter @teamagent/ports typecheck`
Expected: 无错（本 Task 只加类型，无运行时）。

- [ ] **Step 4: Commit**

```bash
git add packages/ports/src/calibrator-v2.ts packages/ports/src/index.ts
git commit -m "feat(m2.2): Task 3 — 定义 CalibratorV2 port 接口"
```

---

## Task 4: `CalibratorV2` 契约测试

**目标：** 写任何 v2 实现都必须通过的不变量套件。具体公式参数是实现细节，不在契约里。

**Files:**
- Create: `packages/ports/src/__tests__/calibrator-v2-contract.ts`
- Modify: `packages/ports/package.json`（exports 字段若已有 `./contracts` subpath 则无需改）

- [ ] **Step 1: 新建 `packages/ports/src/__tests__/calibrator-v2-contract.ts`**

```typescript
import { describe, it, expect } from "vitest";
import type {
  CalibratorV2,
  CalibratorV2Input,
  Observation,
} from "../calibrator-v2.js";
import type { KnowledgeEntry, PersistedEvent } from "@teamagent/types";

/**
 * CalibratorV2 契约。不变量：
 *
 * 1. confidence ∈ [0, 1]
 * 2. demerit ≥ 0
 * 3. 空 observations + 空 events + 零 demerit → 不变化
 * 4. 仅 success observations → confidence_delta ≥ 0
 * 5. 仅 failure observations → confidence_delta ≤ 0（或不变，视衰减）
 * 6. 死亡链：demerit ≥ 30 → tier_after=dormant
 * 7. 纯函数：相同输入 → 相同输出
 * 8. tier_transition 存在时 tier_before !== tier_after
 */
export function runCalibratorV2Contract(make: () => CalibratorV2): void {
  describe("CalibratorV2 contract", () => {
    const now = new Date("2026-04-16T12:00:00Z");

    const baseEntry: KnowledgeEntry = {
      id: "r-v2",
      scope: { level: "team" },
      category: "E",
      tags: [],
      type: "avoidance",
      nature: "subjective",
      trigger: "t",
      wrong_pattern: "w",
      correct_pattern: "c",
      reasoning: "r",
      confidence: 0.4,
      enforcement: "passive",
      status: "active",
      hit_count: 0,
      success_count: 0,
      override_count: 0,
      evidence: { success_sessions: 0, success_users: 0, correction_sessions: 0 },
      created_at: "2026-04-01T00:00:00Z",
      last_hit_at: "",
      last_validated_at: "2026-04-01T00:00:00Z",
      source: "accumulated",
      conflict_with: [],
      current_tier: "probation",
      max_tier_ever: "probation",
      tier_entered_at: "2026-04-10T00:00:00Z",
      demerit: 0,
      demerit_last_updated: "",
      resurrect_count: 0,
    };

    const emptyInput: CalibratorV2Input = {
      events: [],
      observations: [],
      now,
    };

    function obs(
      outcome: "success" | "failure",
      daysAgo = 0,
      id = `o-${Math.random()}`,
    ): Observation {
      const ts = new Date(now.getTime() - daysAgo * 24 * 3600 * 1000).toISOString();
      return {
        id,
        knowledge_id: baseEntry.id,
        timestamp: ts,
        outcome,
      };
    }

    it("clamps confidence to [0,1]", () => {
      const r = make().calibrate(baseEntry, {
        ...emptyInput,
        observations: Array.from({ length: 200 }, (_, i) => obs("success", 0, `s${i}`)),
      });
      expect(r.confidence).toBeGreaterThanOrEqual(0);
      expect(r.confidence).toBeLessThanOrEqual(1);
    });

    it("demerit >= 0", () => {
      const r = make().calibrate(baseEntry, emptyInput);
      expect(r.demerit).toBeGreaterThanOrEqual(0);
    });

    it("empty input + zero demerit → no change", () => {
      const r = make().calibrate(baseEntry, emptyInput);
      expect(r.confidence_delta).toBe(0);
      expect(r.demerit_delta).toBe(0);
      expect(r.tier_transition).toBeNull();
    });

    it("all success observations → confidence_delta >= 0", () => {
      const r = make().calibrate(
        { ...baseEntry, confidence: 0 },
        {
          ...emptyInput,
          observations: Array.from({ length: 10 }, (_, i) => obs("success", i, `s${i}`)),
        },
      );
      expect(r.confidence_delta).toBeGreaterThanOrEqual(0);
    });

    it("demerit >= 30 → tier_after=dormant", () => {
      const r = make().calibrate(
        { ...baseEntry, demerit: 35, demerit_last_updated: now.toISOString() },
        emptyInput,
      );
      expect(r.tier_after).toBe("dormant");
    });

    it("pure function: same input → same output", () => {
      const input: CalibratorV2Input = {
        ...emptyInput,
        observations: [obs("success", 1, "a"), obs("failure", 2, "b")],
      };
      const r1 = make().calibrate(baseEntry, input);
      const r2 = make().calibrate(baseEntry, input);
      expect(r1.confidence).toBe(r2.confidence);
      expect(r1.demerit).toBe(r2.demerit);
      expect(r1.tier_after).toBe(r2.tier_after);
    });

    it("tier_transition only when tier changes", () => {
      const r = make().calibrate(baseEntry, emptyInput);
      if (r.tier_transition) {
        expect(r.tier_transition.from).not.toBe(r.tier_transition.to);
      }
    });

    it("ignores observations from other rules", () => {
      const r = make().calibrate(baseEntry, {
        ...emptyInput,
        observations: [{ ...obs("success", 0), knowledge_id: "other-rule" }],
      });
      expect(r.confidence_delta).toBe(0);
    });
  });
}
```

- [ ] **Step 2: 确认 `packages/ports/package.json` 的 `./contracts` subpath 导出本文件**

查看 `packages/ports/package.json`，确认 `exports["./contracts"]` 指向的 barrel 文件里有没有 v2 导出。若有 `packages/ports/src/contracts.ts`（或类似），追加：

```typescript
export { runCalibratorV2Contract } from "./__tests__/calibrator-v2-contract.js";
```

若原 v1 契约直接从 `__tests__/calibrator-contract.ts` 导出，则照样操作。

- [ ] **Step 3: typecheck 通过**

Run: `pnpm --filter @teamagent/ports typecheck`
Expected: 无错。

- [ ] **Step 4: Commit**

```bash
git add packages/ports/
git commit -m "feat(m2.2): Task 4 — CalibratorV2 契约测试套件"
```

---

## Task 5: Wilson Score Lower Bound 纯函数

**目标：** 按设计文档 §4.2 实现 `computeConfidence(observations, maxTierEver, now)`，含时间衰减加权和 Wilson 公式。

**Files:**
- Create: `packages/core/src/calibrator/v2/wilson.ts`
- Test: `packages/core/src/calibrator/v2/__tests__/wilson.test.ts`

- [ ] **Step 1: 写失败测试**

新建 `packages/core/src/calibrator/v2/__tests__/wilson.test.ts`：

```typescript
import { describe, it, expect } from "vitest";
import { computeConfidence, HALF_LIFE_DAYS } from "../wilson.js";
import type { Observation, Tier } from "@teamagent/ports";

const now = new Date("2026-04-16T12:00:00Z");

function obs(
  outcome: "success" | "failure",
  daysAgo: number,
  id = `o-${daysAgo}-${outcome}`,
): Observation {
  return {
    id,
    knowledge_id: "r",
    timestamp: new Date(now.getTime() - daysAgo * 24 * 3600 * 1000).toISOString(),
    outcome,
  };
}

describe("Wilson LB computeConfidence", () => {
  it("returns 0 when no observations", () => {
    expect(computeConfidence([], "experimental", now)).toBe(0);
  });

  it("perfect 10/10 recent observations → > 0.6", () => {
    const obsList = Array.from({ length: 10 }, (_, i) => obs("success", i, `s${i}`));
    const r = computeConfidence(obsList, "experimental", now);
    expect(r).toBeGreaterThan(0.6);
    expect(r).toBeLessThanOrEqual(1);
  });

  it("10/10 but 30 days ago with experimental half-life → decayed", () => {
    const obsList = Array.from({ length: 10 }, (_, i) =>
      obs("success", 30 + i, `s${i}`),
    );
    const fresh = computeConfidence(
      Array.from({ length: 10 }, (_, i) => obs("success", i, `f${i}`)),
      "experimental",
      now,
    );
    const stale = computeConfidence(obsList, "experimental", now);
    expect(stale).toBeLessThan(fresh);
  });

  it("tier with longer half-life decays slower", () => {
    const obsList = Array.from({ length: 10 }, (_, i) =>
      obs("success", 60 + i, `s${i}`),
    );
    const expHL = computeConfidence(obsList, "experimental", now);
    const enfHL = computeConfidence(obsList, "enforced", now);
    expect(enfHL).toBeGreaterThan(expHL);
  });

  it("mixed 5 success 5 failure → less than all success", () => {
    const mixed = [
      ...Array.from({ length: 5 }, (_, i) => obs("success", i, `s${i}`)),
      ...Array.from({ length: 5 }, (_, i) => obs("failure", i, `f${i}`)),
    ];
    const perfect = Array.from({ length: 10 }, (_, i) => obs("success", i, `p${i}`));
    expect(computeConfidence(mixed, "experimental", now)).toBeLessThan(
      computeConfidence(perfect, "experimental", now),
    );
  });

  it("result clamped [0,1]", () => {
    const r = computeConfidence(
      Array.from({ length: 1000 }, (_, i) => obs("success", 0, `s${i}`)),
      "experimental",
      now,
    );
    expect(r).toBeGreaterThanOrEqual(0);
    expect(r).toBeLessThanOrEqual(1);
  });

  it("HALF_LIFE_DAYS covers all tiers except dormant", () => {
    expect(HALF_LIFE_DAYS.experimental).toBe(30);
    expect(HALF_LIFE_DAYS.probation).toBe(45);
    expect(HALF_LIFE_DAYS.stable).toBe(60);
    expect(HALF_LIFE_DAYS.canonical).toBe(75);
    expect(HALF_LIFE_DAYS.enforced).toBe(90);
  });
});
```

- [ ] **Step 2: 跑测试确认 FAIL**

Run: `pnpm --filter @teamagent/core test -- wilson`
Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 `packages/core/src/calibrator/v2/wilson.ts`**

```typescript
import type { Observation, Tier } from "@teamagent/ports";

/** 按历史最高 tier 选半衰期，防"曾经高 tier 的规则快速复发"。 */
export const HALF_LIFE_DAYS: Record<Exclude<Tier, "dormant">, number> = {
  experimental: 30,
  probation: 45,
  stable: 60,
  canonical: 75,
  enforced: 90,
};

const DAY_MS = 24 * 3600 * 1000;
const Z = 1.96; // 95% confidence

/**
 * Wilson Score Lower Bound，带时间衰减加权。见设计文档 §4.2。
 *
 * @param observations 该规则的全部观察
 * @param maxTierEver 选半衰期依据
 * @param now 当前时间（注入，便于测试）
 * @returns confidence ∈ [0, 1]；空观察返回 0
 */
export function computeConfidence(
  observations: Observation[],
  maxTierEver: Tier,
  now: Date,
): number {
  if (observations.length === 0) return 0;

  const halfLife =
    HALF_LIFE_DAYS[(maxTierEver === "dormant" ? "experimental" : maxTierEver) as Exclude<Tier, "dormant">];
  const lambda = Math.LN2 / halfLife;

  let weightedSuccess = 0;
  let weightedFailure = 0;
  for (const o of observations) {
    const daysAgo = (now.getTime() - new Date(o.timestamp).getTime()) / DAY_MS;
    const w = Math.exp(-lambda * Math.max(0, daysAgo));
    if (o.outcome === "success") weightedSuccess += w;
    else weightedFailure += w;
  }

  const n = weightedSuccess + weightedFailure;
  if (n === 0) return 0;

  const p = weightedSuccess / n;
  const wilson =
    (p + (Z * Z) / (2 * n) - Z * Math.sqrt((p * (1 - p)) / n + (Z * Z) / (4 * n * n))) /
    (1 + (Z * Z) / n);

  return Math.max(0, Math.min(1, wilson));
}
```

- [ ] **Step 4: 跑测试确认 PASS**

Run: `pnpm --filter @teamagent/core test -- wilson`
Expected: PASS（7 个测试全绿）。

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/calibrator/v2/
git commit -m "feat(m2.2): Task 5 — Wilson LB 纯函数 + 时间衰减"
```

---

## Task 6: Demerit 驾照扣分纯函数

**目标：** 实现 `computeDemerit(rule, newEvents, now)` 按当前 tier 衰减旧 demerit 并累加新扣分。见设计文档 §4.3。

**Files:**
- Create: `packages/core/src/calibrator/v2/demerit.ts`
- Test: `packages/core/src/calibrator/v2/__tests__/demerit.test.ts`

- [ ] **Step 1: 写失败测试**

```typescript
import { describe, it, expect } from "vitest";
import {
  computeDemerit,
  DEMERIT_HALF_LIFE_DAYS,
  DEMERIT_BASE_BY_TIER,
  type DemeritEvent,
} from "../demerit.js";
import type { Tier } from "@teamagent/ports";

const now = new Date("2026-04-16T12:00:00Z");
const daysAgo = (d: number) => new Date(now.getTime() - d * 24 * 3600 * 1000).toISOString();

describe("Demerit computeDemerit", () => {
  it("no events + zero demerit → zero", () => {
    const r = computeDemerit(
      { current: 0, last_updated: "", current_tier: "experimental", confidence: 0.3 },
      [],
      now,
    );
    expect(r.demerit).toBe(0);
  });

  it("decays existing demerit by half-life", () => {
    const r = computeDemerit(
      {
        current: 10,
        last_updated: daysAgo(7), // 正好 1 half-life at experimental (7 天)
        current_tier: "experimental",
        confidence: 0.4,
      },
      [],
      now,
    );
    expect(r.demerit).toBeCloseTo(5, 1);
  });

  it("adds base demerit by tier", () => {
    const events: DemeritEvent[] = [{ source: "ai_override_ignored", timestamp: now.toISOString() }];
    const r = computeDemerit(
      { current: 0, last_updated: "", current_tier: "canonical", confidence: 0.4 },
      events,
      now,
    );
    expect(r.demerit).toBeGreaterThanOrEqual(DEMERIT_BASE_BY_TIER.canonical);
  });

  it("log-loss multiplier amplifies when confidence high", () => {
    const events: DemeritEvent[] = [{ source: "ai_override_ignored", timestamp: now.toISOString() }];
    const low = computeDemerit(
      { current: 0, last_updated: "", current_tier: "stable", confidence: 0.4 },
      events,
      now,
    );
    const high = computeDemerit(
      { current: 0, last_updated: "", current_tier: "stable", confidence: 0.95 },
      events,
      now,
    );
    expect(high.demerit).toBeGreaterThan(low.demerit);
  });

  it("user_reject adds extra +10", () => {
    const base: DemeritEvent[] = [{ source: "ai_override_ignored", timestamp: now.toISOString() }];
    const reject: DemeritEvent[] = [{ source: "user_reject", timestamp: now.toISOString() }];
    const baseR = computeDemerit(
      { current: 0, last_updated: "", current_tier: "stable", confidence: 0.5 },
      base,
      now,
    );
    const rejectR = computeDemerit(
      { current: 0, last_updated: "", current_tier: "stable", confidence: 0.5 },
      reject,
      now,
    );
    expect(rejectR.demerit - baseR.demerit).toBeCloseTo(10, 1);
  });

  it("demerit never negative", () => {
    const r = computeDemerit(
      { current: 100, last_updated: daysAgo(1000), current_tier: "experimental", confidence: 0.3 },
      [],
      now,
    );
    expect(r.demerit).toBeGreaterThanOrEqual(0);
  });

  it("breakdown steps explain the math", () => {
    const r = computeDemerit(
      { current: 10, last_updated: daysAgo(7), current_tier: "experimental", confidence: 0.5 },
      [{ source: "ai_override_ignored", timestamp: now.toISOString() }],
      now,
    );
    expect(r.breakdown.length).toBeGreaterThan(0);
    expect(r.breakdown.some((s) => s.type === "demerit_decay")).toBe(true);
    expect(r.breakdown.some((s) => s.type === "demerit_added")).toBe(true);
  });

  it("half-life table matches design doc", () => {
    expect(DEMERIT_HALF_LIFE_DAYS.experimental).toBe(7);
    expect(DEMERIT_HALF_LIFE_DAYS.probation).toBe(10);
    expect(DEMERIT_HALF_LIFE_DAYS.stable).toBe(14);
    expect(DEMERIT_HALF_LIFE_DAYS.canonical).toBe(21);
    expect(DEMERIT_HALF_LIFE_DAYS.enforced).toBe(28);
  });
});
```

- [ ] **Step 2: FAIL 确认**

Run: `pnpm --filter @teamagent/core test -- demerit`

- [ ] **Step 3: 实现 `packages/core/src/calibrator/v2/demerit.ts`**

```typescript
import type { Tier, DeltaStep } from "@teamagent/ports";

const DAY_MS = 24 * 3600 * 1000;

export const DEMERIT_HALF_LIFE_DAYS: Record<Exclude<Tier, "dormant">, number> = {
  experimental: 7,
  probation: 10,
  stable: 14,
  canonical: 21,
  enforced: 28,
};

export const DEMERIT_BASE_BY_TIER: Record<Exclude<Tier, "dormant">, number> = {
  experimental: 1,
  probation: 2,
  stable: 3,
  canonical: 5,
  enforced: 10,
};

export interface DemeritEvent {
  /** ai_override_ignored（AI 被警告后仍原样执行）/ user_reject（用户显式拒绝）/ validator_fail */
  source: "ai_override_ignored" | "user_reject" | "validator_fail" | string;
  timestamp: string;
}

export interface DemeritInput {
  current: number;
  last_updated: string;   // ISO8601, "" 代表从未更新
  current_tier: Tier;
  confidence: number;
}

export interface DemeritResult {
  demerit: number;
  breakdown: DeltaStep[];
}

/**
 * 按当前 tier 衰减现有 demerit，再累加新事件贡献。见设计文档 §4.3。
 * 纯函数：不修改输入。
 */
export function computeDemerit(
  input: DemeritInput,
  events: DemeritEvent[],
  now: Date,
): DemeritResult {
  const breakdown: DeltaStep[] = [];
  const tier: Exclude<Tier, "dormant"> =
    input.current_tier === "dormant" ? "experimental" : (input.current_tier as Exclude<Tier, "dormant">);

  // 1. 衰减现有 demerit
  let d = input.current;
  if (d > 0 && input.last_updated) {
    const daysSince = (now.getTime() - new Date(input.last_updated).getTime()) / DAY_MS;
    if (daysSince > 0) {
      const lambda = Math.LN2 / DEMERIT_HALF_LIFE_DAYS[tier];
      const decayed = d * Math.exp(-lambda * daysSince);
      breakdown.push({
        type: "demerit_decay",
        days_since: daysSince,
        demerit_delta: decayed - d,
        note: `half-life=${DEMERIT_HALF_LIFE_DAYS[tier]}d at tier=${tier}`,
      });
      d = decayed;
    }
  }

  // 2. 累加新事件
  for (const e of events) {
    const base = DEMERIT_BASE_BY_TIER[tier];
    const multiplier =
      input.confidence > 0.5 ? -Math.log(1 - input.confidence) : 1.0;
    const userOverride = e.source === "user_reject" ? 10 : 0;
    const delta = base * multiplier + userOverride;
    breakdown.push({
      type: "demerit_added",
      demerit_delta: delta,
      note: `source=${e.source}, base=${base}, mult=${multiplier.toFixed(2)}, override=+${userOverride}`,
    });
    d += delta;
  }

  return { demerit: Math.max(0, d), breakdown };
}
```

- [ ] **Step 4: PASS**

Run: `pnpm --filter @teamagent/core test -- demerit`

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/calibrator/v2/demerit.ts packages/core/src/calibrator/v2/__tests__/demerit.test.ts
git commit -m "feat(m2.2): Task 6 — Demerit 驾照扣分纯函数"
```

---

## Task 7: 5-Tier 映射 + 阈值表

**目标：** `tierFromConfidence(conf)` 和 `tierFromDemerit(demerit, currentTier)`，按设计 §4.4 / §4.6。

**Files:**
- Create: `packages/core/src/calibrator/v2/tier.ts`
- Test: `packages/core/src/calibrator/v2/__tests__/tier.test.ts`

- [ ] **Step 1: 写测试**

```typescript
import { describe, it, expect } from "vitest";
import {
  tierFromConfidence,
  tierFromDemerit,
  effectiveTier,
  TIER_ORDER,
  CONFIDENCE_UPPER,
} from "../tier.js";

describe("tierFromConfidence", () => {
  it.each([
    [0.0, "experimental"],
    [0.29, "experimental"],
    [0.30, "probation"],
    [0.54, "probation"],
    [0.55, "stable"],
    [0.74, "stable"],
    [0.75, "canonical"],
    [0.89, "canonical"],
    [0.90, "enforced"],
    [1.0, "enforced"],
  ])("conf=%f → tier=%s", (c, expected) => {
    expect(tierFromConfidence(c)).toBe(expected);
  });
});

describe("tierFromDemerit (death chain)", () => {
  it("demerit < 5 at stable → stable", () => {
    expect(tierFromDemerit(4, "stable")).toBe("stable");
  });

  it("demerit >= 5 at stable → soft demote 1 tier", () => {
    expect(tierFromDemerit(5, "stable")).toBe("probation");
  });

  it("demerit >= 15 at canonical → hard demote 2 tiers", () => {
    expect(tierFromDemerit(16, "canonical")).toBe("probation");
  });

  it("demerit >= 30 at any tier → dormant", () => {
    expect(tierFromDemerit(31, "enforced")).toBe("dormant");
    expect(tierFromDemerit(31, "experimental")).toBe("dormant");
  });

  it("experimental cannot soft-demote below experimental", () => {
    expect(tierFromDemerit(5, "experimental")).toBe("experimental");
  });
});

describe("effectiveTier (min of conf/demerit tiers)", () => {
  it("pessimist wins: conf=stable, demerit-dormant → dormant", () => {
    expect(effectiveTier(0.6, 31, "stable")).toBe("dormant");
  });

  it("both happy: conf=enforced, demerit=0 → enforced", () => {
    expect(effectiveTier(0.95, 0, "enforced")).toBe("enforced");
  });
});

describe("TIER_ORDER", () => {
  it("ordered experimental → enforced", () => {
    expect(TIER_ORDER).toEqual([
      "experimental",
      "probation",
      "stable",
      "canonical",
      "enforced",
    ]);
  });
});
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: 实现 `packages/core/src/calibrator/v2/tier.ts`**

```typescript
import type { Tier } from "@teamagent/ports";

export const TIER_ORDER: Exclude<Tier, "dormant">[] = [
  "experimental",
  "probation",
  "stable",
  "canonical",
  "enforced",
];

/** 上界阈值：confidence 跨过此值进入下一 tier。 */
export const CONFIDENCE_UPPER: Record<Exclude<Tier, "dormant">, number> = {
  experimental: 0.30,
  probation: 0.55,
  stable: 0.75,
  canonical: 0.90,
  enforced: 1.01,      // 永不跨
};

/** Confidence → Tier 映射（[下界, 上界)）。 */
export function tierFromConfidence(conf: number): Exclude<Tier, "dormant"> {
  if (conf < 0.30) return "experimental";
  if (conf < 0.55) return "probation";
  if (conf < 0.75) return "stable";
  if (conf < 0.90) return "canonical";
  return "enforced";
}

/**
 * 死亡链：根据 demerit 决定最低允许 tier。返回值是"上限"——
 * 实际 tier 不得高于此值。
 *
 * 规则（设计 §4.6）：
 *   demerit ≥ 5   → 从 current_tier 软降级 1 级
 *   demerit ≥ 15  → 从 current_tier 硬降级 2 级
 *   demerit ≥ 30  → dormant
 */
export function tierFromDemerit(
  demerit: number,
  currentTier: Tier,
): Tier {
  if (demerit >= 30) return "dormant";
  if (currentTier === "dormant") return "dormant";

  const idx = TIER_ORDER.indexOf(currentTier as Exclude<Tier, "dormant">);
  if (idx === -1) return currentTier;

  let demote = 0;
  if (demerit >= 15) demote = 2;
  else if (demerit >= 5) demote = 1;
  const targetIdx = Math.max(0, idx - demote);
  return TIER_ORDER[targetIdx];
}

/** 两条决策链取悲观值。 */
export function effectiveTier(
  confidence: number,
  demerit: number,
  currentTier: Tier,
): Tier {
  const byConf = tierFromConfidence(confidence);
  const byDemerit = tierFromDemerit(demerit, currentTier);
  // dormant 永远胜出
  if (byDemerit === "dormant" || byConf === "dormant" || currentTier === "dormant") {
    return byDemerit === "dormant" || currentTier === "dormant" ? "dormant" : byConf;
  }
  const confIdx = TIER_ORDER.indexOf(byConf);
  const demIdx = TIER_ORDER.indexOf(byDemerit as Exclude<Tier, "dormant">);
  return TIER_ORDER[Math.min(confIdx, demIdx)];
}
```

- [ ] **Step 4: PASS**

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/calibrator/v2/tier.ts packages/core/src/calibrator/v2/__tests__/tier.test.ts
git commit -m "feat(m2.2): Task 7 — 5-tier 映射 + 死亡链 + effectiveTier"
```

---

## Task 8: Hysteresis 防抖逻辑

**目标：** 根据设计 §4.5 决定实际是否执行 tier transition。

**Files:**
- Create: `packages/core/src/calibrator/v2/hysteresis.ts`
- Test: `packages/core/src/calibrator/v2/__tests__/hysteresis.test.ts`

**规则：**

晋升（升 tier）需**同时**：
1. confidence 跨上界
2. 当前 tier 停留 ≥ 10 个观察
3. demerit < 降级阈值 50%（即 demerit < 2.5）

降级（降 tier）满足**任一**：
1. confidence 跨下界 **且** 上次 transition ≥ 7 天
2. demerit 跨死亡链阈值（立即）

- [ ] **Step 1: 写测试**

```typescript
import { describe, it, expect } from "vitest";
import { applyHysteresis, type HysteresisInput } from "../hysteresis.js";

const base: HysteresisInput = {
  current_tier: "probation",
  candidate_tier: "stable",
  confidence: 0.6,
  demerit: 0,
  tier_entered_at: new Date("2026-04-01T00:00:00Z").toISOString(),
  observation_count_in_current_tier: 15,
  now: new Date("2026-04-16T00:00:00Z"),
};

describe("applyHysteresis", () => {
  it("promotion requires 10+ obs in current tier", () => {
    const r = applyHysteresis({ ...base, observation_count_in_current_tier: 5 });
    expect(r.final_tier).toBe("probation");
    expect(r.blocked_reason).toContain("observation");
  });

  it("promotion requires demerit < 2.5", () => {
    const r = applyHysteresis({ ...base, demerit: 3 });
    expect(r.final_tier).toBe("probation");
    expect(r.blocked_reason).toContain("demerit");
  });

  it("promotion allowed when all conditions met", () => {
    const r = applyHysteresis(base);
    expect(r.final_tier).toBe("stable");
    expect(r.blocked_reason).toBeUndefined();
  });

  it("demotion via confidence requires 7+ days since last transition", () => {
    const r = applyHysteresis({
      ...base,
      candidate_tier: "experimental",
      tier_entered_at: new Date("2026-04-13T00:00:00Z").toISOString(), // 3 days ago
    });
    expect(r.final_tier).toBe("probation");
    expect(r.blocked_reason).toContain("7 days");
  });

  it("demotion via confidence allowed after 7+ days", () => {
    const r = applyHysteresis({
      ...base,
      candidate_tier: "experimental",
      tier_entered_at: new Date("2026-04-01T00:00:00Z").toISOString(), // 15 days ago
    });
    expect(r.final_tier).toBe("experimental");
  });

  it("death chain bypasses hysteresis (immediate demote)", () => {
    const r = applyHysteresis({
      ...base,
      candidate_tier: "dormant",
      demerit: 35,
      tier_entered_at: new Date("2026-04-15T23:00:00Z").toISOString(), // <1h ago
    });
    expect(r.final_tier).toBe("dormant");
  });

  it("no change when candidate == current", () => {
    const r = applyHysteresis({ ...base, candidate_tier: "probation" });
    expect(r.final_tier).toBe("probation");
    expect(r.blocked_reason).toBeUndefined();
  });
});
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: 实现 `packages/core/src/calibrator/v2/hysteresis.ts`**

```typescript
import type { Tier } from "@teamagent/ports";
import { TIER_ORDER } from "./tier.js";

const DAY_MS = 24 * 3600 * 1000;
const MIN_OBS_FOR_PROMOTION = 10;
const MIN_DAYS_FOR_DEMOTION = 7;
const MAX_DEMERIT_FOR_PROMOTION = 2.5;

export interface HysteresisInput {
  current_tier: Tier;
  candidate_tier: Tier;
  confidence: number;
  demerit: number;
  tier_entered_at: string;              // ISO8601
  observation_count_in_current_tier: number;
  now: Date;
}

export interface HysteresisResult {
  final_tier: Tier;
  blocked_reason?: string;
}

function tierRank(t: Tier): number {
  if (t === "dormant") return -1;
  return TIER_ORDER.indexOf(t as Exclude<Tier, "dormant">);
}

/**
 * 决定实际 tier。见设计 §4.5。
 *
 * - 候选升 tier: 需 obs ≥ 10 && demerit < 2.5
 * - 候选降 tier: 需距上次 transition ≥ 7 天，**或** demerit ≥ 30（死亡链）
 * - dormant 候选永远立即生效
 */
export function applyHysteresis(input: HysteresisInput): HysteresisResult {
  const cur = input.current_tier;
  const cand = input.candidate_tier;

  if (cur === cand) return { final_tier: cur };

  // 死亡链 → dormant 永远立即
  if (cand === "dormant") return { final_tier: "dormant" };

  const curRank = tierRank(cur);
  const candRank = tierRank(cand);

  if (candRank > curRank) {
    // 升 tier
    if (input.observation_count_in_current_tier < MIN_OBS_FOR_PROMOTION) {
      return {
        final_tier: cur,
        blocked_reason: `need ≥ ${MIN_OBS_FOR_PROMOTION} observations in current tier (have ${input.observation_count_in_current_tier})`,
      };
    }
    if (input.demerit >= MAX_DEMERIT_FOR_PROMOTION) {
      return {
        final_tier: cur,
        blocked_reason: `demerit ${input.demerit.toFixed(2)} ≥ promotion threshold ${MAX_DEMERIT_FOR_PROMOTION}`,
      };
    }
    return { final_tier: cand };
  }

  // 降 tier（非 dormant）
  if (input.demerit >= 30) return { final_tier: cand }; // 死亡链 immediate
  const daysSince =
    (input.now.getTime() - new Date(input.tier_entered_at).getTime()) / DAY_MS;
  if (daysSince < MIN_DAYS_FOR_DEMOTION) {
    return {
      final_tier: cur,
      blocked_reason: `demotion requires ≥ 7 days in current tier (${daysSince.toFixed(1)} days so far)`,
    };
  }
  return { final_tier: cand };
}
```

- [ ] **Step 4: PASS**

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/calibrator/v2/hysteresis.ts packages/core/src/calibrator/v2/__tests__/hysteresis.test.ts
git commit -m "feat(m2.2): Task 8 — Hysteresis 防抖（升/降 tier）"
```

---

## Task 9: 组装 `v2Calibrator`（主实现）

**目标：** 把 wilson/demerit/tier/hysteresis 拼成 `CalibratorV2`，通过契约测试。

**Files:**
- Create: `packages/core/src/calibrator/v2/index.ts`
- Create: `packages/core/src/calibrator/v2/__tests__/v2.test.ts`

- [ ] **Step 1: 写测试（含契约 + 集成）**

```typescript
import { describe, it, expect } from "vitest";
import { runCalibratorV2Contract } from "@teamagent/ports/contracts";
import { v2Calibrator } from "../index.js";
import type { CalibratorV2Input, Observation } from "@teamagent/ports";
import type { KnowledgeEntry, PersistedEvent } from "@teamagent/types";

describe("v2Calibrator", () => {
  runCalibratorV2Contract(() => v2Calibrator);
});

describe("v2Calibrator integration", () => {
  const now = new Date("2026-04-16T12:00:00Z");
  const entry: KnowledgeEntry = {
    id: "r-int",
    scope: { level: "team" },
    category: "E",
    tags: [],
    type: "avoidance",
    nature: "subjective",
    trigger: "t",
    wrong_pattern: "w",
    correct_pattern: "c",
    reasoning: "r",
    confidence: 0.6,
    enforcement: "suggest",
    status: "active",
    hit_count: 0,
    success_count: 0,
    override_count: 0,
    evidence: { success_sessions: 0, success_users: 0, correction_sessions: 0 },
    created_at: "2026-04-01T00:00:00Z",
    last_hit_at: "",
    last_validated_at: "2026-04-01T00:00:00Z",
    source: "accumulated",
    conflict_with: [],
    current_tier: "probation",
    max_tier_ever: "probation",
    tier_entered_at: "2026-04-01T00:00:00Z", // 15 天前
    demerit: 0,
    demerit_last_updated: "",
    resurrect_count: 0,
  };

  it("accumulates 20 success obs → promotes probation → stable", () => {
    const observations: Observation[] = Array.from({ length: 20 }, (_, i) => ({
      id: `o${i}`,
      knowledge_id: "r-int",
      timestamp: new Date(now.getTime() - i * 3600 * 1000).toISOString(),
      outcome: "success",
    }));
    const r = v2Calibrator.calibrate(entry, { events: [], observations, now });
    expect(r.tier_after).toBe("stable");
    expect(r.confidence).toBeGreaterThanOrEqual(0.55);
    expect(r.tier_transition?.from).toBe("probation");
  });

  it("1 user_reject event at stable → demerit 10+, demote to probation", () => {
    const events: PersistedEvent[] = [
      {
        id: "e-reject",
        kind: "calibrator.user_reject" as any,
        knowledge_id: "r-int",
        timestamp: now.toISOString(),
        schema_version: 1,
      } as PersistedEvent,
    ];
    const stableEntry = {
      ...entry,
      current_tier: "stable" as const,
      confidence: 0.6,
      tier_entered_at: "2026-04-01T00:00:00Z",
    };
    const r = v2Calibrator.calibrate(stableEntry, {
      events,
      observations: [],
      now,
    });
    expect(r.demerit).toBeGreaterThanOrEqual(10);
    expect(["probation", "stable"]).toContain(r.tier_after);
  });

  it("breakdown contains steps for each computation", () => {
    const obs: Observation[] = [
      {
        id: "o1",
        knowledge_id: "r-int",
        timestamp: now.toISOString(),
        outcome: "success",
      },
    ];
    const r = v2Calibrator.calibrate(entry, { events: [], observations: obs, now });
    expect(r.delta_breakdown.length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: 实现 `packages/core/src/calibrator/v2/index.ts`**

```typescript
import type {
  CalibratorV2,
  CalibratorV2Input,
  CalibrationResultV2,
  Observation,
  Tier,
  TierTransition,
  DeltaStep,
} from "@teamagent/ports";
import type { KnowledgeEntry, PersistedEvent } from "@teamagent/types";
import { computeConfidence } from "./wilson.js";
import { computeDemerit, type DemeritEvent } from "./demerit.js";
import { effectiveTier } from "./tier.js";
import { applyHysteresis } from "./hysteresis.js";

/** 事件 kind → demerit 触发源映射。其它事件不产生 demerit。 */
const DEMERIT_KIND_TO_SOURCE: Record<string, DemeritEvent["source"]> = {
  "ai.override.ignored": "ai_override_ignored",
  "calibrator.user_reject": "user_reject",
  "validator.failure": "validator_fail",
};

function eventsToDemeritEvents(
  events: PersistedEvent[],
  knowledgeId: string,
): DemeritEvent[] {
  const out: DemeritEvent[] = [];
  for (const e of events) {
    if (e.knowledge_id !== knowledgeId) continue;
    const source = DEMERIT_KIND_TO_SOURCE[e.kind];
    if (!source) continue;
    out.push({ source, timestamp: e.timestamp });
  }
  return out;
}

function statusFromTier(
  oldStatus: KnowledgeEntry["status"],
  tier: Tier,
): KnowledgeEntry["status"] {
  if (tier === "dormant") return "dormant";
  if (oldStatus === "conflict" || oldStatus === "stale") return oldStatus;
  return "active";
}

export const v2Calibrator: CalibratorV2 = {
  calibrate(
    entry: KnowledgeEntry,
    input: CalibratorV2Input,
  ): CalibrationResultV2 {
    const ownObs: Observation[] = input.observations.filter(
      (o) => o.knowledge_id === entry.id,
    );

    // 1. Confidence (Wilson)
    const newConfidence = computeConfidence(
      ownObs,
      entry.max_tier_ever as Tier,
      input.now,
    );

    // 2. Demerit
    const demeritEvents = eventsToDemeritEvents(input.events, entry.id);
    const demeritRes = computeDemerit(
      {
        current: entry.demerit,
        last_updated: entry.demerit_last_updated,
        current_tier: entry.current_tier as Tier,
        confidence: newConfidence,
      },
      demeritEvents,
      input.now,
    );

    // 3. 候选 tier
    const candidate = effectiveTier(
      newConfidence,
      demeritRes.demerit,
      entry.current_tier as Tier,
    );

    // 4. Hysteresis
    const obsInCurrentTier = ownObs.filter(
      (o) => new Date(o.timestamp) >= new Date(entry.tier_entered_at || entry.created_at),
    ).length;
    const hys = applyHysteresis({
      current_tier: entry.current_tier as Tier,
      candidate_tier: candidate,
      confidence: newConfidence,
      demerit: demeritRes.demerit,
      tier_entered_at: entry.tier_entered_at || entry.created_at,
      observation_count_in_current_tier: obsInCurrentTier,
      now: input.now,
    });

    // 5. Breakdown
    const breakdown: DeltaStep[] = [];
    if (ownObs.length > 0) {
      breakdown.push({
        type: "obs_added",
        weight: ownObs.length,
        conf_delta: newConfidence - entry.confidence,
        note: `${ownObs.length} observations → Wilson LB`,
      });
    }
    breakdown.push(...demeritRes.breakdown);
    const tierTransition: TierTransition | null =
      hys.final_tier !== entry.current_tier
        ? {
            from: entry.current_tier as Tier,
            to: hys.final_tier,
            reason:
              demeritRes.demerit >= 30
                ? "death_chain_dormant"
                : hys.final_tier === entry.current_tier
                ? "no_change"
                : "hysteresis_passed",
          }
        : null;
    if (tierTransition) {
      breakdown.push({
        type: "tier_transition",
        note: `${tierTransition.from} → ${tierTransition.to} (${tierTransition.reason})`,
      });
    }

    return {
      confidence: newConfidence,
      demerit: demeritRes.demerit,
      tier_before: entry.current_tier as Tier,
      tier_after: hys.final_tier,
      status: statusFromTier(entry.status, hys.final_tier),
      confidence_delta: newConfidence - entry.confidence,
      demerit_delta: demeritRes.demerit - entry.demerit,
      delta_breakdown: breakdown,
      tier_transition: tierTransition,
      reason_for_no_transition: hys.blocked_reason,
    };
  },
};
```

- [ ] **Step 4: PASS**

Run: `pnpm --filter @teamagent/core test -- v2`
Expected: 契约测试 + 集成测试全绿。

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/calibrator/v2/index.ts packages/core/src/calibrator/v2/__tests__/v2.test.ts
git commit -m "feat(m2.2): Task 9 — v2Calibrator 主实现，组装 wilson/demerit/tier/hysteresis"
```

---

## Task 10: v2 Pipeline —— Events → Observations 胶水

**目标：** 写一个 pipeline 函数把 SQLite `events` + `observations` 两表喂给 v2 calibrator。

**Files:**
- Create: `packages/core/src/pipeline/calibration-pipeline-v2.ts`
- Test: `packages/core/src/pipeline/__tests__/calibration-pipeline-v2.test.ts`

- [ ] **Step 1: 写测试**

```typescript
import { describe, it, expect } from "vitest";
import { runCalibrationPipelineV2 } from "../calibration-pipeline-v2.js";
import { v2Calibrator } from "../../calibrator/v2/index.js";
import type { KnowledgeEntry, PersistedEvent } from "@teamagent/types";
import type { Observation } from "@teamagent/ports";

// TODO: 用 in-memory 或 mock store；沿用现有 test helpers
// 参考 packages/core/src/pipeline/__tests__/calibration-pipeline.test.ts 里的 MemoryStore

describe("calibration-pipeline-v2", () => {
  it("scans all active entries, runs v2 calibrator, writes back tier/demerit/confidence", async () => {
    // 见 Step 3 实现。测试内容：
    // 1. 构造 2 条 knowledge + 一串 observations
    // 2. 跑 pipeline
    // 3. 断言 store 里 confidence/tier/demerit 被更新
    // 4. 断言 bus emit 了 calibrator.v2.adjusted 事件
  });

  it("skips entries with no observations", async () => {
    // 略
  });

  it("honors dryRun (does not write to store)", async () => {
    // 略
  });
});
```

> **注：** 完整测试按 v1 pipeline test 的 `MemoryStore` 模式复制；v1 测试文件路径：`packages/core/src/pipeline/__tests__/calibration-pipeline.test.ts`。先读懂再照搬结构。

- [ ] **Step 2: 实现 `packages/core/src/pipeline/calibration-pipeline-v2.ts`**

```typescript
import type {
  CalibratorV2,
  KnowledgeStore,
  AttributionBus,
  Observation,
  TierTransition,
  DeltaStep,
} from "@teamagent/ports";
import type { KnowledgeEntry, PersistedEvent } from "@teamagent/types";

export interface CalibrationV2Deps {
  calibrator: CalibratorV2;
  store: KnowledgeStore;
  events: PersistedEvent[];
  observations: Observation[];
  bus?: AttributionBus;
  now: () => Date;
  dryRun?: boolean;
}

export interface CalibrationV2Record {
  knowledge_id: string;
  confidence_before: number;
  confidence_after: number;
  demerit_before: number;
  demerit_after: number;
  tier_before: string;
  tier_after: string;
  tier_transition: TierTransition | null;
  delta_breakdown: DeltaStep[];
}

export interface CalibrationV2Result {
  scanned: number;
  adjusted: CalibrationV2Record[];
  dormantNew: string[];
}

export async function runCalibrationPipelineV2(
  deps: CalibrationV2Deps,
): Promise<CalibrationV2Result> {
  const entries = deps.store.getAll();
  const now = deps.now();

  // Observations / events 按 knowledge_id 索引一次
  const obsIdx = indexByKnowledgeId(deps.observations);
  const evtIdx = indexByKnowledgeId(deps.events);

  const adjusted: CalibrationV2Record[] = [];
  const dormantNew: string[] = [];

  for (const entry of entries) {
    if (entry.status === "archived" || entry.status === "dormant") continue;
    const obsForEntry = obsIdx.get(entry.id) ?? [];
    const evtForEntry = evtIdx.get(entry.id) ?? [];
    if (obsForEntry.length === 0 && evtForEntry.length === 0 && entry.demerit === 0) {
      continue; // 无信号
    }

    const result = deps.calibrator.calibrate(entry, {
      events: evtForEntry as PersistedEvent[],
      observations: obsForEntry as Observation[],
      now,
    });

    const confChanged = Math.abs(result.confidence_delta) > 1e-6;
    const demChanged = Math.abs(result.demerit_delta) > 1e-6;
    const tierChanged = result.tier_transition !== null;
    if (!confChanged && !demChanged && !tierChanged) continue;

    if (!deps.dryRun) {
      deps.store.update(entry.id, {
        confidence: result.confidence,
        demerit: result.demerit,
        current_tier: result.tier_after,
        status: result.status,
        demerit_last_updated: now.toISOString(),
        tier_entered_at: tierChanged ? now.toISOString() : entry.tier_entered_at,
        max_tier_ever:
          tierChanged && tierRankGt(result.tier_after, entry.max_tier_ever)
            ? result.tier_after
            : entry.max_tier_ever,
        last_validated_at: now.toISOString(),
      } as Partial<KnowledgeEntry>);
    }

    if (result.tier_after === "dormant" && entry.status !== "dormant") {
      dormantNew.push(entry.id);
    }

    adjusted.push({
      knowledge_id: entry.id,
      confidence_before: entry.confidence,
      confidence_after: result.confidence,
      demerit_before: entry.demerit,
      demerit_after: result.demerit,
      tier_before: result.tier_before,
      tier_after: result.tier_after,
      tier_transition: result.tier_transition,
      delta_breakdown: result.delta_breakdown,
    });

    deps.bus?.emit({
      source: "calibrator",
      action: "v2_adjusted",
      target: { id: entry.id },
      before: { confidence: entry.confidence, tier: entry.current_tier, demerit: entry.demerit },
      after: { confidence: result.confidence, tier: result.tier_after, demerit: result.demerit },
      severity: result.tier_after === "dormant" ? "warning" : "info",
      userFacingValue:
        result.tier_transition
          ? `${entry.id}: ${result.tier_before} → ${result.tier_after} (${result.tier_transition.reason})`
          : `${entry.id}: conf ${entry.confidence.toFixed(2)} → ${result.confidence.toFixed(2)}`,
      timestamp: now.toISOString(),
    });
  }

  return { scanned: entries.length, adjusted, dormantNew };
}

function indexByKnowledgeId<T extends { knowledge_id?: string }>(
  items: T[],
): Map<string, T[]> {
  const m = new Map<string, T[]>();
  for (const it of items) {
    if (!it.knowledge_id) continue;
    const list = m.get(it.knowledge_id);
    if (list) list.push(it);
    else m.set(it.knowledge_id, [it]);
  }
  return m;
}

const TIER_RANK: Record<string, number> = {
  experimental: 0,
  probation: 1,
  stable: 2,
  canonical: 3,
  enforced: 4,
  dormant: -1,
};
function tierRankGt(a: string, b: string): boolean {
  return (TIER_RANK[a] ?? -1) > (TIER_RANK[b] ?? -1);
}
```

- [ ] **Step 3: PASS**

Run: `pnpm --filter @teamagent/core test -- calibration-pipeline-v2`

- [ ] **Step 4: Commit**

```bash
git add packages/core/src/pipeline/calibration-pipeline-v2.ts packages/core/src/pipeline/__tests__/calibration-pipeline-v2.test.ts
git commit -m "feat(m2.2): Task 10 — calibration-pipeline-v2（events+obs 驱动）"
```

---

## Task 11: `KnowledgeStore.update` 允许 v2 字段

**目标：** 核对 `KnowledgeStore` port 的 `update()` 签名能接受 `demerit` / `current_tier` / `tier_entered_at` / `max_tier_ever` / `demerit_last_updated`。若用了严格 `Partial<KnowledgeEntry>` 则自动支持；如为明确白名单则补齐。

**Files:**
- Inspect / modify: `packages/ports/src/knowledge-store.ts`
- Inspect / modify: `packages/adapters/src/storage/sqlite/sqlite-knowledge-store.ts`（若 Task 2 未覆盖 update 的新字段，补齐）

- [ ] **Step 1: 读 `packages/ports/src/knowledge-store.ts`**

若 `update(id, patch: Partial<KnowledgeEntry>): void`，则 schema 扩展（Task 1）已自动使新字段合法。

- [ ] **Step 2: 测试：`update({ demerit: 3, current_tier: "probation" })` 后 `getById()` 能读回**

如 Task 2 测试里没有该断言（含"partial update preserves v2 fields"），再追加一条：

```typescript
it("update({ demerit, current_tier }) persists to DB", () => {
  const s = makeFreshStore();
  s.add({ ...baseEntry, id: "r-u" } as any);
  s.update("r-u", { demerit: 3, current_tier: "probation", tier_entered_at: "2026-04-16T00:00:00Z" } as any);
  const back = s.getById("r-u");
  expect(back?.demerit).toBe(3);
  expect(back?.current_tier).toBe("probation");
});
```

- [ ] **Step 3: 如 FAIL，改 `sqlite-knowledge-store.ts` 的 `update()`**

确保动态 UPDATE 列名白名单包含新字段。

- [ ] **Step 4: PASS**

Run: `pnpm --filter @teamagent/adapters test -- sqlite-knowledge-store`

- [ ] **Step 5: Commit**

```bash
git add packages/adapters/src/storage/sqlite/sqlite-knowledge-store.ts packages/adapters/src/storage/sqlite/__tests__/sqlite-knowledge-store.test.ts
git commit -m "feat(m2.2): Task 11 — KnowledgeStore.update 支持 v2 字段"
```

> 如 Task 2 已覆盖此 case，本 Task 标记跳过，commit 空提交不必要 —— 直接跳到 Task 12。

---

## Task 12: CLI `stats --explain <rule-id>` 展示 delta breakdown

**目标：** 给用户一个入口看 calibrator 做了什么。读取最近一次 `calibrator.v2_adjusted` 事件 payload 并格式化打印。

**Files:**
- Modify: `packages/cli/src/commands/stats.ts`
- Test: `packages/cli/src/commands/__tests__/stats-explain.test.ts`

- [ ] **Step 1: 测试：`stats({ explain: "rule-x" })` 打印 breakdown**

```typescript
import { describe, it, expect, vi } from "vitest";
import { executeStats } from "../stats.js";

describe("stats --explain", () => {
  it("prints tier/confidence/demerit with delta breakdown", async () => {
    const logs: string[] = [];
    const log = (s: string) => logs.push(s);
    await executeStats({
      explain: "rule-x",
      logger: log,
      // 传入测试用的 store/events DB 或 mock
    } as any);
    const out = logs.join("\n");
    expect(out).toMatch(/tier:/);
    expect(out).toMatch(/confidence:/);
    expect(out).toMatch(/demerit:/);
  });
});
```

- [ ] **Step 2: 修 `stats.ts`**

在 options 里加 `explain?: string`；分支：

```typescript
if (options.explain) {
  const entry = store.getById(options.explain);
  if (!entry) { log(`rule ${options.explain} not found`); return; }
  const lastEvent = eventLog
    .query({ kind: "calibrator.v2_adjusted", knowledge_id: options.explain, limit: 1 })
    .at(0);
  log(`rule ${entry.id}`);
  log(`  tier: ${entry.current_tier} (max ever: ${entry.max_tier_ever})`);
  log(`  confidence: ${entry.confidence.toFixed(3)}`);
  log(`  demerit: ${entry.demerit.toFixed(2)} (updated ${entry.demerit_last_updated || "never"})`);
  if (lastEvent?.payload?.delta_breakdown) {
    log("  last calibration breakdown:");
    for (const step of lastEvent.payload.delta_breakdown) {
      log(`    - ${step.type}: ${step.note ?? ""}`);
    }
  }
  return;
}
```

- [ ] **Step 3: PASS**

Run: `pnpm --filter @teamagent/cli test -- stats-explain`

- [ ] **Step 4: Commit**

```bash
git add packages/cli/src/commands/stats.ts packages/cli/src/commands/__tests__/stats-explain.test.ts
git commit -m "feat(m2.2): Task 12 — CLI stats --explain 展示 v2 delta breakdown"
```

---

## Task 13: 把 v2 Calibrator 接入 CLI `calibrate` 命令

**目标：** 让 `pnpm teamagent calibrate` 跑 v2 pipeline（而非 v1 defaultCalibrator）。保留 `--legacy` flag 供对照时用 v1。

**Files:**
- Modify: `packages/cli/src/commands/calibrate.ts`
- Modify: `packages/cli/src/commands/__tests__/calibrate.test.ts`

- [ ] **Step 1: 加测试**

```typescript
it("uses v2 calibrator by default and writes tier/demerit", async () => {
  const env = await makeTestEnv(); // 沿用现有 helper
  // 预置一个 rule + 10 observations
  // 跑 calibrate
  // 断言 rule 的 tier/demerit/confidence 被更新
});

it("--legacy falls back to v1", async () => {
  // 略
});
```

- [ ] **Step 2: 修改 `calibrate.ts`**

- 从 `@teamagent/adapters` import `SqliteObservations`
- 读 observations 表
- 默认走 `runCalibrationPipelineV2(v2Calibrator, ...)`
- `--legacy` flag 走原 `runCalibrationPipeline`

伪代码：

```typescript
if (options.legacy) {
  const result = await runCalibrationPipeline({ calibrator: defaultCalibrator, ... });
  formatV1(result);
} else {
  const obs = sqliteObservations.listAll(days);
  const result = await runCalibrationPipelineV2({
    calibrator: v2Calibrator,
    store,
    events,
    observations: obs,
    bus,
    now: () => options.now?.() ?? new Date(),
    dryRun: options.dryRun,
  });
  formatV2(result);
}
```

- [ ] **Step 3: PASS + typecheck**

Run: `pnpm --filter @teamagent/cli test -- calibrate`
Run: `pnpm --filter @teamagent/cli typecheck`

- [ ] **Step 4: Commit**

```bash
git add packages/cli/src/commands/calibrate.ts packages/cli/src/commands/__tests__/calibrate.test.ts
git commit -m "feat(m2.2): Task 13 — CLI calibrate 默认走 v2 pipeline，--legacy 回 v1"
```

---

## Task 14: 迁移现有 knowledge 的 tier 默认值

**目标：** M2.1 迁移后所有 v1 规则 `current_tier="experimental"`，但 `tier_entered_at` 可能是空字符串 —— 让首次 calibrate 不至于 `hysteresis.tier_entered_at` 爆炸。

**Files:**
- Modify: `packages/adapters/src/storage/sqlite/sqlite-knowledge-store.ts`（`add()` 的 defaults）
- Modify: `packages/cli/src/commands/migrate.ts`（回填空 `tier_entered_at`）
- Test: 加一条 migration 单测

- [ ] **Step 1: 测试：add() 时 `tier_entered_at` 空 → 自动填 `created_at`**

```typescript
it("add() fills tier_entered_at from created_at when empty", () => {
  const s = makeFreshStore();
  s.add({ ...baseEntry, id: "r-empty", tier_entered_at: "", created_at: "2026-01-01T00:00:00Z" } as any);
  const back = s.getById("r-empty");
  expect(back?.tier_entered_at).toBe("2026-01-01T00:00:00Z");
});
```

- [ ] **Step 2: 在 `sqlite-knowledge-store.ts` 的 `add()` 里做 fallback**

```typescript
const tierEnteredAt = entry.tier_entered_at && entry.tier_entered_at.length > 0
  ? entry.tier_entered_at
  : entry.created_at;
```

- [ ] **Step 3: `migrate.ts` 对旧条目补齐 `tier_entered_at`**

若 `migrate` 命令已从 v1 → v2 搬数据，追加一步：在 insert 前设 `tier_entered_at = old.created_at`。跑 dry-run 验证预期。

- [ ] **Step 4: PASS**

Run: `pnpm --filter @teamagent/cli test -- migrate`

- [ ] **Step 5: Commit**

```bash
git add packages/adapters/src/storage/sqlite/ packages/cli/src/commands/migrate.ts
git commit -m "fix(m2.2): Task 14 — tier_entered_at 空值 fallback + migrate 回填"
```

---

## Task 15: Dogfood —— skeleton-demo 跑通 M2.2

**目标：** Walking Skeleton 不断裂。手动跑一遍全链路。

**Files:** 无新文件；可能需补 `packages/cli/src/commands/skeleton-demo.ts` 里 v2 展示。

- [ ] **Step 1: 全测试绿**

Run: `pnpm test`
Expected: 所有包全绿（v1 contract 依旧通过 default calibrator；v2 contract 通过 v2Calibrator）。

- [ ] **Step 2: typecheck 全绿**

Run: `pnpm typecheck`

- [ ] **Step 3: 手动 e2e**

```bash
# 用临时目录避免污染
export T=$(mktemp -d)
cd $T && pnpm teamagent init

# 造一条规则
pnpm teamagent pitfall --non-interactive --trigger="x" --correct="y"

# 模拟几条 observations（手动 INSERT 到 observations 表或扩展 CLI）
# 略：本步仅目测 schema + pipeline 可跑

# 跑 calibrate
pnpm teamagent calibrate --days=30

# stats 解释
pnpm teamagent stats --explain <rule-id>
```

Expected: stats 输出 tier/confidence/demerit + breakdown；calibrate 无 crash。

- [ ] **Step 4: 必要时 commit 小修**

```bash
# 若上一步暴露了问题，修完再 commit：
git add <files>
git commit -m "fix(m2.2): Task 15 — dogfood 中发现的问题"
```

---

## Task 16: 更新文档经验条目

**目标：** CLAUDE.md 经验区沉淀本里程碑踩的坑（见 `teamagent pitfall` 用法）。

- [ ] **Step 1: 手动 review dogfood 过程记的 pitfalls**

- [ ] **Step 2: 用 `teamagent pitfall` CLI 登记 1-3 条**

示例：
```bash
pnpm teamagent pitfall --non-interactive \
  --trigger="写 v2 calibrator 公式验证" \
  --wrong="直接从 events 算成功率" \
  --correct="用 observations 表 + Wilson LB + 时间衰减加权" \
  --reasoning="events 里混 pre/post 噪声；observations 已经是清洗过的 outcome"
```

- [ ] **Step 3: 重编译 CLAUDE.md**

Run: `pnpm teamagent compile`
Expected: CLAUDE.md 尾部 "TeamAgent 经验" 区出现新增条目。

- [ ] **Step 4: Commit**

```bash
git add CLAUDE.md .teamagent/
git commit -m "docs(m2.2): Task 16 — 登记 M2.2 dogfood pitfalls"
```

---

## Task 17: Milestone close-out

- [ ] **Step 1: 写 M2.2 完成说明**

在 `docs/superpowers/plans/2026-04-16-m2.2-calibrator-v2.md` 顶部加 closeout 状态段：

```markdown
## Status: COMPLETED <YYYY-MM-DD>

- 17 tasks merged to master
- All tests green (`pnpm test`)
- Dogfood verified: calibrate + stats --explain 可用
```

- [ ] **Step 2: 最终 commit**

```bash
git add docs/superpowers/plans/2026-04-16-m2.2-calibrator-v2.md
git commit -m "docs(m2.2): Milestone close-out — Calibrator v2 landed"
```

- [ ] **Step 3: 告知用户**

输出里程碑摘要：tier 分布变化、demerit 均值、最近 `calibrator.v2_adjusted` 事件样本，供用户审核。

---

## Self-Review Checklist

| 检查项 | 状态 |
|--------|------|
| 设计文档 §4.1 双轨分数（confidence + demerit） | T5 + T6 |
| §4.2 Wilson LB + 时间衰减 + HALF_LIFE_BY_MAX_HISTORICAL_TIER | T5 |
| §4.3 Demerit + log-loss + user_reject + 按 current_tier 衰减 | T6 |
| §4.4 5-tier 映射 + 阈值表 | T7 |
| §4.5 Hysteresis（升 obs≥10 / 降 7 天 / demerit<2.5） | T8 |
| §4.6 死亡链 4 级（5/15/30/50） | T7 + T8（≥30 已实现；"≥50 提示删除"未在本里程碑，设计文档也是仅提示） |
| §4.7 复活路径（resurrect_count） | schema 已存；命令 `teamagent revive` 放 **M2.3 之后**（需 CLI 子命令工作） |
| §4.8 透明化 event log（delta_breakdown） | T10（pipeline emit）+ T12（CLI 展示） |
| §十三 M2.2 所有子项 | 覆盖 |

**遗留至后续里程碑：**
- `teamagent revive <id>` CLI 命令（M2.3 或专项 hotfix）
- `teamagent delete <id>`（demerit ≥ 50 提示路径）
- 3 次 resurrect 永久归档（需 revive 命令先行）

—— 本 plan 完成后 M2.2 的核心机制（Wilson + Demerit + 5-tier + Hysteresis + 死亡链 ≥30 级 + delta breakdown）全部落地，可度量可观察。复活/删除 CLI 是工具性封装，不影响机制正确性，延后执行风险低。
