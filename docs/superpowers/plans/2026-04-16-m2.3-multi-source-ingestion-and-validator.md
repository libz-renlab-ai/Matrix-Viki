# M2.3 多源摄入 + 分级 Validator Implementation Plan

## Status: COMPLETED 2026-04-16

- 起草日：2026-04-16（M2.2 完成日同一天）
- 完成日：2026-04-16（18 tasks 单日落地 + dogfood）
- 目标：把 Phase 2 SP-1 项 3（分级 Validator）和项 6（6 源摄入）落地，让规则有**正经的质量门闸**、**不只靠 session 纠正一条信号源**

**Landed：**
- 18 tasks merged to master（e5524bc 之后到 HEAD）
- 所有测试绿：`pnpm test` 657/657，`pnpm typecheck` 无错
- 6 源摄入可用：Detector（原有）+ /insights + npm audit + PR review + git hotspot + CI failure
- L0/L1/L2 三级 validator：L0 作为 extract-pipeline 和 ingest-pipeline 的入库门；L1/L2 作为 calibration-pipeline-v2 的晋升门
- Dogfood 验证：
  - `pnpm teamagent skeleton-demo` 展示 L0 拒绝流程
  - `pnpm teamagent ingest --from-git --since=30d --dry-run` 生成 50 条候选 md
  - `pnpm teamagent pitfall` 录入 3 条 dogfood 经验（知识库 35 → 38 条）
- 刻意未做（留 M2.4+）：
  - --from-insights / --from-audit / --from-pr 的完整 LLM 链路自动 e2e（本地 dogfood 需真跑 claude CLI）
  - sqlite-vec 相似规则召回（L1 的 similarityFinder 目前接口在但未默认实现）
  - stuck-in-promotion 命令给被 L1 反复 block 的规则一个出口

---

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 TeamAgent Phase 2 M2.3 —— 分级 Validator（L0 机械 / L1 Haiku / L2 Sonnet）+ 多源规则摄入（6 源：Detector / /insights / npm audit / PR review / git log / CI）。让"规则入库前有机械门、晋升高 tier 前有 LLM 语义门、数据来源不再仅限于 session 纠正"。

**Architecture:**
- Functional-Core / Imperative-Shell 继续。Validator 是 Port（ L0/L1/L2 三级各自纯函数或注入 LLM 的函数），IO（gh/npm/git）在 adapter 层。
- **3 级 Validator 触发时机不同**：
  - **L0**：规则入库前（`store.add` 前的门闸），**每条都跑**，零 LLM 成本
  - **L1**：规则晋升 stable 时（hysteresis 批准后、实际落地前），走 Haiku
  - **L2**：规则晋升 canonical+ 时，走 Sonnet
  - 因此 Validator 接入点**不是一个**——一处在 ingest-pipeline，另一处在 calibration-pipeline-v2 的 `applyHysteresis` 下游
- **双 pipeline 共存**：原 `extract-pipeline`（Detector 源的 session 纠正）和新增 `ingest-pipeline`（其余 5 源）各自独立入口但**共用 L0 门闸 + store + extractor**。区别在于输入形式：前者从 `ParsedSession` 抽 correction moments 喂 extractor，后者直接吃 `ExtractionInput[]`。不合并成单 pipeline 是因为 session 解析与 raw 材料解析职责差距大，强合并会让 detector deps 成为 ingest 的死权重。
- **多源摄入采用 source-agnostic pipeline**：每个源只负责把原始材料转成 `ExtractionInput[]`；后续 extractor → L0 → store 共用一条通路
- **6 源分两档**：
  - 自动档（4 源，高 SNR）：Detector（已有）/ /insights / npm audit / PR review
  - 半自动档（2 源，低 SNR）：git log hotspot / CI failure log —— 输出 candidate 列表让用户勾选
- GitHub/CI 依赖 `gh` CLI 桥接（项目已习惯用 `gh`），不引入 Octokit，保持 local-first

**Tech Stack:** TypeScript、Vitest、zod、pnpm workspace、`gh` CLI（child_process 包装，adapter 层）、`npm audit --json`。

**参考文档：**
- 设计源：`docs/superpowers/specs/2026-04-15-phase2-design-v2.md` §5.3（Validator 分级）+ §5.5（多源摄入）+ §十三（M2.3 拆分）
- 依赖的已落地基础：`docs/superpowers/plans/2026-04-16-m2.2-calibrator-v2.md`（Calibrator v2 + observations + tier/demerit）
- 项目开发约定：`CLAUDE.md`（Functional Core / Port 先写契约 / 小 commit）
- Backlog 中相关条目：`docs/specs/2026-04-15-phase2-backlog.md` §B1（scope.paths 自动推断）/ §D（规则质量审查）

**关键决策（已确认 / 可挑战）：**
1. `ExtractionInput.kind` 从 3 扩到 7：`correction | success | rule-text | insights | npm-audit | pr-review | git-hotspot | ci-failure`。prompt 按 kind 分派不同 header。
2. Validator Port 是**3 个方法的单接口**（`validateLevel0/1/2`），不是 3 个 port。让实现只需一个导出对象，调用方按需分派。
3. 半自动 CLI 产出的 candidate 文件是**可编辑的 TOML/Markdown**——用户勾选后直接 `teamagent ingest --from-candidates <file>` 吃回去。不做交互式 TUI（M2.3 范围太重）。
4. L1/L2 失败的规则**不降级已有 tier**，只阻止**继续晋升**（候选 tier 回落当前 tier）。避免 Haiku 误判反噬历史积累。
5. PR / CI 源**通过 gh CLI** 实现（已是项目依赖环境），不引入 HTTP client 或 Octokit。

---

## 实施顺序与依赖

```
T1 Validator port + 类型 ─┐
T2 Validator 契约测试 ────┤
T3 ExtractionInput kind 扩展 + prompt 分派 + source enum 扩展
                          ↓
T4 L0 纯函数（5 项机械检查）
                          ↓
T5 L0 接入 extract-pipeline（Detector 入库门闸）
                          ↓
T6 source-agnostic ingest-pipeline
   ├─ T7  /insights 源适配器 + CLI
   ├─ T8  npm audit 源适配器 + CLI
   ├─ T9  PR review 源适配器 + CLI (gh)
   ├─ T10 git log hotspot（半自动 dry-run）
   └─ T11 CI failure log（半自动 dry-run）
                          ↓
T12 L1 Haiku 实现 ──┐
T13 L2 Sonnet 实现 ─┤
                    ↓
T14 defaultValidator 组装（过 Validator 契约）
                          ↓
T15 L1/L2 接入 calibration-pipeline-v2（晋升门闸）
                          ↓
T16 skeleton-demo dogfood + 补 CLI 展示
T17 登记 M2.3 dogfood pitfalls + 更新 CLAUDE.md
T18 Milestone close-out
```

---

## Task 1: 定义 `Validator` port 接口

**目标：** 新增 Port，包含 L0/L1/L2 三级方法。契约先行，实现在后。

**Files:**
- Create: `packages/ports/src/validator.ts`
- Modify: `packages/ports/src/index.ts`

- [ ] **Step 1: 新建 `packages/ports/src/validator.ts`**

```typescript
import type { KnowledgeEntry } from "@teamagent/types";

/** L0 机械检查的结果。 */
export interface ValidationL0Result {
  ok: boolean;
  failed_checks: string[];           // 具体失败项，如 "wrong_pattern_not_in_source"
  notes?: string;
}

/** L1/L2 LLM 检查的结果。 */
export interface ValidationLLMResult {
  ok: boolean;
  confidence: number;                // LLM 自评 0-1
  reason: string;                    // 一两句人话解释
  conflicts_with?: string[];         // 与哪些规则 id 矛盾
}

/** L0 输入（无 LLM，纯机械）。 */
export interface ValidateL0Input {
  entry: Partial<KnowledgeEntry>;
  /** 规则来源的源文（diff / transcript / 原 PR 评论等），L0 用它验证 wrong_pattern 确实存在。 */
  sourceText: string;
  /** 项目已有规则（用于 trigger 字面冲突检查）。 */
  existingRules: Pick<KnowledgeEntry, "id" | "trigger" | "wrong_pattern">[];
  /** 项目探测到的 stack（file_types 一致性检查）。 */
  projectStack: string[];            // e.g. ["ts", "tsx", "md"]
  /** 工作区根目录，用于校验 import_path / scope.paths 存在性（L0 只看字符串合法性，不真的 stat）。 */
  workspaceRoot?: string;
}

/** L1 输入（Haiku）。 */
export interface ValidateL1Input {
  entry: KnowledgeEntry;
  /** 近似规则 top-k（embedding 召回），用于矛盾检测。 */
  similarRules: KnowledgeEntry[];
}

/** L2 输入（Sonnet）。 */
export interface ValidateL2Input {
  entry: KnowledgeEntry;
  /** 最近 20 次命中的真实 tool_input 样本，用于过拟合检测。 */
  recentHits: { tool_input: unknown; timestamp: string }[];
  /** 项目已有 stable+ 规则。 */
  existingSeniorRules: KnowledgeEntry[];
}

/** Validator 统一接口。三级之中任意级都可不实现（返回 { ok: true, ... }）以便降级使用。 */
export interface Validator {
  validateLevel0(input: ValidateL0Input): ValidationL0Result;
  validateLevel1(
    input: ValidateL1Input,
    callLLM: (prompt: string) => Promise<string>,
  ): Promise<ValidationLLMResult>;
  validateLevel2(
    input: ValidateL2Input,
    callLLM: (prompt: string) => Promise<string>,
  ): Promise<ValidationLLMResult>;
}
```

- [ ] **Step 2: 在 `packages/ports/src/index.ts` 导出**

```typescript
export type {
  Validator,
  ValidateL0Input,
  ValidateL1Input,
  ValidateL2Input,
  ValidationL0Result,
  ValidationLLMResult,
} from "./validator.js";
```

- [ ] **Step 3: typecheck**

Run: `pnpm --filter @teamagent/ports typecheck`
Expected: 无错。

- [ ] **Step 4: Commit**

```bash
git add packages/ports/src/validator.ts packages/ports/src/index.ts
git commit -m "feat(m2.3): Task 1 — 定义 Validator port 接口 (L0/L1/L2)"
```

---

## Task 2: Validator 契约测试套件

**目标：** 所有 Validator 实现必须满足的不变量。具体 LLM prompt 不入契约（实现细节）。

**Files:**
- Create: `packages/ports/src/__tests__/validator-contract.ts`
- Modify: `packages/ports/src/contracts.ts`

- [ ] **Step 1: 新建 `packages/ports/src/__tests__/validator-contract.ts`**

```typescript
import { describe, it, expect } from "vitest";
import type { Validator } from "../validator.js";
import type { KnowledgeEntry } from "@teamagent/types";

/**
 * Validator 契约不变量：
 *
 * L0:
 *  1. 5 项机械检查全绿 → ok=true, failed_checks 为空
 *  2. wrong_pattern 完全不在 sourceText 里 → ok=false, failed_checks 含 "wrong_pattern_not_in_source"
 *  3. scope.paths 空 + type=avoidance → ok=false, failed_checks 含 "scope_paths_empty"
 *  4. existingRules 里有相同 trigger → ok=false, failed_checks 含 "trigger_collision"
 *  5. 纯函数：相同输入 → 相同输出
 *
 * L1/L2:
 *  6. confidence ∈ [0, 1]
 *  7. ok=false 时必带 reason
 *  8. LLM 返回 garbage → ok=false（不抛异常）
 */
export function runValidatorContract(make: () => Validator): void {
  describe("Validator contract", () => {
    const baseEntry: KnowledgeEntry = {
      id: "r1",
      scope: { level: "team", paths: ["src/**"] },
      category: "E",
      tags: [],
      type: "avoidance",
      nature: "subjective",
      trigger: "use-fetch-not-axios",
      wrong_pattern: "axios",
      correct_pattern: "fetch",
      reasoning: "r",
      confidence: 0.5,
      enforcement: "suggest",
      status: "active",
      hit_count: 0,
      success_count: 0,
      override_count: 0,
      evidence: { success_sessions: 0, success_users: 0, correction_sessions: 0 },
      created_at: "2026-04-16T00:00:00Z",
      last_hit_at: "",
      last_validated_at: "2026-04-16T00:00:00Z",
      source: "accumulated",
      conflict_with: [],
      current_tier: "experimental",
      max_tier_ever: "experimental",
      tier_entered_at: "2026-04-16T00:00:00Z",
      demerit: 0,
      demerit_last_updated: "",
      resurrect_count: 0,
    };

    describe("L0", () => {
      it("all 5 mechanical checks pass → ok=true", () => {
        const r = make().validateLevel0({
          entry: baseEntry,
          sourceText: "import axios from 'axios';",
          existingRules: [],
          projectStack: ["ts"],
        });
        expect(r.ok).toBe(true);
        expect(r.failed_checks).toEqual([]);
      });

      it("wrong_pattern absent in source → fail", () => {
        const r = make().validateLevel0({
          entry: baseEntry,
          sourceText: "console.log('hi');",
          existingRules: [],
          projectStack: ["ts"],
        });
        expect(r.ok).toBe(false);
        expect(r.failed_checks).toContain("wrong_pattern_not_in_source");
      });

      it("avoidance with empty scope.paths → fail", () => {
        const r = make().validateLevel0({
          entry: { ...baseEntry, scope: { level: "team", paths: [] } },
          sourceText: "import axios from 'axios';",
          existingRules: [],
          projectStack: ["ts"],
        });
        expect(r.ok).toBe(false);
        expect(r.failed_checks).toContain("scope_paths_empty");
      });

      it("trigger collision with existing rule → fail", () => {
        const r = make().validateLevel0({
          entry: baseEntry,
          sourceText: "import axios from 'axios';",
          existingRules: [
            { id: "r-old", trigger: "use-fetch-not-axios", wrong_pattern: "axios" },
          ],
          projectStack: ["ts"],
        });
        expect(r.ok).toBe(false);
        expect(r.failed_checks).toContain("trigger_collision");
      });

      it("is pure (same input → same output)", () => {
        const input = {
          entry: baseEntry,
          sourceText: "import axios from 'axios';",
          existingRules: [],
          projectStack: ["ts"],
        };
        const a = make().validateLevel0(input);
        const b = make().validateLevel0(input);
        expect(a).toEqual(b);
      });
    });

    describe("L1/L2", () => {
      it("confidence within [0,1]", async () => {
        const stubLLM = async () =>
          JSON.stringify({ ok: true, confidence: 0.8, reason: "fine" });
        const r = await make().validateLevel1(
          { entry: baseEntry, similarRules: [] },
          stubLLM,
        );
        expect(r.confidence).toBeGreaterThanOrEqual(0);
        expect(r.confidence).toBeLessThanOrEqual(1);
      });

      it("ok=false requires reason", async () => {
        const stubLLM = async () =>
          JSON.stringify({ ok: false, confidence: 0.2, reason: "too broad" });
        const r = await make().validateLevel1(
          { entry: baseEntry, similarRules: [] },
          stubLLM,
        );
        if (!r.ok) expect(r.reason.length).toBeGreaterThan(0);
      });

      it("garbage LLM response → ok=false, no throw", async () => {
        const stubLLM = async () => "lol not json";
        const r = await make().validateLevel1(
          { entry: baseEntry, similarRules: [] },
          stubLLM,
        );
        expect(r.ok).toBe(false);
      });
    });
  });
}
```

- [ ] **Step 2: 在 `packages/ports/src/contracts.ts` 追加导出**

```typescript
export { runValidatorContract } from "./__tests__/validator-contract.js";
```

- [ ] **Step 3: typecheck**

Run: `pnpm --filter @teamagent/ports typecheck`

- [ ] **Step 4: Commit**

```bash
git add packages/ports/
git commit -m "feat(m2.3): Task 2 — Validator 契约测试套件"
```

---

## Task 3: 扩 `ExtractionInput.kind` + prompt 分派 + `KnowledgeEntry.source` 扩枚举

**目标：** 原 3 个 kind 不够覆盖 6 源。加 `insights / npm-audit / pr-review / git-hotspot / ci-failure`，prompt 按 kind 给不同 header。同时把 `KnowledgeEntry.source` 枚举加上 `"ingested"`，让 ingest-pipeline 新条目能带上该来源标记。

**Files:**
- Modify: `packages/ports/src/extractor.ts`
- Modify: `packages/core/src/extractor/prompt.ts`
- Modify: `packages/core/src/extractor/__tests__/prompt.test.ts`
- Modify: `packages/types/src/knowledge-entry.ts`（`source` enum 加 `"ingested"`）
- Modify: `packages/types/src/__tests__/knowledge-entry.test.ts`（加一条 accept "ingested"）

- [ ] **Step 1: 测试**

在 `prompt.test.ts` 追加：
```typescript
it.each([
  ["insights", "Claude Code /insights 报告"],
  ["npm-audit", "npm audit 输出"],
  ["pr-review", "PR review comment"],
  ["git-hotspot", "git log 热点文件"],
  ["ci-failure", "CI failure log"],
])("kind=%s → header mentions '%s'", (kind, fragment) => {
  const prompt = buildExtractionPrompt({
    kind: kind as any,
    context: "...",
    weight: 1.0,
  });
  expect(prompt).toContain(fragment);
});
```

- [ ] **Step 2: FAIL**

Run: `pnpm --filter @teamagent/core test -- prompt`

- [ ] **Step 3: 改 `packages/ports/src/extractor.ts`**

```typescript
export type ExtractionKind =
  | "correction"
  | "success"
  | "rule-text"
  | "insights"
  | "npm-audit"
  | "pr-review"
  | "git-hotspot"
  | "ci-failure";

export interface ExtractionInput {
  kind: ExtractionKind;
  context: string;
  weight: number;
}
```

- [ ] **Step 4: 改 `packages/core/src/extractor/prompt.ts`**

在 `buildHeader` 里扩展：
```typescript
function buildHeader(kind: ExtractionInput["kind"]): string {
  const source = {
    "correction": "用户在 Claude Code 会话中纠正了 AI 的行为",
    "success": "用户的一次成功模式（AI 未被纠正且模式被重复使用）",
    "rule-text": "一段已有的规则文本",
    "insights": "Claude Code /insights 报告",
    "npm-audit": "npm audit 输出",
    "pr-review": "PR review comment",
    "git-hotspot": "git log 热点文件（频繁被修改的路径）",
    "ci-failure": "CI failure log",
  }[kind];
  return `你是知识提取器。任务是把下面这段上下文（${source}）提炼成一条结构化的"知识条目"，供团队 AI 未来参考。`;
}
```

- [ ] **Step 5: 扩 `KnowledgeEntry.source` 枚举**

在 `packages/types/src/knowledge-entry.ts` 的 `source` zod enum 加 `"ingested"`；在 `knowledge-entry.test.ts` 加一条 `source: "ingested"` 能 parse 过的断言。

- [ ] **Step 6: PASS**

Run: `pnpm --filter @teamagent/core test -- prompt`
Run: `pnpm --filter @teamagent/types test -- knowledge-entry`
Run: `pnpm --filter @teamagent/ports typecheck`

- [ ] **Step 7: Commit**

```bash
git add packages/ports/src/extractor.ts packages/core/src/extractor/ packages/types/src/knowledge-entry.ts packages/types/src/__tests__/
git commit -m "feat(m2.3): Task 3 — ExtractionInput kind 扩至 8 类 + prompt 分派 + source enum 加 'ingested'"
```

---

## Task 4: 实现 L0 纯函数（5 项机械检查）

**目标：** 按 design §5.3 Level 0 的 5 条入库前机械检查。

**Files:**
- Create: `packages/core/src/validator/l0.ts`
- Create: `packages/core/src/validator/__tests__/l0.test.ts`

**5 项检查（design §5.3）：**
1. `wrong_pattern` 在源文（sourceText）里 grep 真存在
2. `correct_pattern.import_path` 合法（只校验字符串格式，不真的 stat —— L0 无 IO）
3. `scope.file_types` 与项目 stack 一致（交集非空）
4. 与现有规则 trigger 有字面冲突？
5. `scope.paths` 不为空（对 type=avoidance 的规则）且路径格式合法

**与 design 的偏离（需用户确认）：** 检查 1 和 5 在实现上**只对 `type=avoidance` 的规则强制**。success / wiki 类条目没有 `wrong_pattern`、也不强制要 `scope.paths`，对它们跑这两项会假阳性。design §5.3 没按 type 区分，这是实现层合理化。若用户希望 success/wiki 也走某种等价检查，本 plan 不做，留 M2.4+ 视需求。

- [ ] **Step 1: 写测试**

覆盖 5 项逐一 + 5 项全绿 + 组合失败（多项同时失败）。

```typescript
import { describe, it, expect } from "vitest";
import { validateLevel0 } from "../l0.js";

// ... 每项单独一个 describe，至少 2 case（绿 / 红）
```

- [ ] **Step 2: FAIL**

Run: `pnpm --filter @teamagent/core test -- l0`

- [ ] **Step 3: 实现 `packages/core/src/validator/l0.ts`**

```typescript
import type { ValidateL0Input, ValidationL0Result } from "@teamagent/ports";

/** L0 机械检查。纯函数，无 IO。见 design §5.3。 */
export function validateLevel0(input: ValidateL0Input): ValidationL0Result {
  const failed: string[] = [];
  const { entry, sourceText, existingRules, projectStack } = input;

  // 1. wrong_pattern 在源文里真存在
  if (entry.type === "avoidance" && entry.wrong_pattern) {
    const patterns = entry.wrong_pattern.split("|").map((s) => s.trim()).filter(Boolean);
    const hit = patterns.some((p) => sourceText.includes(p));
    if (!hit) failed.push("wrong_pattern_not_in_source");
  }

  // 2. import_path 字符串合法
  const importPath = (entry as any).correct_pattern_import_path;
  if (importPath !== undefined && !/^[@a-zA-Z0-9_\-./]+$/.test(importPath)) {
    failed.push("invalid_import_path_format");
  }

  // 3. scope.file_types 与 stack 一致
  const fileTypes = entry.scope?.file_types;
  if (fileTypes && fileTypes.length > 0 && projectStack.length > 0) {
    const norm = fileTypes.map((s) => s.replace(/^\*?\.?/, ""));
    const overlap = norm.some((t) => projectStack.includes(t));
    if (!overlap) failed.push("file_types_stack_mismatch");
  }

  // 4. trigger 字面冲突
  if (entry.trigger) {
    const exists = existingRules.some(
      (r) => r.id !== entry.id && r.trigger === entry.trigger,
    );
    if (exists) failed.push("trigger_collision");
  }

  // 5. avoidance 规则 scope.paths 不为空 且路径格式合法
  if (entry.type === "avoidance") {
    const paths = entry.scope?.paths ?? [];
    if (paths.length === 0) {
      failed.push("scope_paths_empty");
    } else {
      const illegal = paths.some((p) => typeof p !== "string" || p.length === 0);
      if (illegal) failed.push("scope_paths_malformed");
    }
  }

  return {
    ok: failed.length === 0,
    failed_checks: failed,
    notes: failed.length ? `L0 failed: ${failed.join(", ")}` : undefined,
  };
}
```

- [ ] **Step 4: PASS**

Run: `pnpm --filter @teamagent/core test -- l0`

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/validator/l0.ts packages/core/src/validator/__tests__/l0.test.ts
git commit -m "feat(m2.3): Task 4 — L0 纯函数（5 项机械检查）"
```

---

## Task 5: 把 L0 接入 `extract-pipeline`（入库门闸）

**目标：** `extract-pipeline` 在调用 `store.add` 前跑 L0，失败写 rejection log 不入库。

**Files:**
- Modify: `packages/core/src/pipeline/extract-pipeline.ts`
- Modify: `packages/core/src/pipeline/__tests__/extract-pipeline.test.ts`

- [ ] **Step 1: 测试：构造 3 条 extraction：合格 / wrong_pattern 不在源文 / trigger 冲突。期望只 1 条入库。**

```typescript
it("L0 gate rejects entries failing mechanical checks", async () => {
  // ... 构造 3 条纠正 + mock extractor 返回对应 entries
  // ... detector 返回 3 条 moments
  // ... run pipeline
  // ... expect store.count=1, result.rejected=2, rejectionLog 含 2 条
});
```

- [ ] **Step 2: FAIL**

- [ ] **Step 3: 改 pipeline**

- 依赖加 `validator: Validator`, `rejectionLog: (entry, reason) => void | Promise<void>`
- 在 `store.add` 前：
  ```typescript
  const l0 = deps.validator.validateLevel0({
    entry: completedEntry,
    sourceText: moment.context,
    existingRules: deps.store.getAll().map(({ id, trigger, wrong_pattern }) => ({ id, trigger, wrong_pattern })),
    projectStack: deps.projectStack ?? [],
  });
  if (!l0.ok) {
    rejected.push({ entry: completedEntry, reason: l0.failed_checks });
    deps.rejectionLog?.(completedEntry, l0);
    continue;
  }
  ```
- `ExtractPipelineResult` 加 `rejected: { entry, reason }[]`

- [ ] **Step 4: PASS + typecheck**

Run: `pnpm --filter @teamagent/core test -- extract-pipeline`
Run: `pnpm --filter @teamagent/core typecheck`

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/pipeline/extract-pipeline.ts packages/core/src/pipeline/__tests__/extract-pipeline.test.ts
git commit -m "feat(m2.3): Task 5 — L0 作为 extract-pipeline 入库门闸"
```

---

## Task 6: Source-agnostic `ingest-pipeline`

**目标：** 所有非 correction 源共用的通路：输入 `ExtractionInput[]` → extractor → L0 → store。

**Files:**
- Create: `packages/core/src/pipeline/ingest-pipeline.ts`
- Create: `packages/core/src/pipeline/__tests__/ingest-pipeline.test.ts`

- [ ] **Step 1: 测试**

```typescript
it("runs extractor + L0 + store.add for each ExtractionInput", async () => {
  // 构造 3 个 ExtractionInput（混合 kind）
  // mock extractor 返回 partials
  // run pipeline
  // expect store.count === 3（all pass L0）
});

it("respects dryRun (no writes)", async () => { /* ... */ });
it("emits 'ingest.accepted' / 'ingest.rejected' events via bus", async () => { /* ... */ });
```

- [ ] **Step 2: 实现 `packages/core/src/pipeline/ingest-pipeline.ts`**

```typescript
import type {
  AttributionBus,
  ExtractionInput,
  KnowledgeExtractor,
  KnowledgeStore,
  Validator,
} from "@teamagent/ports";
import type { KnowledgeEntry, Scope } from "@teamagent/types";
import { computeEnforcement } from "@teamagent/types";

export interface IngestPipelineDeps {
  inputs: ExtractionInput[];
  extractor: KnowledgeExtractor;
  callLLM: (prompt: string) => Promise<string>;
  validator: Validator;
  store: KnowledgeStore;
  bus?: AttributionBus;
  scope: Scope;
  source: KnowledgeEntry["source"];
  projectStack: string[];
  now: () => Date;
  idGen: () => string;
  dryRun?: boolean;
}

export interface IngestPipelineResult {
  scanned: number;
  accepted: KnowledgeEntry[];
  rejected: { partial: Partial<KnowledgeEntry>; reasons: string[] }[];
  skipped: number;   // LLM 返 null
  failed: number;
}

export async function runIngestPipeline(
  deps: IngestPipelineDeps,
): Promise<IngestPipelineResult> {
  const accepted: KnowledgeEntry[] = [];
  const rejected: { partial: Partial<KnowledgeEntry>; reasons: string[] }[] = [];
  let skipped = 0;
  let failed = 0;

  for (const input of deps.inputs) {
    let partial: Partial<KnowledgeEntry> | null = null;
    try {
      partial = await deps.extractor.extract(input, deps.callLLM);
    } catch {
      failed += 1;
      continue;
    }
    if (!partial) {
      skipped += 1;
      continue;
    }

    const entry: KnowledgeEntry = completeEntry(partial, deps);

    const l0 = deps.validator.validateLevel0({
      entry,
      sourceText: input.context,
      existingRules: deps.store.getAll().map(({ id, trigger, wrong_pattern }) => ({
        id, trigger, wrong_pattern,
      })),
      projectStack: deps.projectStack,
    });

    if (!l0.ok) {
      rejected.push({ partial: entry, reasons: l0.failed_checks });
      deps.bus?.emit({
        source: "ingest",
        action: "rejected_l0",
        target: { id: entry.id },
        severity: "info",
        userFacingValue: `rejected: ${l0.failed_checks.join(",")}`,
        timestamp: deps.now().toISOString(),
      });
      continue;
    }

    if (!deps.dryRun) deps.store.add(entry);
    accepted.push(entry);
    deps.bus?.emit({
      source: "ingest",
      action: "accepted",
      target: { id: entry.id },
      severity: "info",
      userFacingValue: `accepted: ${entry.trigger}`,
      timestamp: deps.now().toISOString(),
    });
  }

  return { scanned: deps.inputs.length, accepted, rejected, skipped, failed };
}

function completeEntry(
  partial: Partial<KnowledgeEntry>,
  deps: IngestPipelineDeps,
): KnowledgeEntry {
  const nowIso = deps.now().toISOString();
  const confidence = 0;
  return {
    id: partial.id ?? deps.idGen(),
    scope: partial.scope ?? deps.scope,
    category: partial.category ?? "E",
    tags: partial.tags ?? [],
    type: partial.type ?? "avoidance",
    nature: partial.nature ?? "subjective",
    trigger: partial.trigger ?? "",
    wrong_pattern: partial.wrong_pattern,
    correct_pattern: partial.correct_pattern ?? "",
    reasoning: partial.reasoning ?? "",
    confidence,
    enforcement: partial.enforcement ?? computeEnforcement(confidence),
    status: "active",
    hit_count: 0,
    success_count: 0,
    override_count: 0,
    evidence: { success_sessions: 0, success_users: 0, correction_sessions: 0 },
    created_at: nowIso,
    last_hit_at: "",
    last_validated_at: nowIso,
    source: deps.source,
    conflict_with: [],
    current_tier: "experimental",
    max_tier_ever: "experimental",
    tier_entered_at: nowIso,
    demerit: 0,
    demerit_last_updated: "",
    resurrect_count: 0,
  };
}
```

- [ ] **Step 3: PASS**

Run: `pnpm --filter @teamagent/core test -- ingest-pipeline`

- [ ] **Step 4: Commit**

```bash
git add packages/core/src/pipeline/ingest-pipeline.ts packages/core/src/pipeline/__tests__/ingest-pipeline.test.ts
git commit -m "feat(m2.3): Task 6 — source-agnostic ingest-pipeline"
```

---

## Task 7: `/insights` 源适配器 + CLI

**目标：** `teamagent ingest --from-insights <report.json>` 读取 Claude Code `/insights` 产生的报告，拆成 `ExtractionInput[]` 走 ingest-pipeline。

**Files:**
- Create: `packages/adapters/src/ingest/insights.ts`
- Modify: `packages/cli/src/commands/` 新建 `ingest.ts`
- Test: `packages/adapters/src/ingest/__tests__/insights.test.ts`

- [ ] **Step 1: 测试**

```typescript
it("parses insights JSON into ExtractionInput[]", () => {
  const report = {
    insights: [
      { type: "correction", text: "用 fetch 别用 axios", weight: 0.9 },
      { type: "pattern", text: "每次都在 config/ 下新建...", weight: 0.6 },
    ],
  };
  const inputs = parseInsightsReport(JSON.stringify(report));
  expect(inputs).toHaveLength(2);
  expect(inputs[0].kind).toBe("insights");
  expect(inputs[0].context).toContain("axios");
  expect(inputs[0].weight).toBe(0.9);
});
```

- [ ] **Step 2: 实现 `packages/adapters/src/ingest/insights.ts`**

```typescript
import type { ExtractionInput } from "@teamagent/ports";
import { z } from "zod";

const InsightItemSchema = z.object({
  type: z.string(),
  text: z.string(),
  weight: z.number().default(1),
});
const InsightsReportSchema = z.object({
  insights: z.array(InsightItemSchema),
});

export function parseInsightsReport(raw: string): ExtractionInput[] {
  const parsed = InsightsReportSchema.parse(JSON.parse(raw));
  return parsed.insights.map((i) => ({
    kind: "insights" as const,
    context: `[type=${i.type}] ${i.text}`,
    weight: i.weight,
  }));
}
```

- [ ] **Step 3: CLI 命令 `ingest.ts`**

```typescript
// 支持子命令: --from-insights <path> / --from-audit / --from-pr <n> / --from-git / --from-ci
export async function executeIngest(options: IngestOptions): Promise<void> {
  const source = detectSource(options);
  const inputs = await loadInputs(source, options);
  const result = await runIngestPipeline({
    inputs,
    extractor: llmBasedKnowledgeExtractor,
    callLLM: makeLLMClient(),
    validator: defaultValidator,
    store: makeStore(),
    projectStack: detectStack(process.cwd()),
    scope: { level: "personal" },
    source: "ingested",   // 新增 source 值，见 types 更新
    now: () => new Date(),
    idGen: () => nanoid(),
    dryRun: options.dryRun,
  });
  formatReport(result);
}
```

- [ ] **Step 4: PASS**

Run: `pnpm --filter @teamagent/adapters test -- insights`
Run: `pnpm --filter @teamagent/cli test -- ingest`

- [ ] **Step 5: Commit**

```bash
git add packages/adapters/src/ingest/insights.ts packages/cli/src/commands/ingest.ts
git add packages/adapters/src/ingest/__tests__/ packages/cli/src/commands/__tests__/
git commit -m "feat(m2.3): Task 7 — /insights 源适配器 + CLI ingest --from-insights"
```

---

## Task 8: npm audit 源适配器 + CLI

**目标：** `teamagent ingest --from-audit` 跑 `npm audit --json` → 把高危漏洞转成 `ExtractionInput[]`。

**Files:**
- Create: `packages/adapters/src/ingest/npm-audit.ts`
- Test: `packages/adapters/src/ingest/__tests__/npm-audit.test.ts`
- Modify: `packages/cli/src/commands/ingest.ts`

- [ ] **Step 1: 测试**

```typescript
it("parses npm audit JSON, filters high/critical, makes ExtractionInput[]", () => {
  const auditJson = JSON.stringify({
    vulnerabilities: {
      "lodash": {
        severity: "high",
        title: "Prototype Pollution in lodash",
        url: "https://github.com/advisories/...",
      },
      "moment": { severity: "low" }, // 忽略
    },
  });
  const inputs = parseNpmAudit(auditJson);
  expect(inputs).toHaveLength(1);
  expect(inputs[0].context).toContain("lodash");
  expect(inputs[0].kind).toBe("npm-audit");
});

it("getNpmAuditOutput invokes 'npm audit --json' via runner", async () => {
  const runner = vi.fn().mockResolvedValue('{"vulnerabilities":{}}');
  await getNpmAuditOutput(runner);
  expect(runner).toHaveBeenCalledWith("npm audit --json", expect.any(Object));
});
```

- [ ] **Step 2: 实现**

```typescript
export function parseNpmAudit(raw: string): ExtractionInput[] {
  const data = JSON.parse(raw);
  const vulns = data.vulnerabilities ?? {};
  const out: ExtractionInput[] = [];
  for (const [pkg, v] of Object.entries<any>(vulns)) {
    if (!["high", "critical"].includes(v.severity)) continue;
    out.push({
      kind: "npm-audit",
      context: `[severity=${v.severity}] ${pkg}: ${v.title ?? ""} (${v.url ?? "no url"})`,
      weight: v.severity === "critical" ? 1.0 : 0.8,
    });
  }
  return out;
}

export async function getNpmAuditOutput(
  runner: (cmd: string, opts: { cwd?: string }) => Promise<string>,
  cwd?: string,
): Promise<string> {
  return runner("npm audit --json", { cwd });
}
```

- [ ] **Step 3: CLI：注入 child_process runner，`--from-audit` 分支**

- [ ] **Step 4: PASS**

- [ ] **Step 5: Commit**

```bash
git add packages/adapters/src/ingest/ packages/cli/src/commands/
git commit -m "feat(m2.3): Task 8 — npm audit 源 + CLI ingest --from-audit"
```

---

## Task 9: PR review 源适配器 + CLI（gh CLI 桥接）

**目标：** `teamagent ingest --from-pr <n>` 通过 `gh pr view` 读评论 → 评论文本转 `ExtractionInput`。

**Files:**
- Create: `packages/adapters/src/ingest/pr-review.ts`
- Test: `packages/adapters/src/ingest/__tests__/pr-review.test.ts`
- Modify: `packages/cli/src/commands/ingest.ts`

- [ ] **Step 1: 测试**

```typescript
it("parses gh PR review JSON into ExtractionInput[]", () => {
  const ghOutput = JSON.stringify({
    reviews: [
      { body: "This is nitpick but prefer fetch over axios.", state: "COMMENTED" },
      { body: "Why did you use var here?", state: "CHANGES_REQUESTED" },
    ],
  });
  const inputs = parseGhPrReviews(ghOutput);
  expect(inputs).toHaveLength(2);
  expect(inputs[0].kind).toBe("pr-review");
  // CHANGES_REQUESTED 权重高于 COMMENTED
  expect(inputs[1].weight).toBeGreaterThan(inputs[0].weight);
});
```

- [ ] **Step 2: 实现**

```typescript
export function parseGhPrReviews(raw: string): ExtractionInput[] {
  const data = JSON.parse(raw);
  const reviews = data.reviews ?? [];
  return reviews
    .filter((r: any) => r.body?.trim().length > 10)
    .map((r: any) => ({
      kind: "pr-review" as const,
      context: `[state=${r.state}] ${r.body}`,
      weight: r.state === "CHANGES_REQUESTED" ? 0.9 : 0.5,
    }));
}

export async function getGhPrReviews(
  prNumber: number,
  runner: (cmd: string) => Promise<string>,
): Promise<string> {
  return runner(`gh pr view ${prNumber} --json reviews`);
}
```

- [ ] **Step 3: CLI 加 `--from-pr <n>` 分支，错误处理 `gh` 未安装**

若 `which gh` 失败 → 明确报错："gh CLI 未安装，参考 https://cli.github.com"。

- [ ] **Step 4: PASS**

- [ ] **Step 5: Commit**

```bash
git add packages/adapters/src/ingest/ packages/cli/src/commands/
git commit -m "feat(m2.3): Task 9 — PR review 源（gh CLI） + CLI ingest --from-pr"
```

---

## Task 10: git log hotspot（半自动 dry-run）

**目标：** `teamagent ingest --from-git --since=30d --dry-run` 找热点文件（频繁被修改）→ 输出候选 Markdown 让用户勾选。

**Files:**
- Create: `packages/adapters/src/ingest/git-hotspot.ts`
- Test: 同
- Modify: `packages/cli/src/commands/ingest.ts`

- [ ] **Step 1: 测试**

```typescript
it("aggregates git log --numstat to compute hotspots", () => {
  const logOutput = [
    "commit a", "2 1 src/utils/path.ts",
    "commit b", "5 3 src/utils/path.ts",
    "commit c", "1 0 src/foo.ts",
  ].join("\n");
  const hotspots = parseGitHotspots(logOutput, { threshold: 2 });
  expect(hotspots).toHaveLength(1); // 只有 path.ts 达阈值
  expect(hotspots[0].path).toBe("src/utils/path.ts");
  expect(hotspots[0].change_count).toBe(2);
});

it("formats hotspots as candidate markdown", () => {
  const md = formatCandidateMd([{ path: "src/x.ts", change_count: 5 }]);
  expect(md).toContain("- [ ]"); // 复选框
  expect(md).toContain("src/x.ts");
});
```

- [ ] **Step 2: 实现**

```typescript
export interface GitHotspot {
  path: string;
  change_count: number;
}

export function parseGitHotspots(
  logOutput: string,
  opts: { threshold: number },
): GitHotspot[] {
  const counts = new Map<string, number>();
  for (const line of logOutput.split("\n")) {
    const m = line.match(/^\s*\d+\s+\d+\s+(.+)$/);
    if (!m) continue;
    counts.set(m[1], (counts.get(m[1]) ?? 0) + 1);
  }
  return [...counts.entries()]
    .filter(([, c]) => c >= opts.threshold)
    .map(([path, c]) => ({ path, change_count: c }))
    .sort((a, b) => b.change_count - a.change_count);
}

export function formatCandidateMd(hotspots: GitHotspot[]): string {
  return [
    "# TeamAgent ingest candidates (git hotspot)",
    "",
    "勾选后运行:",
    "  teamagent ingest --from-candidates <this-file>",
    "",
    ...hotspots.map((h, i) =>
      `- [ ] Candidate ${i + 1}: \`${h.path}\` 被修改 ${h.change_count} 次`,
    ),
  ].join("\n");
}
```

- [ ] **Step 3: CLI 写 candidate 文件到 `.teamagent/candidates/git-hotspot-<date>.md`**

- [ ] **Step 4: `--from-candidates <path>` 读 md，只处理勾选项（`- [x]`）**

读 markdown，对每个勾选行 `- [x] ... \`path\`` 构造 `ExtractionInput { kind: "git-hotspot", context: <path + 文件内容预览 300 行>, weight: 0.5 }` 走 ingest-pipeline。

- [ ] **Step 5: PASS + commit**

```bash
git commit -m "feat(m2.3): Task 10 — git-hotspot 半自动源 + dry-run candidate md"
```

---

## Task 11: CI failure log 半自动源

**目标：** `teamagent ingest --from-ci --since=30d --dry-run` 通过 `gh run list --status=failure` 读近期失败，输出候选。

**Files:**
- Create: `packages/adapters/src/ingest/ci-failure.ts`
- Test: 同
- Modify: `packages/cli/src/commands/ingest.ts`

- [ ] **Step 1: 测试**

```typescript
it("parses gh run list JSON into candidate list", () => {
  const raw = JSON.stringify([
    { databaseId: 1, name: "CI", conclusion: "failure", headBranch: "feat/x", createdAt: "..." },
  ]);
  const cands = parseGhRunList(raw);
  expect(cands).toHaveLength(1);
});

it("fetches failed job log via gh run view --log-failed", async () => {
  const runner = vi.fn().mockResolvedValue("pnpm test FAIL\nExpected 'foo' ...");
  const log = await getRunFailedLog(1, runner);
  expect(runner).toHaveBeenCalledWith("gh run view 1 --log-failed");
  expect(log).toContain("FAIL");
});
```

- [ ] **Step 2: 实现 + CLI**

候选 markdown 格式同 git-hotspot。接受的候选每个 → 一条 `ExtractionInput { kind: "ci-failure", context: <log 片段 500 行>, weight: 0.5 }`。

- [ ] **Step 3: PASS + commit**

```bash
git commit -m "feat(m2.3): Task 11 — CI failure 半自动源 (gh run) + dry-run candidate md"
```

---

## Task 12: L1 Haiku Validator 实现

**目标：** 晋升 stable 前跑一次 Haiku 语义检查。

**Files:**
- Create: `packages/core/src/validator/l1.ts`
- Test: 同

- [ ] **Step 1: 测试（用 stub LLM 覆盖 ok/not-ok/garbage 3 路径）**

- [ ] **Step 2: 实现**

```typescript
import type {
  ValidateL1Input,
  ValidationLLMResult,
} from "@teamagent/ports";

export async function validateLevel1(
  input: ValidateL1Input,
  callLLM: (prompt: string) => Promise<string>,
): Promise<ValidationLLMResult> {
  const prompt = buildL1Prompt(input);
  let raw: string;
  try {
    raw = await callLLM(prompt);
  } catch (e) {
    return { ok: false, confidence: 0, reason: `llm_error: ${String(e)}` };
  }
  return parseLLMValidation(raw, input);
}

function buildL1Prompt(input: ValidateL1Input): string {
  return [
    "你是规则质量审查官（L1）。请判断这条规则是否足够 specific 且无矛盾。",
    "",
    "【待审规则】",
    JSON.stringify({
      trigger: input.entry.trigger,
      wrong_pattern: input.entry.wrong_pattern,
      correct_pattern: input.entry.correct_pattern,
      reasoning: input.entry.reasoning,
      scope: input.entry.scope,
    }, null, 2),
    "",
    "【近邻规则（embedding 召回）】",
    input.similarRules.map((r) => `- ${r.id}: ${r.trigger}`).join("\n") || "(无)",
    "",
    "返回严格 JSON:",
    '{"ok": true|false, "confidence": 0-1, "reason": "...", "conflicts_with": ["id1"]}',
  ].join("\n");
}

function parseLLMValidation(
  raw: string,
  input: ValidateL1Input,
): ValidationLLMResult {
  try {
    const obj = JSON.parse(raw.trim().replace(/^```json\s*|\s*```$/g, ""));
    const ok = typeof obj.ok === "boolean" ? obj.ok : false;
    const confidence = typeof obj.confidence === "number"
      ? Math.max(0, Math.min(1, obj.confidence))
      : 0;
    const reason = typeof obj.reason === "string" ? obj.reason : "no_reason";
    return {
      ok,
      confidence,
      reason,
      conflicts_with: Array.isArray(obj.conflicts_with) ? obj.conflicts_with : undefined,
    };
  } catch {
    return { ok: false, confidence: 0, reason: "llm_response_unparseable" };
  }
}
```

- [ ] **Step 3: PASS + commit**

```bash
git commit -m "feat(m2.3): Task 12 — L1 Haiku Validator 实现"
```

---

## Task 13: L2 Sonnet Validator 实现

**目标：** 晋升 canonical+ 前跑 Sonnet 深检。样本：近 20 次 hit 的 tool_input。

**Files:**
- Create: `packages/core/src/validator/l2.ts`
- Test: 同

- [ ] **Step 1 + 2: 结构同 L1，prompt 内容不同**

Prompt 重点：
- 列出近 20 次 tool_input 样本
- 让 Sonnet 判断"规则是否过拟合到特定项目"
- 让 Sonnet 对比 `existingSeniorRules` 判断是否冗余

- [ ] **Step 3: Commit**

```bash
git commit -m "feat(m2.3): Task 13 — L2 Sonnet Validator 实现"
```

---

## Task 14: 组装 `defaultValidator`

**目标：** 把 l0 / l1 / l2 合成一个 `Validator` 实现对象，通过契约测试。

**Files:**
- Create: `packages/core/src/validator/index.ts`
- Test: `packages/core/src/validator/__tests__/default-validator.test.ts`

- [ ] **Step 1: 测试 `runValidatorContract(() => defaultValidator)` 全绿**

- [ ] **Step 2: 实现**

```typescript
import type { Validator } from "@teamagent/ports";
import { validateLevel0 } from "./l0.js";
import { validateLevel1 } from "./l1.js";
import { validateLevel2 } from "./l2.js";

export const defaultValidator: Validator = {
  validateLevel0,
  validateLevel1,
  validateLevel2,
};
```

- [ ] **Step 3: Commit**

```bash
git commit -m "feat(m2.3): Task 14 — defaultValidator 组装"
```

---

## Task 15: L1/L2 接入 calibration-pipeline-v2（晋升门闸）

**目标：** 在 `runCalibrationPipelineV2` 里，当 hysteresis 批准晋升到 stable（或 canonical+）时，再跑 L1（或 L2）；不通过则回退候选 tier = 当前 tier。

**Files:**
- Modify: `packages/core/src/pipeline/calibration-pipeline-v2.ts`
- Modify: `packages/core/src/pipeline/__tests__/calibration-pipeline-v2.test.ts`

- [ ] **Step 1: 测试**

```typescript
it("promotion to stable blocked when L1 returns ok=false", async () => {
  const validator: Validator = {
    validateLevel0: () => ({ ok: true, failed_checks: [] }),
    validateLevel1: async () => ({ ok: false, confidence: 0.2, reason: "too broad" }),
    validateLevel2: async () => ({ ok: true, confidence: 0.9, reason: "good" }),
  };
  // ... 构造会晋升 probation→stable 的场景
  // ... 注入 validator + callLLM
  // ... 断言 tier_after === 'probation'（被 L1 blocked）
  // ... 断言 bus emit 'validator.blocked_promotion' 事件
});

it("promotion to canonical requires both L1 and L2 pass", async () => { /* ... */ });
```

- [ ] **Step 2: 改 pipeline**

**(a) 扩展 `CalibrationV2Deps`（新增 3 个可选字段，非破坏）：**

```typescript
export interface CalibrationV2Deps {
  // ... 原有字段不变 ...
  /** 可选：LLM 门闸。提供则晋升 stable/canonical/enforced 时会跑 L1/L2。 */
  validator?: Validator;
  callLLM?: (prompt: string) => Promise<string>;
  /** 可选：召回 top-k 近邻规则给 L1。缺省时 L1 的 similarRules=[]。 */
  similarityFinder?: (entry: KnowledgeEntry) => Promise<KnowledgeEntry[]>;
}
```

**(b) 新增内部辅助 `overrideTier`（在本文件内，不公开）：**

```typescript
function overrideTier(
  result: CalibrationResultV2,
  revertTo: Tier,
  reason: string,
): CalibrationResultV2 {
  return {
    ...result,
    tier_after: revertTo,
    tier_transition: null,
    reason_for_no_transition: reason,
    delta_breakdown: [
      ...result.delta_breakdown,
      { type: "tier_transition", note: `reverted to ${revertTo}: ${reason}` },
    ],
  };
}

function isPromotion(from: Tier, to: Tier): boolean {
  const rank = (t: Tier) => ["experimental","probation","stable","canonical","enforced"].indexOf(t);
  return rank(to) > rank(from);
}
```

**(c) 在 `runCalibrationPipelineV2` 的 calibrate 返回后、写 store 前插入门闸：**

```typescript
let result = deps.calibrator.calibrate(entry, { events: evtForEntry, observations: obsForEntry, now });

if (
  deps.validator && deps.callLLM &&
  result.tier_transition && isPromotion(result.tier_before, result.tier_after)
) {
  // L1 门（晋升到 stable / canonical / enforced 都要跑；canonical+ 还要额外跑 L2）
  const needsL1 = ["stable", "canonical", "enforced"].includes(result.tier_after);
  if (needsL1) {
    const similar = await (deps.similarityFinder?.(entry) ?? Promise.resolve([]));
    const l1 = await deps.validator.validateLevel1({ entry, similarRules: similar }, deps.callLLM);
    if (!l1.ok) {
      result = overrideTier(result, entry.current_tier as Tier, `l1_blocked: ${l1.reason}`);
      deps.bus?.emit({
        source: "validator",
        action: "blocked_promotion",
        target: { id: entry.id },
        severity: "info",
        userFacingValue: `L1 blocked ${entry.current_tier} → ${result.tier_before}: ${l1.reason}`,
        timestamp: now.toISOString(),
      });
    }
  }
  // L2 门（只在晋升 canonical / enforced 时跑）
  const needsL2 = ["canonical", "enforced"].includes(result.tier_after) && result.tier_transition;
  if (needsL2) {
    const recentHits = deps.events
      .filter((e) => e.knowledge_id === entry.id && e.kind === "hook-pre.matched")
      .slice(-20)
      .map((e) => ({ tool_input: (e.payload as any)?.tool_input ?? null, timestamp: e.timestamp }));
    const seniors = deps.store.getAll().filter((r) =>
      ["canonical", "enforced"].includes(r.current_tier) && r.id !== entry.id,
    );
    const l2 = await deps.validator.validateLevel2({ entry, recentHits, existingSeniorRules: seniors }, deps.callLLM);
    if (!l2.ok) {
      result = overrideTier(result, entry.current_tier as Tier, `l2_blocked: ${l2.reason}`);
      deps.bus?.emit({
        source: "validator",
        action: "blocked_promotion",
        target: { id: entry.id },
        severity: "info",
        userFacingValue: `L2 blocked: ${l2.reason}`,
        timestamp: now.toISOString(),
      });
    }
  }
}

// 继续原有写 store 逻辑（用最终的 result）
```

**(d) `tierPromoted` / `isPromotion` 只看非 dormant 晋升**：demerit 导致的降级或 dormant 一律不触发 validator（保守策略：validator 只把关升，不把关降）。

**注意：** 现有 M2.2 的 `runCalibrationPipelineV2` 签名是同步的（`async`）。加 await validator 调用后依旧 async，不破坏外部调用。

- [ ] **Step 3: PASS**

Run: `pnpm --filter @teamagent/core test -- calibration-pipeline-v2`

- [ ] **Step 4: Commit**

```bash
git add packages/core/src/pipeline/calibration-pipeline-v2.ts packages/core/src/pipeline/__tests__/calibration-pipeline-v2.test.ts
git commit -m "feat(m2.3): Task 15 — L1/L2 晋升门闸接入 calibration-pipeline-v2"
```

---

## Task 16: Dogfood —— skeleton-demo 展示 M2.3 新能力

**目标：** Walking Skeleton 不断裂 + CLI 展示 validator 拒绝 / 多源摄入样例。

**Files:**
- Modify: `packages/cli/src/commands/skeleton-demo.ts`（添加一个 "ingest 演示" 步骤）

- [ ] **Step 1: 全测试绿**

Run: `pnpm test`
Run: `pnpm typecheck`

- [ ] **Step 2: 手动 e2e**

```bash
# 准备临时工作区
export T=$(mktemp -d)
cd $T && pnpm teamagent init

# 构造假 /insights 报告
echo '{"insights":[{"type":"correction","text":"用 fetch 别用 axios","weight":0.9}]}' > insights.json
pnpm teamagent ingest --from-insights insights.json

# 跑 audit（若 workspace 有 package.json，真跑；否则跳过）
pnpm teamagent ingest --from-audit --dry-run

# 半自动 git hotspot
pnpm teamagent ingest --from-git --since=30d --dry-run
# 看到 .teamagent/candidates/git-hotspot-<date>.md 已生成

# 跑 calibrate 触发 L1/L2 门
pnpm teamagent calibrate

# stats --explain 看看 validator 拒绝事件
pnpm teamagent stats --explain <some-id>
```

Expected:
- ingest 报告打印 `accepted=X rejected=Y skipped=Z`
- candidate md 文件存在且含 `- [ ]`
- calibrate 输出里出现 `validator_blocked` 或 `promoted_via_l1` 字样

- [ ] **Step 3: 小修（若暴露问题）**

```bash
git commit -m "fix(m2.3): Task 16 — dogfood 中暴露的小问题"
```

---

## Task 17: 登记 M2.3 dogfood pitfalls + 更新 CLAUDE.md

- [ ] **Step 1: 用 `teamagent pitfall` 登记 1-3 条**

示例候选：
- 半自动 CLI 的 candidate md 格式若用 `- [ ]` 则要支持 GitHub markdown checkbox，并严格区分勾选/未勾选
- `gh` CLI 未装的场景必须给明确报错，不要让 child_process 退出码 127 裸泄到用户
- npm audit 在 monorepo 子包里行为不一致（根目录 / 子包 run 结果不同），默认应跑在根

- [ ] **Step 2: 重编译 CLAUDE.md**

Run: `pnpm teamagent compile`

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md .teamagent/
git commit -m "docs(m2.3): Task 17 — 登记 M2.3 dogfood pitfalls"
```

---

## Task 18: Milestone close-out

- [ ] **Step 1: 写完成状态**

在本文件顶部改：
```markdown
## Status: COMPLETED <YYYY-MM-DD>

- 18 tasks merged to master
- 所有测试绿（`pnpm test`）、typecheck 绿（`pnpm typecheck`）
- 6 源摄入可用: /insights / npm audit / PR / git hotspot / CI failure（+ Detector 原有）
- L0/L1/L2 三级 validator 落地，L0 作为入库门、L1/L2 作为晋升门
- Dogfood 验证：`ingest --from-*` 各命令 + `stats --explain` + `calibrate` 显示 validator 事件
```

- [ ] **Step 2: 最终 commit**

```bash
git add docs/superpowers/plans/2026-04-16-m2.3-multi-source-ingestion-and-validator.md
git commit -m "docs(m2.3): Milestone close-out — Multi-source ingestion + Validator landed"
```

- [ ] **Step 3: 告知用户**

输出摘要：新增源的采纳率、L0 拒绝率、L1/L2 阻挡的晋升数、规则数增量。

---

## Self-Review Checklist

| 检查项 | 来源 | 覆盖 Task |
|--------|------|----------|
| §5.3 Level 0 的 5 项机械检查 | design §5.3 | T4 |
| §5.3 Level 1 Haiku（晋升 stable 时） | design §5.3 | T12 + T15 |
| §5.3 Level 2 Sonnet（晋升 canonical+ 时） | design §5.3 | T13 + T15 |
| §5.3 成本估计（Level 0 免费 / 7% L1 / 3% L2） | design §5.3 | 自然满足（只在晋升时调用） |
| §5.5 自动 4 源 | design §5.5 | T7 (/insights) + T8 (audit) + T9 (PR) + 原 Detector |
| §5.5 半自动 2 源（git / CI，dry-run 候选） | design §5.5 | T10 + T11 |
| §5.5 走共同管道（Extractor + Validator L0） | design §5.5 | T6 (ingest-pipeline) |
| Port 先契约后实现（M0 元约束） | CLAUDE.md | T1 + T2 + T14 |
| Functional Core（core 无 IO） | CLAUDE.md | T4/T6/T12/T13 均纯函数 + LLM 注入 |
| 归因事件走 AttributionBus | CLAUDE.md | T5/T6/T15 emit 事件 |
| Walking Skeleton 不断裂 | CLAUDE.md | T16 skeleton-demo |

**刻意延后到后续里程碑：**
- `teamagent revive <id>` / `teamagent delete <id>` CLI（M2.2 已声明延后；M2.3 不接，等 M2.4 或独立小项）
- 规则冲突自动检测 LLM 判定（§5.1 项 9，留 M2.4+）
- CEL `when_expression`（§5.1 项 10，留 M2.5+）
- correct_pattern 子字段 code_example / import_path / tldr 的 schema 扩展（§5.1 项 8，可在 M2.3 结束后作为 patch-level 独立工作）
- sqlite-vec 相似规则召回（L1 需要 `similarityFinder`，M2.3 先用简单的 trigger 前缀匹配替代，待 SP-3 Wiki 引入 sqlite-vec 后复用）

**假设与风险：**
1. **Haiku 的成本估计（~$0.001/次）基于 2026-04 pricing**。若 Haiku API 被淘汰，换 Haiku 4.5 等价品。每次晋升 stable 仅触发一次 → 月均调用数应在 2 位数，成本可忽略。
2. **`gh` CLI 未装**：CLI 层做 precheck + 清晰报错；不硬依赖。
3. **npm audit 在零依赖 workspace 报 `{}`**：合法零结果，ingest result 标记 `scanned=0 accepted=0`。
4. **sqlite-vec 暂不可得**：T12 L1 的 `similarRules` 先用同 `trigger` 前缀 / 同 `category` 的规则替代（粗召回）。行为降级但不阻塞 milestone。
5. **Validator L1/L2 误判反噬**：设计上 L1/L2 失败只阻晋升不降级；若发现 L1 持续 ok=false 卡在 probation 的规则，通过 `teamagent stats --stuck-in-promotion` （M2.4 新命令）给用户一个出口。**M2.3 不引入这个命令**，但记下需求。

---

## 待用户确认点

1. 6 个源的选择（/insights / npm audit / PR review / git hotspot / CI failure + Detector 原有）是否都值得做？某些源（如 CI failure）若你的项目 CI 信号太少，可以砍掉改做其它。
2. 半自动源的 candidate 交付格式选的是**Markdown + checkbox**，比交互式 TUI 轻但需手动编辑。接受否？
3. L1/L2 blocked 只回退候选 tier，不反向扣 demerit —— 避免 Haiku 的误判伤到规则。你认为这个保守策略对？还是认为 L1 blocked 应该 +1 demerit 作为"需要人工 review 的信号"？
4. `gh` CLI 桥接 vs Octokit/HTTP —— 选 `gh` 的理由是项目已在用。若你计划做 CI 环境跑 M2.3，`gh` 可能不可得，需切 Octokit。确认当前只在本地 dogfood 即用 `gh` 合理？
5. 本 plan 里的 Task 粒度与 M2.2 一致（18 task ≈ M2.2 的 17 task）。太粗或太细请指出。
