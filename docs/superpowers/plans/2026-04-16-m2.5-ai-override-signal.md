# M2.5 AI Override Signal 闭环 Implementation Plan

## Status: COMPLETED 2026-04-16

- 9 tasks merged to feat/m2.5-ai-override-signal
- 所有测试绿（715 pass，Windows SQLite EBUSY 预存在 2 条不计入）、typecheck 绿
- PostToolUse 检测 ignored → ai.override.ignored event → demerit 上升
- PreToolUse clean pass 检测 complied → ai.override.complied event → 合成 Observation(success) → confidence 上升
- `teamagent stats --override-signals` 可查每条规则的遵守/违反计数
- Hook bundle 已重建

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 PreToolUse/PostToolUse Hook 里检测 AI 是否遵守了警告，把 `ai.override.ignored`（违反）和 `ai.override.complied`（遵守）信号喂给 Calibrator v2，让规则置信度/惩罚度根据真实 AI 行为自动更新。

**Architecture:**
- **PostToolUse** 检测 `ignored`：同一 `tool_use_id` 存在 `hook-pre.warned` + 工具实际执行了 → AI 无视警告 → emit `ai.override.ignored` event → Calibrator demerit 上升。
- **PreToolUse** 检测 `complied`：当前工具调用无匹配 + 近期（5 min 窗口）对同 `tool_name` 有 `hook-pre.warned` → AI 改了做法 → emit `ai.override.complied` event → Calibrator 合成 Observation(success)。
- **Functional Core 不破**：`detectIgnoredSignals` / `detectCompliedSignals` 都是纯函数，IO 在 adapter/bin 层。

**Tech Stack:** TypeScript、Vitest、pnpm workspace。无新依赖。

**参考文档：**
- `docs/superpowers/specs/2026-04-15-phase2-design-v2.md` §5.4（AI Override Signal 闭环伪代码）
- `packages/core/src/calibrator/v2/index.ts`：`DEMERIT_KIND_TO_SOURCE` 已包含 `"ai.override.ignored"` → `"ai_override_ignored"`（demerit 处理已就绪，只缺 emit）
- `packages/types/src/persisted-event.ts`：`kind` union 尚未包含 `ai.override.*`

**前置已落地：** M2.4（MarkdownCompiler / SkillCompiler / runCompile / stats --stuck-in-promotion）

**关键设计决策：**

1. **`ai.override.ignored`** 在 PostToolUse 检测：PostToolUse 触发 ↔ 工具已执行。存在 `hook-pre.warned`（not `hook-pre.blocked`） + 同 `tool_use_id` = AI 被警告且仍执行了原始内容 → `ignored` 信号。`block` tier 的规则触发 `hook-pre.blocked`，PostToolUse 不会被调用（Claude Code 协议：deny 时不执行工具），故无需特殊处理。
2. **`ai.override.complied`** 在 PreToolUse 检测：当前调用无匹配（clean pass）时，在最近 50 个事件里找 5 分钟内对同 `tool_name` 的 `hook-pre.warned` 事件 → 推断 AI 改了做法。这是启发式（heuristic），会有偶发 false positive，但对 Calibrator 的影响是信号中的正偏，可接受。
3. **tool_name 存在 warned 事件里**：`hook-pre.warned` 新增 `tool_name` 字段（走 `payload` 序列化，向后兼容）。
4. **`ai.override.complied` → Observation 合成**：在 `runCalibrationPipelineV2` 把 events 里的 `ai.override.complied` 转成内存中的 `Observation { outcome: "success" }`，追加到 `deps.observations` 后传给 calibrator。不引入新 IO，不写入 observations 表（那是后续 T15-class 任务）。
5. **Calibrator 已就绪**：`DEMERIT_KIND_TO_SOURCE["ai.override.ignored"] = "ai_override_ignored"` 已在 index.ts，只缺 `kind` 类型补全 + 实际 emit。

---

## 文件变更清单

| 操作 | 文件 | 原因 |
|------|------|------|
| Modify | `packages/types/src/persisted-event.ts` | kind union 加 `ai.override.ignored` / `ai.override.complied` |
| Create | `packages/core/src/pipeline/override-signal.ts` | 纯函数：detectIgnoredSignals / detectCompliedSignals |
| Create | `packages/core/src/pipeline/__tests__/override-signal.test.ts` | 契约测试 |
| Modify | `packages/core/src/index.ts` | re-export 新函数 |
| Modify | `packages/adapters/src/hook/claude-agent-sdk/pre-tool-use-sdk.ts` | 存 tool_name；clean pass 时 detect/emit complied |
| Modify | `packages/adapters/src/hook/claude-agent-sdk/__tests__/pre-tool-use-sdk.test.ts` | 更新测试 |
| Modify | `packages/adapters/src/hook/claude-agent-sdk/post-tool-use-sdk.ts` | detect/emit ignored |
| Modify | `packages/adapters/src/hook/claude-agent-sdk/__tests__/post-tool-use-sdk.test.ts` | 更新测试 |
| Modify | `packages/core/src/pipeline/calibration-pipeline-v2.ts` | 合成 Observations from complied events |
| Modify | `packages/core/src/pipeline/__tests__/calibration-pipeline-v2.test.ts` | 测试合成逻辑 |
| Modify | `packages/cli/src/commands/stats.ts` | `--override-signals` flag |
| Modify | `packages/cli/src/__tests__/stats.test.ts` | 测试新 flag |
| Modify | `packages/cli/src/bin.ts` | parse `--override-signals` |

---

## Task 1: `PersistedEvent.kind` 扩展

**目标：** 给 `kind` union 加 `"ai.override.ignored"` 和 `"ai.override.complied"`，解锁 Calibrator 里已有的 `DEMERIT_KIND_TO_SOURCE["ai.override.ignored"]` 映射。

**Files:**
- Modify: `packages/types/src/persisted-event.ts`

- [ ] **Step 1: 扩 kind union**

在 `persisted-event.ts` 里，找到 `kind:` union 字段，在 `"hook-post.result"` 之后加两个新 kind：

```typescript
  kind:
    | "hook-pre.matched"
    | "hook-pre.blocked"
    | "hook-pre.warned"
    | "hook-post.result"
    | "ai.override.ignored"    // 新增：AI 无视 warn 继续执行
    | "ai.override.complied"   // 新增：AI 接受 warn 改了做法
    | "pitfall.added"
    | "compiler.updated"
    | "extractor.extracted"
    | "calibrator.adjusted"
    | "init.completed"
    | "scenario.run";
```

- [ ] **Step 2: 确认 typecheck 绿**

```bash
pnpm --filter @teamagent/types typecheck
```

Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add packages/types/src/persisted-event.ts
git commit -m "feat(m2.5): Task 1 — PersistedEvent.kind 加 ai.override.ignored / complied"
```

---

## Task 2: Override signal 纯函数 + 契约测试

**目标：** `detectIgnoredSignals`（PostToolUse 用）和 `detectCompliedSignals`（PreToolUse 用）两个纯函数，核心逻辑可独立测试。

**Files:**
- Create: `packages/core/src/pipeline/override-signal.ts`
- Create: `packages/core/src/pipeline/__tests__/override-signal.test.ts`
- Modify: `packages/core/src/index.ts`

- [ ] **Step 1: 先写测试（红）**

创建 `packages/core/src/pipeline/__tests__/override-signal.test.ts`：

```typescript
import { describe, it, expect } from "vitest";
import { detectIgnoredSignals, detectCompliedSignals } from "../override-signal.js";

const BASE_TS = "2026-04-16T10:00:00.000Z";
const RECENT_TS = "2026-04-16T10:04:00.000Z"; // 4 min ago → within 5min window
const OLD_TS    = "2026-04-16T09:50:00.000Z"; // 10 min ago → outside window
const NOW = new Date("2026-04-16T10:05:00.000Z");

describe("detectIgnoredSignals", () => {
  it("returns knowledge_id when hook-pre.warned matches same tool_use_id", () => {
    const events = [
      { kind: "hook-pre.warned", tool_use_id: "t1", knowledge_id: "rule-A", timestamp: BASE_TS, tool_name: "Write" },
    ];
    expect(detectIgnoredSignals("t1", events)).toEqual([{ knowledge_id: "rule-A" }]);
  });

  it("returns empty when no warned event for tool_use_id", () => {
    const events = [
      { kind: "hook-pre.warned", tool_use_id: "t2", knowledge_id: "rule-A", timestamp: BASE_TS, tool_name: "Write" },
    ];
    expect(detectIgnoredSignals("t1", events)).toEqual([]);
  });

  it("returns empty when event is hook-pre.blocked (not warned)", () => {
    const events = [
      { kind: "hook-pre.blocked", tool_use_id: "t1", knowledge_id: "rule-A", timestamp: BASE_TS, tool_name: "Write" },
    ];
    expect(detectIgnoredSignals("t1", events)).toEqual([]);
  });

  it("ignores events without knowledge_id", () => {
    const events = [
      { kind: "hook-pre.warned", tool_use_id: "t1", timestamp: BASE_TS, tool_name: "Write" },
    ];
    expect(detectIgnoredSignals("t1", events)).toEqual([]);
  });
});

describe("detectCompliedSignals", () => {
  it("returns knowledge_id when recent warned event exists for same tool_name", () => {
    const events = [
      { kind: "hook-pre.warned", tool_use_id: "t1", knowledge_id: "rule-A", timestamp: RECENT_TS, tool_name: "Write" },
    ];
    expect(detectCompliedSignals("Write", events, NOW)).toEqual([{ knowledge_id: "rule-A" }]);
  });

  it("returns empty when warned event is outside time window", () => {
    const events = [
      { kind: "hook-pre.warned", tool_use_id: "t1", knowledge_id: "rule-A", timestamp: OLD_TS, tool_name: "Write" },
    ];
    expect(detectCompliedSignals("Write", events, NOW)).toEqual([]);
  });

  it("returns empty when tool_name differs", () => {
    const events = [
      { kind: "hook-pre.warned", tool_use_id: "t1", knowledge_id: "rule-A", timestamp: RECENT_TS, tool_name: "Edit" },
    ];
    expect(detectCompliedSignals("Write", events, NOW)).toEqual([]);
  });

  it("deduplicates same knowledge_id from multiple warned events", () => {
    const events = [
      { kind: "hook-pre.warned", tool_use_id: "t1", knowledge_id: "rule-A", timestamp: RECENT_TS, tool_name: "Write" },
      { kind: "hook-pre.warned", tool_use_id: "t2", knowledge_id: "rule-A", timestamp: RECENT_TS, tool_name: "Write" },
    ];
    const result = detectCompliedSignals("Write", events, NOW);
    expect(result).toHaveLength(1);
    expect(result[0]).toEqual({ knowledge_id: "rule-A" });
  });

  it("respects custom windowMs", () => {
    const events = [
      { kind: "hook-pre.warned", tool_use_id: "t1", knowledge_id: "rule-A", timestamp: RECENT_TS, tool_name: "Write" },
    ];
    // window = 1ms → nothing qualifies
    expect(detectCompliedSignals("Write", events, NOW, 1)).toEqual([]);
  });
});
```

- [ ] **Step 2: 跑测试，确认红（模块不存在）**

```bash
pnpm --filter @teamagent/core test -- override-signal
```

Expected: test file errors with "Cannot find module".

- [ ] **Step 3: 实现纯函数**

创建 `packages/core/src/pipeline/override-signal.ts`：

```typescript
/** 供 detectIgnoredSignals / detectCompliedSignals 使用的最小事件结构。
 *  不依赖 PersistedEvent，让 core 保持无类型耦合。
 */
export interface OverrideSignalEvent {
  kind: string;
  tool_use_id?: string;
  knowledge_id?: string;
  timestamp: string;
  /** 存在于 hook-pre.warned 事件的 payload 里（M2.5 新增）。 */
  tool_name?: string;
}

/**
 * PostToolUse 用：当同一 tool_use_id 有 hook-pre.warned 事件时，
 * 说明 AI 被警告后仍执行了原始工具调用 → "ignored"。
 */
export function detectIgnoredSignals(
  currentToolUseId: string,
  recentEvents: OverrideSignalEvent[],
): Array<{ knowledge_id: string }> {
  return recentEvents
    .filter(
      (e) =>
        e.kind === "hook-pre.warned" &&
        e.tool_use_id === currentToolUseId &&
        Boolean(e.knowledge_id),
    )
    .map((e) => ({ knowledge_id: e.knowledge_id! }));
}

/**
 * PreToolUse 用（clean pass 时调用）：在近期 warned 事件里找同 tool_name 的，
 * 推断 AI 在下次调用同类工具时改掉了之前触发警告的内容 → "complied"。
 *
 * @param currentToolName  当前 clean pass 的工具名
 * @param recentEvents     事件日志最近 N 条（调用方传入，通常 50）
 * @param now              当前时间（纯函数，不内部 new Date()）
 * @param windowMs         时间窗口，默认 5 分钟
 */
export function detectCompliedSignals(
  currentToolName: string,
  recentEvents: OverrideSignalEvent[],
  now: Date,
  windowMs = 300_000,
): Array<{ knowledge_id: string }> {
  const cutoff = now.getTime() - windowMs;
  const seen = new Set<string>();
  const result: Array<{ knowledge_id: string }> = [];

  for (const e of recentEvents) {
    if (
      e.kind === "hook-pre.warned" &&
      e.knowledge_id &&
      e.tool_name === currentToolName &&
      new Date(e.timestamp).getTime() > cutoff &&
      !seen.has(e.knowledge_id)
    ) {
      seen.add(e.knowledge_id);
      result.push({ knowledge_id: e.knowledge_id });
    }
  }

  return result;
}
```

- [ ] **Step 4: 跑测试，确认绿**

```bash
pnpm --filter @teamagent/core test -- override-signal
```

Expected: all 8 tests pass.

- [ ] **Step 5: 把函数加入 core/index.ts**

在 `packages/core/src/index.ts` 末尾追加：

```typescript
export {
  detectIgnoredSignals,
  detectCompliedSignals,
  type OverrideSignalEvent,
} from "./pipeline/override-signal.js";
```

- [ ] **Step 6: typecheck**

```bash
pnpm --filter @teamagent/core typecheck
```

Expected: no errors.

- [ ] **Step 7: Commit**

```bash
git add packages/core/src/pipeline/override-signal.ts \
        packages/core/src/pipeline/__tests__/override-signal.test.ts \
        packages/core/src/index.ts
git commit -m "feat(m2.5): Task 2 — detectIgnoredSignals / detectCompliedSignals 纯函数"
```

---

## Task 3: PreToolUse — 存 tool_name + clean pass 检测 complied

**目标：**
1. `hook-pre.warned` 事件里新增 `tool_name` 字段（payload 序列化，向后兼容）。
2. clean pass（无匹配）时读近期事件 → 检测 complied → emit `ai.override.complied` 事件。

**Files:**
- Modify: `packages/adapters/src/hook/claude-agent-sdk/pre-tool-use-sdk.ts`
- Modify: `packages/adapters/src/hook/claude-agent-sdk/__tests__/pre-tool-use-sdk.test.ts`

- [ ] **Step 1: 先更新测试（红）**

在 `pre-tool-use-sdk.test.ts` 里，找到 "warn/suggest" 分支测试，检查 warned 事件要含 `tool_name`；并加一个 clean pass 后产出 complied 的测试：

```typescript
// 找到 "warn/suggest" 分支的 test，加 assertion：
expect(appended[0]).toMatchObject({
  kind: "hook-pre.warned",
  tool_name: "Write",       // 新增断言
  knowledge_id: expect.any(String),
});

// 新增 test：
it("emits ai.override.complied on clean pass after a recent warn for same tool_name", async () => {
  const recentEvents = [
    {
      kind: "hook-pre.warned",
      tool_use_id: "t-prev",
      knowledge_id: "rule-A",
      timestamp: new Date(Date.now() - 60_000).toISOString(), // 1 min ago
      tool_name: "Write",
    },
  ];
  const appended: any[] = [];
  const handler = createPreToolUseHandler({
    matcher: { match: async () => ({ matched: [] }) },  // no match
    eventLog: {
      append: (e: any) => appended.push(e),
      readLast: (_n: number) => recentEvents,
    },
  });
  await handler({ tool_name: "Write", tool_input: {}, tool_use_id: "t-new" });
  const complied = appended.filter((e: any) => e.kind === "ai.override.complied");
  expect(complied).toHaveLength(1);
  expect(complied[0]).toMatchObject({ knowledge_id: "rule-A" });
});

it("does NOT emit complied when no recent warns exist", async () => {
  const appended: any[] = [];
  const handler = createPreToolUseHandler({
    matcher: { match: async () => ({ matched: [] }) },
    eventLog: {
      append: (e: any) => appended.push(e),
      readLast: (_n: number) => [],
    },
  });
  await handler({ tool_name: "Write", tool_input: {}, tool_use_id: "t-new" });
  expect(appended.filter((e: any) => e.kind === "ai.override.complied")).toHaveLength(0);
});
```

- [ ] **Step 2: 跑测试，确认红**

```bash
pnpm --filter @teamagent/adapters test -- pre-tool-use-sdk
```

Expected: new tests fail ("readLast is not a function" or assertion mismatch).

- [ ] **Step 3: 更新 `PreToolUseDeps` 接口 + 实现**

完整替换 `pre-tool-use-sdk.ts`：

```typescript
import type { PreToolUseHookInput } from "@anthropic-ai/claude-agent-sdk";
import { detectCompliedSignals, type OverrideSignalEvent } from "@teamagent/core";

export interface PreToolUseDeps {
  matcher: {
    match(input: { tool_name: string; tool_input: unknown }): Promise<{ matched: any[] }>;
  };
  eventLog: {
    append(e: any): void;
    readLast(n: number): any[];
  };
}

export interface PreToolUseResult {
  permissionDecision: "allow" | "deny" | "ask";
  permissionDecisionReason?: string;
  systemMessage?: string;
}

export function createPreToolUseHandler(deps: PreToolUseDeps) {
  return async (input: PreToolUseHookInput): Promise<PreToolUseResult> => {
    const { tool_name, tool_input, tool_use_id } = input;
    const now = new Date().toISOString();

    const { matched } = await deps.matcher.match({ tool_name, tool_input });

    if (matched.length === 0) {
      // Clean pass — 检测 AI 是否遵守了近期对同 tool 的警告
      const recent = deps.eventLog.readLast(50) as OverrideSignalEvent[];
      const complied = detectCompliedSignals(tool_name, recent, new Date(now));
      for (const c of complied) {
        deps.eventLog.append({
          id: `e-complied-${tool_use_id}-${c.knowledge_id}`,
          kind: "ai.override.complied",
          knowledge_id: c.knowledge_id,
          tool_use_id,
          timestamp: now,
          schema_version: 1,
        });
      }
      deps.eventLog.append({
        id: `e-${tool_use_id}-passed`,
        kind: "hook-pre.passed",
        tool_use_id,
        timestamp: now,
        schema_version: 1,
      });
      return { permissionDecision: "allow" };
    }

    // 取 enforcement 最严格的那条
    const sorted = [...matched].sort(
      (a, b) => severityOrder(b.enforcement) - severityOrder(a.enforcement),
    );
    const top = sorted[0];
    const reason = formatReason(top);

    if (top.enforcement === "block") {
      deps.eventLog.append({
        id: `e-${tool_use_id}-blocked`,
        kind: "hook-pre.blocked",
        knowledge_id: top.id,
        tool_use_id,
        timestamp: now,
        schema_version: 1,
      });
      return { permissionDecision: "deny", permissionDecisionReason: reason };
    }

    // warn / suggest → allow + systemMessage hint
    deps.eventLog.append({
      id: `e-${tool_use_id}-warned`,
      kind: "hook-pre.warned",
      knowledge_id: top.id,
      tool_use_id,
      tool_name,              // M2.5: 供 detectCompliedSignals 用
      timestamp: now,
      schema_version: 1,
    });
    return { permissionDecision: "allow", systemMessage: reason };
  };
}

function severityOrder(e: string): number {
  return ({ block: 3, warn: 2, suggest: 1, passive: 0 } as Record<string, number>)[e] ?? 0;
}

function formatReason(rule: any): string {
  const tier = rule.current_tier ?? "unknown";
  const correct = rule.correct_pattern ?? "";
  const reasoning = rule.reasoning ?? "";
  return `[${tier}] ${rule.trigger ?? ""}: ${correct}\n理由: ${reasoning}`;
}
```

- [ ] **Step 4: 跑测试，确认绿**

```bash
pnpm --filter @teamagent/adapters test -- pre-tool-use-sdk
```

Expected: all tests pass.

- [ ] **Step 5: Commit**

```bash
git add packages/adapters/src/hook/claude-agent-sdk/pre-tool-use-sdk.ts \
        packages/adapters/src/hook/claude-agent-sdk/__tests__/pre-tool-use-sdk.test.ts
git commit -m "feat(m2.5): Task 3 — PreToolUse 存 tool_name + clean pass emit ai.override.complied"
```

---

## Task 4: PostToolUse — 检测并 emit ai.override.ignored

**目标：** 当 PostToolUse 找到同 `tool_use_id` 的 `hook-pre.warned` 事件时，追加 `ai.override.ignored` 事件。Calibrator 的 `DEMERIT_KIND_TO_SOURCE` 已就绪，只需 emit。

**Files:**
- Modify: `packages/adapters/src/hook/claude-agent-sdk/post-tool-use-sdk.ts`
- Modify: `packages/adapters/src/hook/claude-agent-sdk/__tests__/post-tool-use-sdk.test.ts`

- [ ] **Step 1: 先更新测试（红）**

在 `post-tool-use-sdk.test.ts` 加：

```typescript
import { detectIgnoredSignals } from "@teamagent/core";

it("emits ai.override.ignored when hook-pre.warned exists for same tool_use_id", async () => {
  const recentEvents = [
    {
      kind: "hook-pre.warned",
      tool_use_id: "t1",
      knowledge_id: "rule-A",
      timestamp: new Date().toISOString(),
    },
  ];
  const appended: any[] = [];
  const handler = createPostToolUseHandler({
    eventLog: {
      append: (e: any) => appended.push(e),
      readLast: (_n: number) => recentEvents,
    },
  });
  await handler({ tool_use_id: "t1", tool_response: { is_error: false } } as any);
  const ignored = appended.filter((e: any) => e.kind === "ai.override.ignored");
  expect(ignored).toHaveLength(1);
  expect(ignored[0]).toMatchObject({ knowledge_id: "rule-A", tool_use_id: "t1" });
});

it("does NOT emit ai.override.ignored when only hook-pre.blocked exists (tool did not execute)", async () => {
  const recentEvents = [
    {
      kind: "hook-pre.blocked",
      tool_use_id: "t1",
      knowledge_id: "rule-A",
      timestamp: new Date().toISOString(),
    },
  ];
  const appended: any[] = [];
  const handler = createPostToolUseHandler({
    eventLog: {
      append: (e: any) => appended.push(e),
      readLast: (_n: number) => recentEvents,
    },
  });
  await handler({ tool_use_id: "t1", tool_response: {} } as any);
  expect(appended.filter((e: any) => e.kind === "ai.override.ignored")).toHaveLength(0);
});
```

- [ ] **Step 2: 跑测试，确认红**

```bash
pnpm --filter @teamagent/adapters test -- post-tool-use-sdk
```

Expected: new tests fail.

- [ ] **Step 3: 更新 `post-tool-use-sdk.ts`**

```typescript
import type { PostToolUseHookInput } from "@anthropic-ai/claude-agent-sdk";
import { detectIgnoredSignals, type OverrideSignalEvent } from "@teamagent/core";

export interface PostToolUseDeps {
  eventLog: {
    append(e: any): void;
    readLast(n: number): any[];
  };
}

export function createPostToolUseHandler(deps: PostToolUseDeps) {
  return async (input: PostToolUseHookInput): Promise<Record<string, never>> => {
    const { tool_use_id, tool_response } = input;
    const now = new Date().toISOString();
    const success = inferToolSuccess(tool_response);

    const recent = deps.eventLog.readLast(50);

    // 原有逻辑：找 pre 事件 → emit hook-post.result
    const preEvents = recent.filter(
      (e: any) => e.tool_use_id === tool_use_id && e.kind.startsWith("hook-pre."),
    );
    for (const pre of preEvents) {
      if (!pre.knowledge_id) continue;
      deps.eventLog.append({
        id: `e-post-${tool_use_id}-${pre.knowledge_id}`,
        kind: "hook-post.result",
        knowledge_id: pre.knowledge_id,
        tool_use_id,
        timestamp: now,
        schema_version: 1,
        payload: { success, source_pre_kind: pre.kind },
      });
    }

    // M2.5: detect ignored signals
    const ignoredList = detectIgnoredSignals(tool_use_id, recent as OverrideSignalEvent[]);
    for (const ig of ignoredList) {
      deps.eventLog.append({
        id: `e-override-ignored-${tool_use_id}-${ig.knowledge_id}`,
        kind: "ai.override.ignored",
        knowledge_id: ig.knowledge_id,
        tool_use_id,
        timestamp: now,
        schema_version: 1,
      });
    }

    return {};
  };
}

export function inferToolSuccess(toolResponse: unknown): boolean {
  if (toolResponse === null || toolResponse === undefined) return true;
  if (typeof toolResponse !== "object") return true;
  const r = toolResponse as Record<string, unknown>;
  if (r.is_error === true) return false;
  if (r.error) return false;
  if (typeof r.exit_code === "number" && r.exit_code !== 0) return false;
  return true;
}
```

- [ ] **Step 4: 跑测试，确认绿**

```bash
pnpm --filter @teamagent/adapters test -- post-tool-use-sdk
```

Expected: all tests pass.

- [ ] **Step 5: Commit**

```bash
git add packages/adapters/src/hook/claude-agent-sdk/post-tool-use-sdk.ts \
        packages/adapters/src/hook/claude-agent-sdk/__tests__/post-tool-use-sdk.test.ts
git commit -m "feat(m2.5): Task 4 — PostToolUse emit ai.override.ignored"
```

---

## Task 5: calibration-pipeline-v2 合成 Observations from complied events

**目标：** 在 `runCalibrationPipelineV2` 入口把 `ai.override.complied` events 转为内存 `Observation[]`，追加到 `deps.observations`，让规则在 AI 遵守警告时 confidence 上升。

**Files:**
- Modify: `packages/core/src/pipeline/calibration-pipeline-v2.ts`
- Modify: `packages/core/src/pipeline/__tests__/calibration-pipeline-v2.test.ts`

- [ ] **Step 1: 先写测试（红）**

在 `calibration-pipeline-v2.test.ts` 里加一组测试（在文件末尾加 describe block）：

```typescript
import type { PersistedEvent } from "@teamagent/types";

describe("ai.override.complied → synthetic Observation boosts confidence", () => {
  it("complied event creates synthetic success observation for target rule", async () => {
    const entry = makeEntry({ id: "rule-X", confidence: 0.1, demerit: 0 });
    const compliedEvent: PersistedEvent = {
      id: "e-complied-1",
      kind: "ai.override.complied",
      knowledge_id: "rule-X",
      tool_use_id: "t1",
      timestamp: new Date().toISOString(),
      schema_version: 1,
    };
    const result = await runCalibrationPipelineV2({
      calibrator: v2Calibrator,
      store: makeStore([entry]),
      events: [compliedEvent],
      observations: [],
      now: () => new Date(),
      dryRun: true,
    });
    // Should have adjusted rule-X (synthetic obs → confidence change)
    const adj = result.adjusted.find((a) => a.knowledge_id === "rule-X");
    expect(adj).toBeDefined();
    // confidence should be higher (or at least not lower) than before
    if (adj) {
      expect(adj.confidence_after).toBeGreaterThanOrEqual(entry.confidence);
    }
  });

  it("ignored event increases demerit for target rule", async () => {
    const entry = makeEntry({ id: "rule-Y", confidence: 0.5, demerit: 0 });
    const ignoredEvent: PersistedEvent = {
      id: "e-ignored-1",
      kind: "ai.override.ignored",
      knowledge_id: "rule-Y",
      tool_use_id: "t2",
      timestamp: new Date().toISOString(),
      schema_version: 1,
    };
    const result = await runCalibrationPipelineV2({
      calibrator: v2Calibrator,
      store: makeStore([entry]),
      events: [ignoredEvent],
      observations: [],
      now: () => new Date(),
      dryRun: true,
    });
    const adj = result.adjusted.find((a) => a.knowledge_id === "rule-Y");
    expect(adj).toBeDefined();
    if (adj) {
      expect(adj.demerit_after).toBeGreaterThan(0);
    }
  });
});
```

Note: `makeEntry` and `makeStore` should already exist in the test file — if not, use the pattern from existing tests.

- [ ] **Step 2: 跑测试，确认红**

```bash
pnpm --filter @teamagent/core test -- calibration-pipeline-v2
```

Expected: new tests fail (complied event doesn't affect confidence yet).

- [ ] **Step 3: 实现 complied event → Observation 合成**

在 `calibration-pipeline-v2.ts` 的 `runCalibrationPipelineV2` 函数里，在 `const obsIdx = indexByKnowledgeId(deps.observations)` 之前加：

```typescript
import type { Observation } from "@teamagent/ports";

// M2.5: ai.override.complied events → synthetic success observations
const syntheticObs: Observation[] = deps.events
  .filter((e) => e.kind === "ai.override.complied" && e.knowledge_id)
  .map((e) => ({
    id: `synth-complied-${e.id}`,
    knowledge_id: e.knowledge_id!,
    timestamp: e.timestamp,
    outcome: "success" as const,
    source_event: e.id,
    tool_use_id: e.tool_use_id,
  }));
const allObservations = [...deps.observations, ...syntheticObs];
```

然后把后续所有 `deps.observations` 改为 `allObservations`（只有 `indexByKnowledgeId(deps.observations)` 这一处需改）：

```typescript
const obsIdx = indexByKnowledgeId(allObservations);  // 原来是 deps.observations
```

- [ ] **Step 4: 跑所有 core 测试，确认绿**

```bash
pnpm --filter @teamagent/core test
```

Expected: all tests pass including new ones.

- [ ] **Step 5: Commit**

```bash
git add packages/core/src/pipeline/calibration-pipeline-v2.ts \
        packages/core/src/pipeline/__tests__/calibration-pipeline-v2.test.ts
git commit -m "feat(m2.5): Task 5 — calibration-pipeline-v2 合成 Observations from ai.override.complied"
```

---

## Task 6: `teamagent stats --override-signals`

**目标：** 显示每条规则的 `ai.override.ignored` 和 `ai.override.complied` 计数，按 ignored 数降序。

**Files:**
- Modify: `packages/cli/src/commands/stats.ts`
- Modify: `packages/cli/src/__tests__/stats.test.ts`
- Modify: `packages/cli/src/bin.ts`

- [ ] **Step 1: 先写测试（红）**

在 `stats.test.ts` 末尾加：

```typescript
describe("executeStats --override-signals", () => {
  it("renders override signal counts per rule", async () => {
    const tmp = mkdtempSync("stats-override-");
    const eventsDbPath = path.join(tmp, "events.db");
    const db = openDb(eventsDbPath);
    const log = new SqliteEventLog(db);

    const now = new Date().toISOString();
    log.append({ id: "e1", kind: "ai.override.ignored",  knowledge_id: "rule-A", tool_use_id: "t1", timestamp: now, schema_version: 1 });
    log.append({ id: "e2", kind: "ai.override.ignored",  knowledge_id: "rule-A", tool_use_id: "t2", timestamp: now, schema_version: 1 });
    log.append({ id: "e3", kind: "ai.override.complied", knowledge_id: "rule-A", tool_use_id: "t3", timestamp: now, schema_version: 1 });
    log.append({ id: "e4", kind: "ai.override.complied", knowledge_id: "rule-B", tool_use_id: "t4", timestamp: now, schema_version: 1 });
    db.close();

    const result = await executeStats({
      eventsDbPath,
      projectDbPath: path.join(tmp, "knowledge.db"),
      userGlobalDbPath: path.join(tmp, "global.db"),
      overrideSignals: true,
    });

    expect(result).toContain("rule-A");
    expect(result).toContain("ignored: 2");
    expect(result).toContain("complied: 1");
    expect(result).toContain("rule-B");
    expect(result).toContain("complied: 1");
  });
});
```

- [ ] **Step 2: 跑测试，确认红**

```bash
pnpm --filter @teamagent/cli test -- stats
```

Expected: new test fails ("overrideSignals is not a valid option" or similar).

- [ ] **Step 3: 在 `StatsOptions` 加 `overrideSignals?: boolean`，实现查询+渲染**

在 `stats.ts` 的 `StatsOptions` interface 里加：

```typescript
/** 展示 ai.override.ignored / complied 的统计 */
overrideSignals?: boolean;
```

在 `executeStats` 函数（或其 return 的字符串组装处），加 override signals 处理分支。`executeStats` 目前返回 `string`；我们在已有 events 读取逻辑后加：

```typescript
// 在 executeStats 里，已有 events 读取后加（与 stuckInPromotion 并列的 if 分支）：
if (opts.overrideSignals) {
  return renderOverrideSignals(events);
}
```

新增纯函数 `renderOverrideSignals`：

```typescript
export function renderOverrideSignals(events: PersistedEvent[]): string {
  // 聚合 per knowledge_id
  const counts = new Map<string, { ignored: number; complied: number }>();

  for (const e of events) {
    if (e.kind !== "ai.override.ignored" && e.kind !== "ai.override.complied") continue;
    const id = e.knowledge_id ?? "(unknown)";
    const entry = counts.get(id) ?? { ignored: 0, complied: 0 };
    if (e.kind === "ai.override.ignored") entry.ignored++;
    else entry.complied++;
    counts.set(id, entry);
  }

  if (counts.size === 0) {
    return "TeamAgent Override Signals\n\n  (无记录)\n";
  }

  const rows = [...counts.entries()]
    .sort((a, b) => b[1].ignored - a[1].ignored);

  const lines = ["TeamAgent Override Signals", ""];
  lines.push(
    "  Rule ID".padEnd(32) +
    "ignored".padEnd(10) +
    "complied",
  );
  lines.push("  " + "─".repeat(50));
  for (const [id, { ignored, complied }] of rows) {
    lines.push(
      `  ${id.slice(0, 30).padEnd(32)}ignored: ${String(ignored).padEnd(6)}complied: ${complied}`,
    );
  }
  lines.push("");
  return lines.join("\n");
}
```

- [ ] **Step 4: 跑测试，确认绿**

```bash
pnpm --filter @teamagent/cli test -- stats
```

Expected: all tests pass including new one.

- [ ] **Step 5: 在 `bin.ts` 里 parse `--override-signals` 并传入 `executeStats`**

在 `packages/cli/src/bin.ts` 的 stats case 里，找到 `parseStatsArgs`（或直接解析 `rest`），确保 `--override-signals` flag 被处理：

找到 `parseStatsArgs` 函数（在 `stats.ts` 里），加：

```typescript
else if (a === "--override-signals") opts.overrideSignals = true;
```

- [ ] **Step 6: 全套测试**

```bash
pnpm test
```

Expected: 64+ tests pass (only pre-existing Windows SQLite EBUSY flakes may fail).

- [ ] **Step 7: Commit**

```bash
git add packages/cli/src/commands/stats.ts \
        packages/cli/src/__tests__/stats.test.ts \
        packages/cli/src/bin.ts
git commit -m "feat(m2.5): Task 6 — stats --override-signals 展示 AI 遵守/违反警告统计"
```

---

## Task 7: 重建 Hook bundle + 端到端验证

**目标：** 把新代码打进 hook bundle，让实际 Claude Code Hook 生效。检查 pre/post hook 基本流程。

**Files:**
- Modify: 无（build artifact only）

- [ ] **Step 1: 重建 bundle**

```bash
pnpm --filter @teamagent/cli build:hook
```

Expected: `packages/cli/dist/bin-pre-tool-use.cjs` 和 `packages/cli/dist/bin-post-tool-use.cjs` 重新生成，无构建错误。

- [ ] **Step 2: 全 typecheck**

```bash
pnpm typecheck
```

Expected: no errors across all packages.

- [ ] **Step 3: 手动冒烟测试（PreToolUse — ignored 路径）**

```bash
# 模拟 PreToolUse warn：输入 hook-pre.warned 已有的场景
echo '{"tool_name":"Write","tool_input":{"content":"import axios from \"axios\""},"tool_use_id":"t-test-1","cwd":"C:/bzli/teamagent"}' \
  | node packages/cli/dist/bin-pre-tool-use.cjs
```

Expected: `{"permissionDecision":"allow"}` 或含 `systemMessage` 的 JSON（如果 axios 规则存在且 tier=warn）。无 stderr 崩溃即过。

- [ ] **Step 4: 手动冒烟测试（PostToolUse — 检测 ignored）**

```bash
# 模拟 PostToolUse（假设 events.db 里有近期 hook-pre.warned 事件）
echo '{"tool_use_id":"t-test-1","tool_response":{"is_error":false},"cwd":"C:/bzli/teamagent"}' \
  | node packages/cli/dist/bin-post-tool-use.cjs
```

Expected: `{}` 返回；stderr 无报错。

- [ ] **Step 5: Commit bundle（如 .cjs 在 git 追踪内）**

```bash
git add packages/cli/dist/
git commit -m "chore(m2.5): Task 7 — rebuild hook bundle with override signal logic"
```

如 `dist/` 在 `.gitignore`，略过此步。

---

## Task 8: Dogfood — 运行 `teamagent stats --override-signals` + 登记 pitfalls

**目标：** 在真实 events.db 上验证 override signals 功能端到端，登记本 Milestone 的 pitfall 经验。

- [ ] **Step 1: 跑 skeleton-demo**

```bash
pnpm teamagent skeleton-demo
```

Expected: 正常跑完，含 M2.4 的 compile 展示。

- [ ] **Step 2: 跑 override-signals 查询**

```bash
pnpm teamagent stats --override-signals
```

Expected: 输出 "Override Signals" 表格（可能为空，因为 events.db 里还没有新格式的 override 事件——这是预期行为）。

- [ ] **Step 3: 登记 pitfalls**

用 pitfall 命令登记至少 2 条本 Milestone 观察到的 pitfall，例如：

候选 pitfall 1：
- Trigger: `detectCompliedSignals`
- Correct: `detectCompliedSignals` 用 time window 而非 session_id，5 min 窗口内对同 tool_name 的 warn 被认为 complied；接受 false positive 风险
- Wrong: 尝试用 session_id 做精确 complied 判断——Claude Code 不保证 session_id 在 hook input 里

候选 pitfall 2：
- Trigger: `ai.override.ignored` / `complied`
- Correct: kind union 在 `packages/types/src/persisted-event.ts` 先加类型再 emit；DEMERIT_KIND_TO_SOURCE 已就绪，只需类型补全
- Wrong: 直接 emit 未声明 kind → TS 编译挂（M2.3 教训重演）

```bash
pnpm teamagent pitfall --non-interactive \
  --trigger="detectCompliedSignals time-window heuristic" \
  --correct="用 5 min 时间窗 + tool_name 匹配；接受 false positive" \
  --wrong="用 session_id 精确判断——hook input 不保证有 session_id"
```

- [ ] **Step 4: 跑全套测试**

```bash
pnpm test
```

Expected: 所有测试绿（Windows SQLite EBUSY 两条除外）。

- [ ] **Step 5: Commit**

```bash
git add CLAUDE.md .teamagent/
git commit -m "docs(m2.5): Task 8 — 登记 M2.5 dogfood pitfalls"
```

---

## Task 9: Milestone close-out

- [ ] **Step 1: 改顶部状态**

在本文档顶部加 Status 块（Tasks 1–8 完成后）：

```markdown
## Status: COMPLETED 2026-04-16

- 9 tasks merged to master
- 所有测试绿（Windows SQLite EBUSY 预存在 2 条不计入）、typecheck 绿
- PostToolUse 检测 ignored → ai.override.ignored event → demerit 上升
- PreToolUse clean pass 检测 complied → ai.override.complied event → 合成 Observation(success) → confidence 上升
- `teamagent stats --override-signals` 可查每条规则的遵守/违反计数
- Hook bundle 已重建
```

- [ ] **Step 2: 最终 commit**

```bash
git add docs/superpowers/plans/2026-04-16-m2.5-ai-override-signal.md
git commit -m "docs(m2.5): Milestone close-out — AI Override Signal 闭环 landed"
```

- [ ] **Step 3: 告知用户摘要**

输出：
- 新增 event kinds：`ai.override.ignored` / `ai.override.complied`
- 触发路径：PostToolUse (ignored) / PreToolUse clean pass (complied)
- Calibrator 集成：ignored → demerit，complied → 合成 success observation
- stats 新 flag：`--override-signals`
- 下一 Milestone：M2.6 Wiki Pipeline（SP-3 起点）

---

## Self-Review Checklist

| 检查项 | 设计来源 | 覆盖 Task |
|--------|---------|----------|
| `ai.override.ignored` / `complied` 新 event kind | phase2-design-v2 §5.4 | T1 |
| PostToolUse 检测 ignored | §5.4 伪代码 `signal === "ai.override.ignored"` | T4 |
| PreToolUse clean pass 检测 complied | §5.4 `signal === "ai.override.complied"` | T3 |
| `detectIgnoredSignals` / `detectCompliedSignals` 纯函数 | CLAUDE.md Functional Core | T2 |
| tool_name 存在 warned event 里 | 本 plan 设计决策 #3 | T3 |
| ai.override.ignored → demerit（`DEMERIT_KIND_TO_SOURCE` 已就绪） | calibrator/v2/index.ts | T1（type unlock）+ T4（emit） |
| ai.override.complied → 合成 Observation(success) → confidence | 本 plan 设计决策 #4 | T5 |
| `teamagent stats --override-signals` | design §5.4 `stats --override-signals --live`（M2.5 做静态版） | T6 |
| Hook bundle 重建 | CLAUDE.md 已知限制 | T7 |
| Port 先契约后实现（M0 元约束） | 纯函数先测试（T2）→ adapter 改 handler（T3/T4） | T2–T4 |
| 归因走 AttributionBus | 不适用：override signals 走 PersistedEvent（事件日志），不走 AttributionBus | — |
| Walking Skeleton 不断裂 | CLAUDE.md | T8 skeleton-demo |

**刻意延后：**
- `--live` streaming 实时显示（`teamagent stats --override-signals --live`）→ 需要 daemon 或 watch loop，M2.9 dogfood 期再加
- 精确 complied 检测（session_id 绑定）→ 需要 Claude Code 在 hook input 里稳定传 session_id，当前测试未证实，M2.6+ 观察后再决定
- Observations 表的真正 IO 接入（`SqliteObservations.readAll()` 传给 calibrate pipeline）→ 待 "T15-class" 任务（calibrate.ts 里有注释 `// T15 will add observations adapter`），M2.5 用 event→Observation 合成绕开

**假设与风险：**
1. **complied false positive**：AI 恰好调用同 tool_name 的 clean pass 时，可能触发不相关的 complied 信号。接受：complied 信号权重低（confidence 通过 Wilson LB 慢慢爬），而不是直接跳 tier。
2. **`tool_name` 字段序列化**：`SqliteEventLog` 把 CORE_KEYS 之外的字段写进 `payload` JSON，`readLast` 的 hydrate 函数会把它展回来。`tool_name` 不在 CORE_KEYS 里 → 走 payload → 兼容。
3. **hook bundle .cjs 不在 git 追踪**：如果 `dist/` 在 `.gitignore`，T7 Step 5 略过。用户每次 clone 后需手动 `pnpm build:hook`。

---

## 待用户确认点

1. **5 分钟时间窗口** (`windowMs = 300_000`)：AI 通常在同一个对话回合内 retry，5 分钟够用。如果你觉得太短/太长（比如有时写代码会思考很久），可以改为 `TEAMAGENT_OVERRIDE_WINDOW_MS` 环境变量，默认 5 分钟。现在先 hardcode，有问题再提？
2. **ignored → demerit 的语义争议**：设计doc 说 `addDemerit(rule, { source: "ai_ignored_warn" })`，直觉是"规则触发了但 AI 无视 → 规则可能 over-trigger"。但也可以解读为"规则 catch 到了真实违反 → 正向信号"。你倾向哪种语义？（当前按 design doc 走 demerit，但如果你觉得应该是 confidence up，M2.5 里改一行。）
