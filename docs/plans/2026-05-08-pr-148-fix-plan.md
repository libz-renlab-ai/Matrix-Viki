# PR-PLAN: fix PR #148 post-open issues

> Per project rule (`docs/PR-PLAN.md`): when issues surface after opening
> a PR, fix in the same PR — no follow-up-issue punt. Three sections per
> rule.

## ① Task description

PR #148 (`docs(playbooks): canned-answer purge + 61 md playbook migration`)
opened cleanly but Codex review + GitHub merge state surfaced two
blockers:

**Blocker 1 — merge conflict against main**: branch is 12 commits behind
`origin/main` (PRs #142 / #145 / #140 / #139 / #141 / #138 / #137 / #135
/ #136 / #134 / #133 / #132). Need a merge resolution that preserves both
intents: main's recent fixes + this branch's playbook migration.

**Blocker 2 — Codex P2 (`81a595e` reviewed)**: archiving `scripts/*.sh`
to `docs/legacy/judge-scripts/` removed entrypoints still referenced by
docs (e.g. `bash scripts/verify-all-rules.sh` in
`docs/rule-verify/INDEX.md`). Per Codex: either add compatibility
wrappers at old paths, **or** sweep-update all referenced commands.

Decision: **sweep-update**, not wrappers. Compatibility wrappers would
re-introduce the very `.sh` harness shape the new rule forbids. Each
referenced command should point to the corresponding
`docs/plans/<slug>/judge.md` md playbook (which now exists).

Out of scope:
- Fix Phase 2-surfaced regression #1 / #5 / #6 (already addressed in
  Phase 3 commits).
- Re-test 61 playbooks post-merge (Phase 2 ran on this branch's content;
  merge of unrelated main commits doesn't invalidate playbook bodies).
- Squash 36-commit history (separate cleanup).

## ② Expected outputs

- [ ] Merge commit on `worktree-mdplaybook` containing
      `Merge branch 'main' into worktree-mdplaybook` with conflicts
      resolved (no `<<<<<<<` markers in any file).
- [ ] All references in `*.md` to archived paths
      (`scripts/{verify,judge,duck,hook,user-collect}-*.sh`,
      `scripts/verify/tbrain-verify.sh`,
      `docs/<rule>/verify-canned-answer.sh`,
      `docs/canary-verify/*.sh`,
      `docs/feature-verify-kit/*.sh`,
      `docs/features/*/{run,prod,real,extraction,transfer}-judge.sh`,
      `docs/features/*/verify-canned-answer.sh` for the 61-archived subset)
      are either:
      - Updated to point at the corresponding `docs/plans/<slug>/judge.md`, or
      - Updated to a footnote noting "archived; see md playbook" with
        link, or
      - Removed if the surrounding section is now obsolete.
- [ ] `docs/rule-verify/INDEX.md` (the file Codex named) updated.
- [ ] `RULE-VERIFY` row in `docs/PROJECT-TOOLS.md` either removed (the
      orchestrator script is archived) or rewritten to dispatch md
      playbooks.
- [ ] No new `.sh` files introduced.
- [ ] After push: PR mergeable, CI green, Codex re-review on latest
      commit either silent or 👍.

Negative outputs (anti-goals):
- No compatibility shims at `scripts/*.sh` paths.
- No `--legacy-claude-md` regen of `<!-- TEAMAGENT:START -->` block.
- No `git reset --hard` / force-push.
- No revert of any Phase 1/2/3 commit.

## ③ Judge harness — md playbook (third-party)

Live at `docs/plans/2026-05-08-pr-148-fix/judge.md` (sibling of this
plan). Three sections:

- **§V1 RUN**:
  1. `git fetch origin main`
  2. `git merge origin/main` (no-ff)
  3. Resolve conflicts; assert no `<<<<<<<` markers in tree
  4. `grep -rEn "scripts/(verify|judge|duck-mode-verify|hook-prompt-verify|user-collect/run-v4-judge|verify/tbrain-verify)" --include='*.md' .` returns 0 matches outside `docs/legacy/`, `docs/plans/`, this plan, and the final report.
  5. `grep -rEn "docs/(canary-verify|feature-verify-kit)/.*\.sh|docs/[a-z-]+/verify-canned-answer\.sh|docs/features/[a-z0-9-]+/.*-judge\.sh" --include='*.md' .` likewise.
  6. `gh pr view 148 --json mergeable` → `MERGEABLE`.
  7. `gh pr checks 148` → all green.
  8. `env -u GITHUB_TOKEN gh api repos/libz-renlab-ai/TeamBrain/pulls/148/comments --jq '[.[] | select(.user.login == "chatgpt-codex-connector[bot]") | select(.created_at > "<this-iteration-start>")]'` → length 0 OR all entries 👍.
- **§V2 DUMP**: `.judge/pr-148-fix/<run_id>/judge.json` with
  `{exit_code, metrics: {merge_clean, dangling_ref_count, mergeable, ci_status, codex_p1_p2_open_count}, evidence_dir, stdout_path}`.
- **§V3 READ**: separate `claudefast -p` (or this lead agent if API
  down) reads only the JSON + evidence and emits PASS / FAIL / SKIP.
  PASS criteria: all §V1 assertions met; FAIL if any P1/P2 still open
  on Codex; SKIP if claudefast unavailable AND merge resolution
  irreducibly ambiguous.

## Execution

Two parallel sonnet workers (TEAMWORK `2+1+(0)` — 0 claudefast probes
because API is down):
- **Worker M** — merge resolution (touches all merging files, but
  *only* what merge surfaces).
- **Worker S** — doc sweep (touches `docs/**/*.md` references to
  archived paths, plus `docs/rule-verify/INDEX.md`,
  `docs/PROJECT-TOOLS.md`, `docs/legacy/judge-scripts/README.md`).

Lead (this agent) does final push + re-runs POSTPR loop; iterates if
Codex finds new items.
