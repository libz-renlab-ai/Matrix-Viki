---
Migrated-from: /Users/m1/.gstack/projects/liush2yuxjtu-v4-team-brain/m1-main-design-20260426-094157.md
Part: 02
Source-lines: 181-237 of 237
Tags: gstack, archive, user-level
---

If Gate 1 fails:

1. Disable Stop hook capture first.
2. Keep `/insight-search` read-only for 1 week so no useful local memory disappears.
3. Archive mirror jsonl for audit.
4. Remove automatic UserPromptSubmit injection.
5. Keep manual `/insight-add` only if teammates explicitly ask for it.
6. Write a failure memo naming which premise failed.

## Open Questions

1. Exact random sampling command for the 50-turn PII audit.
2. Whether weekly memos live in the mirror repo or `~/.gstack/projects/`.
3. Whether time-saved estimates should be exact minutes or buckets: 0-15, 15-60,
   1-4h, 4h+.
4. Whether a cross-engineer retrieval means different Unix user, different git author,
   or different Claude session owner.

## Success Criteria for This Runbook

- A founder can run Gate 1 without inventing the process mid-flight.
- A teammate can participate with normal Claude Code use plus occasional
  `/insight-rate`.
- Week 4 ends in one of two states: continue with evidence, or kill with evidence.

## Distribution Plan

No new distribution channel. Use the existing local marketplace install path and the
existing `claude plugin ...` command discipline from project rules. Gate 1 is
internal-only.

## Dependencies

- Existing B-scope plugin implementation in `/Users/m1/projects/insights-share/`.
- Mirror repo for the tested primary repo.
- Per-repo fine-grained PAT configured via `scripts/pat-auth.sh`.
- Gate 0 remains passing before live upload.
- Teammates know only one extra command: `/insight-rate`.

## The Assignment

Before the four-week run starts, write the first weekly memo template and test it on
one synthetic incident. The template must include: lesson ID, injecting session,
receiving session, rating, changed action, time saved, privacy audit result, and
latency sample.

Then start Gate 1. Do not add features during the 4 weeks unless a privacy issue or
hard outage appears.

## What I noticed about how you think

- You chose to continue without new observation data. That is a founder bet, and you
  did not pretend it was validated.
- You accepted a hard Gate 1 instead of asking for softer success criteria. That is
  the right kind of discomfort.
- You are still protecting the core thesis: humans are not the lesson carrier,
  Claude Code sessions are.
