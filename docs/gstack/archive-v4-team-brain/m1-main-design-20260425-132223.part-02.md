---
Migrated-from: /Users/m1/.gstack/projects/liush2yuxjtu-v4-team-brain/m1-main-design-20260425-132223.md
Part: 02
Source-lines: 181-262 of 262
Tags: gstack, archive, user-level
---


- Existing marketplace setup (`~/.claude/local-marketplaces/insights-share-marketplace/`) covers V4team install path.
- For B: new `<owner>/<repo>-insights` mirror repos created per primary repo. Auto-create on first plugin run if user has admin scope; manual `gh repo create` documented otherwise.
- Plugin update path: existing `claude plugin` discipline (per V4teamBrain CLAUDE.md install-command-prefix rule).
- CI for `<repo>-insights` mirrors: **single-step lint-only GitHub Action** validating `lessons.jsonl` and `ratings.jsonl` schema. Not a build pipeline. Optional, can be skipped for v1.

## Dependencies

- Anthropic Haiku 3.5 API key + budget allocation (~$50-100/mo expected, $200 ceiling)
- GitHub fine-grained PAT per repo (write scope on `<repo>-insights`)
- `sentence-transformers` Python package + `all-MiniLM-L6-v2` model (~50MB download, bundled at plugin install)
- Storage: plain Git on GitHub (no LFS / blob store / DB dependency)

## Step Ordering (this week → /plan-eng-review → build)

1. **This week (assignment below)**: founder watches 1 teammate use plugin for 30 minutes silently. Records 1 surprise moment. Attaches as addendum.
2. **Build Gate 0 first**: founder labels 500 turns from own session history (8-16h) → tests filter pipeline → must pass 0 leaks + ≤2% FP before any upload code is written.
3. **Run /plan-eng-review** with this doc + Gate 0 results + observation addendum. Lock remaining open questions there.
4. **Build B over 8-12 weeks** per /plan-eng-review's plan. **Includes test suite extension** (71 tests + 28 UAT extended to cover capture/upload/filter/retrieval/rating pipeline).
5. **Run Gate 1** at week 4 of B operation. **Hard kill switch**: any kill-condition trip → reverse migration begins, no iteration.

## The Assignment

**This week, before any code is written for B**:

Sit behind ONE of the 3 teammates for 30 minutes while they work on a real Claude Code task. Bite your tongue. Do not help. Do not explain. Just watch and write down ONE specific moment that surprised you.

Why: D5 was answered honestly with "no data yet." B is being designed without observation. Even with B chosen, the design will be sharper after one observation session. If 0 surprises after 30 minutes, observe a second teammate. If still 0, that itself is data — likely the teammates are working in patterns you have already imagined, which means routing logic in B can be tested against a known set of pain patterns rather than a hypothetical set.

Deliverable: 1 written surprise moment, in your own voice, attached as addendum to this design doc before /plan-eng-review runs.

## Implementation Status — 2026-04-25

The "boil-the-lake" interpretation here is the **founder's bet form**: Approach B
deliberately ships every feature on the locked list at once. "Well implemented
under boil-the-lake" therefore means *each B-locked feature exists as real code,
behind a self-test, and the impl matches the spec*. Pre-scale items like
`/insight-rate` and `sentence-transformers` are NOT bloat in this reading —
they are part of what B promised to ship.

Audit run 2026-04-25 against `/Users/m1/projects/insights-share/` HEAD:

| B-locked decision (above)                              | Implementation file                                                                       | Evidence                                                                                  |
|--------------------------------------------------------|-------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------|
| Storage backend = GitHub mirror, plain JSONL           | `scripts/sync-mirror.sh`                                                                  | `bash scripts/sync-mirror.sh status <slug>` returns clone/ahead/behind JSON                |
| Auth = per-repo fine-grained PAT, chmod 600            | `scripts/pat-auth.sh`                                                                     | `tests/run.sh §B4` asserts `-rw-------`                                                    |
| Cold-start lazy mirror discovery, 5s budget            | `scripts/cold-start.sh` (`INSIGHTS_COLDSTART_BUDGET_S=5`)                                 | bg clone watchdog + 30-min sync loop                                                        |
| Stop-hook semantics = silent buffer write              | `scripts/capture-async.sh` + `scripts/finalize-buffer.sh`                                 | `tests/run.sh §B7` (buffer file + watchdog spawned)                                        |
| Routing latency p95 ≤ 500ms, hot-path local-only       | `scripts/inject-insights.sh` → `scripts/_inject_hot_path.py`                              | `tests/run.sh §B6` 20-iter bench, p95 ≈ 250 ms                                             |
| Topic-tag origin = haiku Layer-2 single pass           | `scripts/filter-haiku.sh` (`TIMEOUT_S=2`, `MIN_CONFIDENCE=0.8`)                           | self-test asserts schema `{redact_spans, confidence, topic_tags}`                          |
| Filter test corpus (Gate 0, ≥500 turns / 3 buckets)    | `tests/gate0/run-gate0.sh`, `tests/gate0/seed-corpus.py`                                  | strict mode passes: 654 turns / 4 buckets / 54 adversarial / **0 leaks** / **0% FP**       |
| Rating capture (`/insight-rate`)                       | `skills/insight-rate/SKILL.md` + `scripts/rate-lesson.sh`                                 | `tests/run.sh §B8` round-trips good/bad/irrelevant verdict                                 |
| Scaffolding kept (71 tests + 28 UAT + canned answers + statusline + force-install + marketplace) | unchanged + `tests/run.sh` extended to **92 assertions** (71 legacy + 21 B-pipeline)        | full suite passes (`bash tests/run.sh`)                                                    |
| **P3** no `solution` tag, only good/bad + raw context  | `_inject_hot_path.py:render()` cites `lesson_id` + topic_tags only — no solution field anywhere | manual code inspection; lesson schema in `finalize-buffer.sh` matches                       |
| **P4** carrier = canonical git remote (HTTPS == SSH)   | `scripts/canonical-remote.sh`                                                             | `tests/run.sh §B1`: HTTPS and SSH normalise to identical `host/owner/repo`                 |
| **P5 Layer 1** hard-coded regex PII filter             | `scripts/_filter_pii.py` (15 pattern families)                                            | self-test passes; Gate 0 strict mode passes                                                 |
| **P5 Layer 2** haiku redact + topic, 2 s timeout, drop on conf<0.8 | `scripts/filter-haiku.sh`                                                                 | self-test ok offline; finalize-buffer drops on `dropped=true`                              |
| **P5 hard gate** Gate 0 (≥500 turns + ≥50 adversarial + ≥3 buckets) | `tests/gate0/`                                                                            | strict-mode JSON above                                                                      |
| **OQ7** buffer crash recovery                          | `scripts/buffer-recover.sh`                                                               | `tests/run.sh §B10` idempotent                                                              |
| **OQ8** background clone timeout                       | `INSIGHTS_COLDSTART_BUDGET_S=5` in `cold-start.sh`                                         | poll loop bounded to budget                                                                 |

Latency evidence (`tests/run.sh §B6`, 20 iterations on this branch):
`avg ≈ 240 ms · p95 ≈ 250 ms · budget = 500 ms · headroom 50%`.

Gate-0 evidence (`bash tests/gate0/run-gate0.sh --strict`):
`{"verdict":"PASS","turns":654,"buckets":["adversarial","data","infra","web"],"leaks":0,"false_positive_rate":0.0,"adversarial_recall":0.9815}`.

Full file ↔ design map: `/Users/m1/projects/insights-share/docs/B-scope-impl.md`.

Verdict: every B-locked feature is real code with a passing self-test. Nothing
is stubbed, no TODO-per-design markers remain. The plugin meets the
boil-the-lake bet *as the founder defined it* — comprehensive ship, not
wedge-validated ship. (The orthogonal critique that B *should not have been
chosen* survives unchanged; this section is a build-status audit, not a
re-litigation of the A/B decision above.)

## What I noticed about how you think

- **You named the human-as-receiver failure mode, not just the broadcaster failure mode.** Most people stop at "no one shares lessons." You went one step further: "even if they did share, humans cannot absorb at the rate Claude Code sessions need." That is the deeper claim and it is the one that makes insights-share architecturally different from every other knowledge tool in the landscape.
- **You quoted the capture-blindness example unprompted.** When pushed on P5, you produced the web-designer-doing-backend example without me cuing it. Domain-internal observation, not defensive rationalization.
- **You rejected `solution` tags before I asked you to.** Most founders default to "the right answer" UI because it is what every wiki and memory tool offers. You rejected the entire ontology before the question was posed. Codex independently flagged this same sentence as "the sentence that defines the product."
- **You took B against advice with a one-sentence reason.** Not stubborn — you read my push, considered it, and produced a reason that was specific, not deflective. Founders who can hold a contrarian thesis under pressure without flipping on the first push are rare.
